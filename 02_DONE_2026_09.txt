08/09/2026 16:45:50

Tâche : migration des colonnes et données de la table consultant vers la table user_account.
La table user_account remplace la table consultant (qui disparaît).
Le menu « Collaborateurs » pointe vers user_account ; le menu « Utilisateurs » disparaît.
Rôles : ADMIN voit/modifie tout · RESPONSIBLE_SOC voit/modifie les users de sa société sélectionnée ·
MANAGER voit/modifie les users qu'il manage dans la société sélectionnée · CONSULTANT ne voit/modifie
que son propre profil.

Implémentation :
- Backend — Flyway V41__consultant_merge_into_user_account.sql : ajout des colonnes RH
  (birth_date, social_number, position, base_salary, currency, nationality, hire_date,
  emergency_contact) dans user_account ; migration des données consultant → user_account
  (fusion dans les comptes liés via consultant_id, création de comptes role=CONSULTANT pour les
  consultants sans compte) ; re-pointage des FK (cra, note_frais, fiche_paie, activity, hr_document,
  histo_consultant) vers user_account.id ; suppression de user_account.consultant_id puis de la table
  consultant.
- Backend — suppression de l'entité Consultant et de ConsultantRepository ; UserAccount porte les
  colonnes RH ; Cra/NoteFrais/FichePaie/Activity référencent UserAccount ; consultantId du principal
  = user.id pour un CONSULTANT (CustomUserDetailsService) ; AuthService.toDto expose consultantId =
  id pour un CONSULTANT.
- Backend — ConsultantService réécrit (opère sur user_account) : findAll avec visibilité par rôle
  (visibleUsers), create/update/delete/purge, summaries/managedSummaries/managers, historique ;
  requireCanManage (ADMIN/RESPONSIBLE_SOC tout, MANAGER ses consultants, CONSULTANT lui-même).
- Backend — adaptation de CraService, NoteFraisService, FichePaieService, ActivityService,
  DashboardService, SocService, AuthService, DataSeeder, DocumentController, ConsultantImportService,
  FichePaiePdfService.
- Backend — username modifiable dans ConsultantService.update (avec contrôle d'unicité), soumis à
  requireCanManage.
- Frontend — types.ts (ConsultantDto/ConsultantRequest mis à jour) ; page Collaborateurs
  (Consultants.tsx) pointe vers user_account (colonnes : Collaborateur, Username, Contact, Rôle,
  Manager, Salaire, Embauche, Statut ; formulaire d'édition avec username) ; suppression de la page
  Users.tsx, de la route /utilisateurs et du menu « Utilisateurs » (MainLayout) ; Dashboard redirigé
  vers /consultants.
- Tests : backend — seul échec = SecurityIntegrationTest.adminCannotDeleteActiveUser (pré-existant,
  order-dependent) ; frontend build OK, tests 129/129, lint 0 erreur.

09/09/2026 12:30:19

Tâche : Indispos — statut PARTIEL, règles d'ajout et validation partielle par le manager.
Un CONSULTANT peut ajouter/maj une Indispo dans un mois : impossible si le mois contient déjà un CRA
(validé ou non) ; si le mois contient une Indispo « full » (tous les jours ouvrables ont une
indisponibilité valide) → impossible ; sinon (Indispo non full ou aucun) → ajout possible dans les
jours sans indisponibilité valide. Si l'Indispo contient des jours ouvrables non validés, il peut la
soumettre à son manager. Le manager peut valider tous les jours remplis en un clic, ou une partie
(case « valide » par activité). Statut : VALIDATED / REJECTED / PARTIAL. Le manager d'un user peut
toujours modifier un CRA ou une Indispo de son user, même validé ou PARTIEL. Historique des
modifications conservé (CraExchange).

Implémentation :
- Backend — statut PARTIAL ajouté à l'enum CraStatus.
- Backend — CraService.getOrCreate : règle métier assertIndispoAllowed (blocage si CRA du mois, ou
  Indispo « full » via isIndispoFull sur les jours ouvrables).
- Backend — CraService.save : autorise l'édition par le manager (isManagerOwner) des CRA/Indispos
  VALIDATED ou PARTIAL, et l'ajout par le consultant sur une Indispo VALIDATED/PARTIAL (jours sans
  indisponibilité valide, activités validées préservées via existingValidKeys).
- Backend — CraService.setActivityValid / setRangeValid : recalcul du statut (refreshIndispoStatus →
  VALIDATED / PARTIAL / SUBMITTED) ; requireManagerable étendu à VALIDATED/PARTIAL.
- Frontend — types.ts (CraStatus + PARTIAL), format.ts (CRA_STATUS_LABELS « Partiel », statusBadge
  info), CraDetail.tsx (PARTIAL traité comme validé pour l'édition consultant/manager, managerCanAct
  étendu, statusAdjective « partielle »), IndispoList.tsx (PARTIAL non supprimable).
- Tests : CraServiceTest 30/30 (ajout getOrCreate_indispoBlockedWhenCraExists,
  setActivityValid_partialSetsPartialStatus, setActivityValid_allValid_setsValidated) ; backend total
  225 tests (seul échec pré-existant adminCannotDeleteActiveUser) ; frontend build OK, tests 129/129,
  lint 0 erreur.

09/09/2026 14:42:49

Tâche : page Profil — ajouter les infos du manager (Nom complet, Username, Email, Tel, Role).

Implémentation :
- Backend — ajout du record UserDtos.ManagerInfo (id, fullName, username, email, phone, role) et du
  champ `manager` dans UserDtos.UserDto ; AuthService.toDto remplit le manager depuis
  user.getManager() (null si absent).
- Frontend — types.ts : ajout de l'interface ManagerInfo et du champ `manager` dans UserDto ;
  Profile.tsx : section « Manager » affichant Nom complet, Username, E-mail, Téléphone et Rôle du
  manager (affichée seulement si user.manager présent).
- Tests : backend 228 tests (seul échec pré-existant adminCannotDeleteActiveUser) ; frontend build OK,
  tests 129/129, lint 0 erreur.

09/09/2026 14:57:36

Tâche : page CRA/Indispo — le MANAGER (et RESPONSABLE_SOC) peut modifier un CRA ou une Indispo
validé(e). Le message devient « Ce CRA (ou cette Indispo) est validé. » sans « et n'est plus
modifiable » (ce suffixe reste pour le CONSULTANT). Le manager peut modifier/supprimer/valider/
invalider chaque événement (case à cocher), et valider ou invalider tout (bouton). Envoi au
consultant via notification (validate/reject) comme d'habitude.

Implémentation :
- Frontend — CraDetail.tsx : `managerEditsValidated` étendu au CRA (VALIDATED/PARTIAL), `managerCanAct`
  étendu à SUBMITTED/PENDING_SEND/VALIDATED/PARTIAL pour CRA et Indispo ; bannière dédiée au manager
  « Ce CRA/Cette Indispo est validé(e)/partiel(le). » (sans « n'est plus modifiable ») ; ajout du
  bouton « Invalider tout » (handleInvalidateAll via invalidateRange sur tout le mois) et relibellage
  « Valider » → « Valider tout ».
- Backend — CraService : setActivityValid/setRangeValid passent de requireManagerable (Indispo
  uniquement) à requireReviewable (CRA + Indispo, états SUBMITTED/PENDING_SEND/VALIDATED/PARTIAL).
- Tests : backend 228 tests (seul échec pré-existant adminCannotDeleteActiveUser) ; frontend build OK,
  tests 129/129, lint 0 erreur.

09/09/2026 15:18:14

Tâche : page CRA/Indispo — le manager (et responsable_soc) a 3 boutons en bas : « Valider tout »,
« Invalider tout », « Envoyer ». Valider tout coche tous les événements ouvrables ; Invalider tout
les décoche ; Envoyer demande un commentaire dans une dialog (textarea), sauvegarde, envoie la
notification puis ferme l'objet.

Implémentation :
- Frontend — CraDetail.tsx : 3 boutons manager (« Valider tout », « Invalider tout », « Envoyer »),
  suppression du bouton « Rejeter » (et de handleReject) ; nouveau composant SendBackModal (textarea
  obligatoire) + handler handleSendBack (doSave puis crasApi.sendBack puis fermeture via
  onClose/navigate) ; import Textarea.
- Frontend — cras.ts : ajout sendBack(id, comment) → POST /cras/{id}/send-back.
- Backend — CraController : endpoint POST /{id}/send-back ; CraService.sendBack (requireManagerOwnership,
  recordExchange + notifyConsultant, statut inchangé).
- Backend — « Invalider tout » corrigé : le précédent requireManagerable bloquait les CRAs ; setRangeValid
  utilise requireReviewable (CRA + Indispo) et fonctionne (test setRangeValid_invalidate_unchecksAllActivities).
- Tests : CraServiceTest 32/32 (ajout setRangeValid_invalidate_unchecksAllActivities,
  sendBack_recordsExchangeAndNotifiesConsultant) ; backend total 230 tests (seul échec pré-existant
  adminCannotDeleteActiveUser) ; frontend build OK, tests 129/129, lint 0 erreur.

09/09/2026 15:19:54

Tâche : page CRA/Indispo — les boutons « Valider tout », « Invalider tout », « Envoyer » doivent
être de même taille, sur la même ligne, centrés. Valider tout : vert ; Invalider tout : rouge ;
Envoyer : bleu.

Implémentation :
- Frontend — CraDetail.tsx : les 3 boutons utilisent désormais le même composant Button (même taille),
  dans un conteneur `flex w-full justify-center gap-3` (même ligne, centrés) ; couleurs : vert
  (bg-green-600), rouge (bg-red-600), bleu (bg-blue-600).
- Tests : frontend build OK, tests 129/129, lint 0 erreur.

10/09/2026 11:57:14

Tâche : un CRA ou une Indispo « semi valide » (contient des événements valides ET non valides) a le
statut PARTIEL.

Implémentation :
- Backend — CraService.refreshIndispoStatus : suppression du garde « CONGE uniquement » ; le statut
  est recalculé pour CRA ET Indispo (tous validés → VALIDATED, une partie → PARTIAL, aucun → SUBMITTED).
- Tests : ajout setActivityValid_partialCRA_setsPartialStatus (CRA partiellement validé → PARTIAL) ;
  CraServiceTest 33/33.

Tâche : page CRA/Indispo — en bas, 4 boutons centrés sur une ligne pour le MANAGER
(Enregistrer, Valider tout, Invalider tout, Envoyer) et 2 boutons pour le CONSULTANT
(Enregistrer, Envoyer). Le manager peut modifier une Indispo/CRA de son collaborateur (y compris
soumis), valider tout, invalider tout, valider partiellement. « Envoyer » : MANAGER/RESPONSABLE_SOC →
dialog (commentaire) + save + notification au propriétaire ; CONSULTANT → save + notification à son
manager.

Implémentation :
- Backend — CraService.save : managerEdits étendu à SUBMITTED/PENDING_SEND (le manager peut modifier
  un CRA/Indispo soumis).
- Frontend — CraDetail.tsx : managerEditsValidated étendu à SUBMITTED/PENDING_SEND ; boutons
  restructurés en un conteneur `justify-center gap-3` : « Enregistrer » (si canAddEvents),
  « Envoyer » (consultant, ex « Envoyer pour validation »), et pour le manager « Valider tout »,
  « Invalider tout », « Envoyer » (sendBack via SendBackModal).
- Tests : backend 231 tests (seul échec pré-existant adminCannotDeleteActiveUser) ; frontend build OK,
  tests 129/129, lint 0 erreur.

10/09/2026 12:11:07

Tâche : page CRA/Indispo — une Indispo « Annulée » (CANCELLED) doit afficher les 4 boutons
manager en bas : Enregistrer, Valider tout, Invalider tout, Envoyer.

Implémentation :
- Frontend — CraDetail.tsx : `managerCanAct` étendu à CANCELLED (ligne 296-302). Le bouton
  « Enregistrer » apparaît déjà via `editable` qui inclut CANCELLED ; les 3 autres boutons
  (Valider tout, Invalider tout, Envoyer) apparaissent désormais via `managerCanAct`.
- Tests : frontend build OK, tests 129/129, lint 0 erreur.

10/09/2026 12:20:52

Tâche : un CRA ou une Indispo « semi valide » (contient des événements valides ET non valides) doit
avoir le statut PARTIAL.

Implémentation :
- Backend — CraService.refreshIndispoStatus : passage d'un calcul au niveau du JOUR à un calcul au
  niveau de l'ÉVÉNEMENT (CraDayActivity). Désormais :
  - aucun événement rempli ou aucun événement valide → SUBMITTED ;
  - présence d'événements valides ET non valides (y compris au sein d'un même jour) → PARTIAL ;
  - tous les événements remplis valides → VALIDATED.
  Avant, un jour contenant à la fois des événements valides et non valides était considéré
  « validé » (allFilledValid au niveau jour), ce qui pouvait donner VALIDATED au lieu de PARTIAL.
- Tests : ajout setActivityValid_mixedEventsSameDay_setsPartialStatus (1 jour = 2 événements,
  1 validé + 1 non validé → PARTIAL) ; CraServiceTest 34/34.
- Vérification : backend 232 tests (seul échec pré-existant adminCannotDeleteActiveUser) ;
  backend redémarré 12:20:50.

10/09/2026 12:41:14

Tâche : renommer le statut PARTIAL en SEMI_VALID (chercher « partiel », « partial », « partielle »
dans front et backend) ; et à l'enregistrement d'un CRA/Indispo, recalculer le statut depuis ses
événements : tous valides → VALIDATED, tous non valides → REJECTED, mélange valides/non valides →
SEMI_VALID.

Implémentation :
- Backend :
  - CraStatus.java : `PARTIAL` → `SEMI_VALID`.
  - CraService.java : renommage `refreshIndispoStatus` → `recomputeStatusFromEvents` ; logique
    événement par événement : tous événements remplis valides → VALIDATED (+ validatedAt),
    présence de valides ET non valides → SEMI_VALID, aucun valide → REJECTED, sans événement rempli
    → on conserve le statut en cours. Appelée par setActivityValid / setRangeValid et par `save()`
    (sauf si le statut avant save est DRAFT — un consultant qui enregistre son brouillon ne passe pas
    en REJECTED).
  - V42__cra_status_semi_valid.sql : `UPDATE cra SET status = 'SEMI_VALID' WHERE status = 'PARTIAL';`
- Frontend : types.ts (SEMI_VALID), format.ts (libellé « Semi-validé », badge info), CraDetail.tsx
  (statusAdjective « semi-validée », managerEditsValidated / consultantAddsToValidated /
  managerCanAct / bannière), IndispoList.tsx (non-supprimable pour SEMI_VALID).
- Tests : renommés PARTIAL→SEMI_VALID ; ajout save_submittedWithAllEventsInvalid_setsRejected et
  save_submittedWithMixedEvents_setsSemiValid ; mise à jour
  save_validatedIndispo_consultantPreservesValidatedActivities (VALIDATED → SEMI_VALID, consultant
  qui ajoute un événement non valide à une Indispo validée). CraServiceTest 36/36.
- Vérification : backend 234 tests (seul échec pré-existant adminCannotDeleteActiveUser) ;
  frontend build OK, tests 129/129, lint 0 erreur ; Flyway V42 appliqué ; backend redémarré 12:41:04.

10/09/2026 14:39:41

Tâche : page CRA/Indispo — pour un CONSULTANT, le bouton « Envoyer » devient « Soumettre ». Au clic,
l'objet passe au statut SUBMITTED, est sauvegardé, et une notification est envoyée au manager.

Implémentation :
- Frontend — CraDetail.tsx : le bouton du consultant (rendu par `canAddEvents && !managerCanAct`,
  donc réservé au CONSULTANT) est renommé « Envoyer » → « Soumettre ». Le bouton « Envoyer » du
  MANAGER (sendBack) et celui de la SendBackModal restent inchangés.
- Backend — aucun changement : `CraService.submit(id)` fait déjà STATUS=SUBMITTED + save +
  recordExchange + notification + email au(x) manager(s) (notificationRecipients du consultant).
- Tests : frontend build OK, tests 129/129, lint 0 erreur (avertissement exhaustivedeps pré-existant
  sur CraDetail.tsx:186).

10/09/2026 14:58:25

Tâche : page CRA/Indispo — droits d'édition du CONSULTANT selon le statut :
- objet VALIDÉ : non modifiable ; mais si c'est une Indispo validée avec événements libres → le
  consultant peut les remplir (déjà en place).
- objet REJETÉ (aucun événement valide) : tout est modifiable (déjà en place : editable inclut REJECTED).
- objet SEMI-VALIDÉ : le consultant peut modifier les événements invalides et remplir les événements
  vides, mais ne peut pas modifier les événements valides.

Implémentation :
- Backend — CraService.save() : `statusEditable` accepte désormais SEMI_VALID pour tous les types
  (CRA non-congé inclus), pas seulement CONGE. La garde existante `existingValidKeys`
  (isConsultantPrincipal) empêche déjà le consultant de supprimer/modifier un événement validé.
- Frontend — CraDetail.tsx : nouveau drapeau `consultantEditsSemiValid`
  (isConsultant && status==='SEMI_VALID') ajouté à `canAddEvents` ; `consultantAddsToValidated`
  réduit au cas VALIDATED + Indispo ; bannière dédiée pour le SEMI-VALID consultant (modifier les
  invalides / remplir les vides puis soumettre, événements validés non modifiables) ; bandeau
  « n'est plus modifiable » masqué dans ce cas ; canModifyActivity garde le verrou des événements validés.
- Tests : nouveau CraServiceTest.save_semiValidCRA_consultantCanModifyInvalidAndPreservesValid
  (SEMI_VALID CRA → le consultant enregistre en conservant l'événement valide, statut reste
  SEMI_VALID). CraServiceTest 37/37.
- Vérification : backend 235 tests (seul échec pré-existant adminCannotDeleteActiveUser) ;
  frontend lint 0 erreur, tests 129/129, build OK ; backend redémarré 14:58:52.

11/09/2026 16:41:30

Tâche : Indisponibilité saisie durant les week-ends — actuellement, ajouter une indisponibilité sur
un mois saisit aussi les week-ends comme indisponibilité. Comportement attendu : les indisponibilités
sont interdites le week-end.

Implémentation :
- Frontend — CraDetail.tsx : `handleFillAllDays` (« Remplir tout le mois ») et `handleFillRange`
  (« Remplir une plage ») ne remplissent plus que les jours ouvrables (dayType === 'WORKED') ;
  les week-ends restent sans activité. Messages d'erreur ajustés (« Tous les jours ouvrables du mois
  sont déjà renseignés. » / « Aucun jour ouvrable à remplir dans cette plage (déjà renseigné ou
  week-end). »).
- Backend — CraService.save() : toute tentative d'enregistrer une activité sur un jour de type
  WEEKEND est rejetée (« Impossible de saisir une activité un week-end (date) »), pour CRA comme
  pour Indispo (défense en profondeur derrière l'UI).
- Tests : nouveau CraServiceTest.save_weekendActivity_isRejected → CraServiceTest 38/38.
- Vérification : backend 236 tests (seul échec pré-existant adminCannotDeleteActiveUser) ;
  frontend lint 0 erreur (warnings pré-existants), tests 129/129, build OK ; backend redémarré 16:42:00.

14/09/2026 10:39:21

Tâche : Ajout d'une nouvelle indisponibilité pour un mois totalement saisie — quand je saisis tout
le mois en indisponibilité, le clic sur le bouton « Nouvelle Indispo » ne fait rien.
Comportement attendu : soit un message d'information, soit le bouton désactivé.

Implémentation :
- Frontend — IndispoList.tsx : le bouton « Nouvelle Indispo » est désactivé (disabled + opacity,
  title « Une Indispo existe déjà pour ce mois ») dès qu'une Indispo (DRAFT, soumise, validée,
  semi-valide ou rejetée) existe déjà pour le mois sélectionné (`hasIndispoThisMonth`, calculée
  sur la liste `data` du consultant pour l'année en cours). Un message d'information est affiché
  sous le bouton : « Une Indispo existe déjà pour ce mois. Cliquez sur "Éditer" dans la liste
  ci-dessus. » L'utilisateur rouvre l'Indispo existante via le bouton « Éditer » / « Ouvrir » des
  lignes de la liste.
- Backend : aucun changement — `assertIndispoAllowed` renvoyait déjà une erreur explicite
  (« …ce mois est déjà complet » / « …un CRA existe déjà pour ce mois ») ; la cause du « il ne se
  passe rien » était l'absence de retour visuel dans le flux liste/consultant quand l'Indispo du
  mois existait déjà (danger : getOrCreate renvoyait le même mois sans effet apparent).
- Vérification : frontend lint 0 erreur (warnings pré-existants), tests 129/129, build OK.

14/09/2026 11:58:10

Tâche : Liste des activités possibles pour un consultant erronée — en tant que consultant, ajouter
une activité dans mon CRA remonte des activités en double et des activités destinées à d'autres
consultants. Résultat attendu : seules les activités qui concernent le consultant connecté remontent.

Implémentation :
- Backend — ActivityService.findAll : quand un consultantId est fourni (avec ou sans socId), ne
  renvoie que les activités partagées de la société (consultant == null) + celles assignées à ce
  consultant (consultant.id == consultantId). Les activités personnelles des autres consultants
  (ex. défauts CONGE_* créés par ensureDefaultActivitiesForConsultant pour chaque consultant)
  — source des doublons et des activités « pour les autres » — sont écartées. Sans consultantId
  (page de gestion ADMIN/RESPONSIBLE_SOC/MANAGER), le comportement est inchangé.
- Frontend — CraDetail.tsx : la requête des activités passe désormais `consultantId: cra.consultantId`
  dès que le consultant du CRA est connu (anciennement socId seul) ; la liste « fillable » 
  (filteredActivities) du CRA/Indispo ne propose donc que les activités partagées + celles du
  consultant concerné.
- Tests : nouveau ActivityServiceTest (findAll_avec_socId_et_consultantId garde partagées + miennes,
  findAll_socId seul rend tout, findAll_consultantId seul ne rend que les assignées). ActivityServiceTest 3/3,
  CraServiceTest 38/38.
- Vérification : backend 239 tests (seul échec pré-existant adminCannotDeleteActiveUser) ;
  frontend lint 0 erreur (warnings pré-existants), tests 129/129 (échecs Register transitoires
  d'ordre non reproductibles), build OK ; backend redémarré 11:58.

14/09/2026 14:47:28

Tâche : Message d'erreur « CRA Incomplet » — en tant que consultant, sélectionner un mois vide puis
cliquer sur « Nouveau CRA » affiche un CRA vide avec la bannière « CRA incomplet : 22 jours travaillés
ne totalisent pas 1 jour… ». Comportement attendu : un CRA vide s'affiche sans ce message ; le message
peut être sur l'info-bulle du bouton « Soumettre ».

Implémentation :
- Frontend — CraDetail.tsx :
  - Nouveau `hasAnyActivity` (au moins une activité sur le mois). La bannière rouge « CRA incomplet »
    n'est plus affichée sur un mois totalement vide (`!craValid && editable && hasAnyActivity`) —
    on ne prévient que lorsqu'un remplissage est **commencé** (jours partiellement saisis) ;
  - Le bouton « Soumettre » du consultant porte désormais une info-bulle (`title`) :
    « CRA incomplet : chaque jour travaillé doit totaliser 1 jour. » quand il est désactivé,
    « Soumettre pour validation » sinon.
  - `craValid` inchangé (tous les jours travaillés totalisent exactement 1 jour) ; Soumettre reste
    donc désactivé sur un mois vide, avec l'explication en info-bulle.
- Backend : aucun changement — `submit()` rejette déjà tout jour travaillé ≠ 1 jour (« Le CRA est
  incomplet : le jour … doit totaliser 1 jour »).
- Vérification : frontend lint 0 erreur (warnings pré-existants), tests 129/129, build OK.

----------------------------------------------------------------------
### 2026-09-14 15:26:00 — Impossible de réinitialiser un mot de passe (01_TODO_01)

**Bug 1 (sécurité grave)** : le lien de réinitialisation était affiché sur la page
« Mot de passe oublié » (`forgotPassword` retournait l'URL dans `ResetResponse`, et
`ForgotPassword.tsx` l'affichait). Aujourd'hui le lien n'est envoyé QUE par email.

- Backend (`AuthService.forgotPassword`) : `ResetResponse` ne contient plus l'URL
  (`UserDtos.ResetResponse(message)` — champ `resetUrl` supprimé). Plus aucune énumération
  de comptes : email inconnu → même message générique « Un email de réinitialisation a été
  envoyé » (plus de `ApiException.notFound` / 404).

**Bug 2 (404)** : le lien reçu menait à `/auth/reset-password?token=…` alors que la route
front était `/reset-password/:token` (et que `ResetPassword.tsx` lit le token via
`useSearchParams`).

- Backend : l'URL du lien envoyé par email devient `baseFrontendUrl() + "/reset-password?token=" + token`.
- Frontend : route `App.tsx` `/reset-password/:token` → `/reset-password` (le composant lit
  le token en query param).

**Fichiers touchés**
- Backend : `AuthService.java` (forgotPassword), `UserDtos.java` (ResetResponse sans url),
  `AuthServiceTest.java` (test création token : vérifie que le lien est dans le corps de
  l'email et contient `/reset-password?token=` ; test email inconnu renommé
  `forgotPassword_unknownEmail_doesNotDiscloseAccount` : message générique, aucun email envoyé).
- Frontend : `ForgotPassword.tsx` (plus d'affichage du lien), `App.tsx` (route),
  `api/types.ts` (ResetResponse = message seul).

**Vérification** : backend AuthServiceTest 33/33 + suite complète 239 tests (seul échec
pré-existant connu et documenté `adminCannotDeleteActiveUser` — dépendant de l'ordre) ;
frontend lint 0 erreur (warnings pré-existants), tests 129/129, build OK.

----------------------------------------------------------------------
### 2026-09-14 16:34:26 — Présence du bouton « Valider tout » sur un CRA déjà validé (01_TODO_01)

**Problème** : sur un CRA dont le statut est `VALIDATED`, le bouton « Valider tout » était
présent et actif ; le cliquer relançait un traitement de validation.

**Correctif (frontend `CraDetail.tsx`)** : le bouton « Valider tout » est désormais désactivé
(« grisé ») quand `cra.status === 'VALIDATED'` (`disabled` → `opacity-50` + `cursor-not-allowed`),
avec une info-bulle « CRA déjà validé » portée par un `span` englobant (les navigateurs
n'affichent pas le `title` d'un bouton désactivé). Aucun changement backend (le bouton
« Invalider tout » reste actif pour pouvoir revenir en arrière).

**Vérification** : frontend lint 0 erreur (warnings pré-existants), tests 129/129, build OK.

----------------------------------------------------------------------
### 2026-09-14 16:34:26 — Lien de réinitialisation reçu au format /auth/reset-password (retour utilisateur)

**Problème** : l'utilisateur a reçu (avant le redémarrage du backend à 15:35) un lien
`http://localhost:5173/auth/reset-password?token=…` (ancien format) qui aboutissait sur une
page 404, alors que la route est `/reset-password` (composant `ResetPassword.tsx` lit le
token via `useSearchParams`).

**Correctif (frontend `App.tsx`)** : ajout d'une route d'aliasing
`/auth/reset-password` → `<ResetPassword />` afin que les liens de l'ancien format, dont le
jeton reste valide en base, fonctionnent. Les nouveaux emails (backend redémarré avec le
code corrigé) envoient déjà `/reset-password?token=…`.

**Vérification** : frontend lint 0 erreur, tests 129/129, build OK.

----------------------------------------------------------------------
### 2026-09-14 16:58:24 — Manager : 4 boutons après Invalider tout (01_TODO_01)

**Problème** : en tant que manager, après avoir cliqué « Invalider tout » sur un CRA, seuls
les boutons « Enregistrer » et « Soumettre » apparaissaient. Les 4 boutons attendus :
Enregistrer, Valider tout, Invalider tout, Envoyer.

**Cause** : `handleInvalidateAll` appelle `invalidateRange` → le backend passe le CRA en
`REJECTED`. Or `managerCanAct` n'incluait pas `REJECTED`, donc les 3 boutons manager
(Valider tout, Invalider tout, Envoyer) et le bouton « Soumettre » (consultant) se
comportaient comme si c'était une vue consultant.

**Correctif (frontend `CraDetail.tsx`)** : `managerCanAct` inclut désormais
`cra.status === 'REJECTED'`. Impact :
- Les 3 boutons manager réapparaissent (Valider tout, Invalider tout, Envoyer) ✓
- Le bouton « Soumettre » (consultant) masqué pour le manager sur un CRA REJECTED ✓
- Le checkbox « Valid » dans le jour reste actif dans EventModal (comportement cohérent) ✓
- Le bouton « Valider tout » est désactivé uniquement quand `cra.status === 'VALIDATED'`
  (donc actif sur REJECTED — on peut re-valider après invalidation) ✓

**Vérification** : frontend lint 0 erreur (warnings pré-existants), tests 129/129, build OK.

----------------------------------------------------------------------
### 2026-09-14 17:25:05 — Manager : « Invalider tout » grisé quand tous les événements sont invalides (01_TODO_01)

**Problème** : sur la page CRA, quand tous les événements sont invalides, le bouton
« Invalider tout » restait actif.

**Correctif (frontend `CraDetail.tsx`)** :
- Nouveau calcul `allEventsInvalid = days.every(d => d.activities.every(a => !a.valid))`
  (vrai si aucun événement n'est valide, y compris CRA vide).
- Le bouton « Invalider tout » est désactivé (« grisé », `opacity-50` + `cursor-not-allowed`)
  quand `allEventsInvalid` est vrai, avec info-bulle « tout est invalide » portée par un
  `span` englobant (les navigateurs n'affichent pas le `title` d'un bouton désactivé).
- Le calcul est réactif à l'état local `days` : re-valider une activité via la case « Valid »
  réactive le bouton.

**Vérification** : frontend lint 0 erreur (warnings pré-existants), tests 129/129, build OK.

----------------------------------------------------------------------
### 2026-09-15 10:43:11 — Processus de sauvegarde périodique de la BDD (01_TODO_01)

**Tâche** : un process sauvegarde la BDD toutes les x heures, x défini dans les properties
du serveur (par défaut x = 2). H2 : `SCRIPT TO '<fichier>.sql';`. PostgreSQL sur Azure :
`PGSSLMODE=require pg_dump -h <host> -p 5432 -U <user> -d <db> -Fc -f <fichier>.dump`.

**Implémentation**
- Backend — `com.soc360.batch.DbBackupService` : `@Scheduled(fixedDelayString =
  "${app.backup.interval-hours:2}h", initialDelayString = "1m")` — Spring 6.2 parse la durée
  simple « 2h » (`DurationFormatterUtils.detectAndParse`). Première sauvegarde 1 minute après
  le démarrage, puis toutes les x heures (délai fixe entre la fin d'un run et le début du suivant).
- Moteur détecté automatiquement via `spring.datasource.url` :
  - H2 (`jdbc:h2:*`) → exécution JDBC de `SCRIPT TO '<chemin absolu en />'` → fichier
    `soc360-backup-<yyyyMMdd-HHmmss>-<nanos>.sql` (suffixe nanos pour garantir l'unicité
    même dans la même seconde) ;
  - PostgreSQL (`jdbc:postgresql://…`) → commande `pg_dump -h <host> -p <port> -U <user> -d <db>
    -Fc -f <…>.dump`, host/port/base extraits de l'URL JDBC (port défaut 5432), `PGSSLMODE=require`
    et `PGPASSWORD` injectés dans l'environnement du process enfant (ProcessBuilder), stderr+réunis
    pour loguer le détail en cas d'échec.
- Répertoire `app.backup.directory` (défaut `./data/backups`) créé à la volée ; rétention des N
  dernières sauvegardes (`app.backup.retain`, défaut 30) : les plus anciennes sont supprimées.
- `application.yml` : nouvelle section `app.backup.{interval-hours, directory, retain}`, pilotable
  par variables d'environnement `BACKUP_INTERVAL_HOURS`, `BACKUP_DIRECTORY`, `BACKUP_RETAIN`.
- Isolation multi-société : sans objet (sauvegarde infra, aucun objet par société manipulé).
- Aucun changement Flyway ni frontend (nécessaire).

**Vérification** : nouveau `DbBackupServiceTest` (5 tests : dump H2 réel contenant la table
user_account, rétention retain=2 appliquée via runBackup, parsing URL pg avec port+query string,
sans port, URL invalide → null) ; backend : 244 tests, seul échec pré-existant connu
`adminCannotDeleteActiveUser` (order-dependent) ; builds H2 local OK (fichiers générés dans
`data/backups`).

----------------------------------------------------------------------
### 2026-09-15 11:01:00 — Activité week-end dans un CRA (01_TODO_02)

**Tâche** : un consultant peut ajouter une activité durant le week-end dans son CRA. Il ajoute
manuellement (jour par jour), mais PAS via les boutons « Remplir une plage » / « Remplir tout
le mois » (qui restent limités aux jours ouvrables). Comportement inchangé pour les Indispo
(toujours interdites le week-end).

**Implémentation**
- Backend — `CraService.save()` : le rejet « Impossible de saisir une activité un week-end »
  n'est plus appliqué qu'aux Indispos (`conge == true`) ; un CRA peut donc enregistrer une
  activité sur un jour `WEEKEND`. Contrôles de complétude inchangés (seuls les jours `WORKED`
  doivent totaliser 1 jour : `submit()`, `recomputeStatusFromEvents`), l'export ne tient toujours
  compte que des jours travaillés.
- Frontend — `CraDetail.tsx` : le bouton « + » (ajout manuel d'événement) de la vue calendrier
  est désormais aussi affiché sur les jours `WEEKEND` quand il s'agit d'un CRA
  (`!isIndispo`) ; pour une Indispo, le « + » reste réservé aux jours `WORKED`.
- `handleFillAllDays` / `handleFillRange` **inchangés** : ils continuent de ne remplir que les
  jours `dayType === 'WORKED'`.

**Vérification** : nouveau `CraServiceTest.save_weekendActivityInCRA_isAllowed` (CRA : activité sur
un jour week-end enregistrée, 1 activité présente après save) ; `save_weekendActivity_isRejected`
(Indispo) conserve le rejet. CraServiceTest 39/39 ; backend 245 tests (seul échec pré-existant
`adminCannotDeleteActiveUser`) ; frontend lint 0 erreur (warnings pré-existants sur docs/),
tests 129/129, build OK.

----------------------------------------------------------------------
### 2026-09-15 11:53:29 — Valider tout / Invalider tout sur un CRA validé (01_TODO_01)

**Tâche** : en tant que manager (ou responsable_soc), j'ouvre un CRA validé → le bouton
« Valider tout » doit être désactivé et le bouton « Invalider tout » actif. Si je clique sur
« Invalider tout », celui-ci devient inactif et « Valider tout » devient actif (et vice-versa).

**Implémentation** : nouveau test frontend `src/pages/CraDetail.test.tsx` qui reproduit le
scénario — ouverture d'un CRA VALIDATED par un MANAGER (managerId = user.id), puis bascule des
boutons « Valider tout » / « Invalider tout » → VERIFIÉ côté UI uniquement : le composant
`CraDetail` avait déjà la logique (désactivation par `cra.status === 'VALIDATED'` et
`allEventsInvalid`), aucun code de production à modifier.

**Vérification** : test vert (bascule, assertions `toBeDisabled`/`toBeEnabled`, appels
`invalidateRange(1, '2026-08-01', '2026-08-31')` et `validate(1)`) ; frontend 130/130 tests,
lint 0 erreur sur le fichier ajouté (warnings pré-existants docs/).

----------------------------------------------------------------------
### 2026-09-15 15:41:10 — Connexion via email (01_TODO_01)

**Tâche** : en tant qu'utilisateur, je souhaite me connecter en saisissant mon email ou bien mon
username.

**Implémentation**
- Backend — `CustomUserDetailsService.loadUserByUsername()` : résolution de l'identifiant par
  `findByUsername` puis repli sur `findByEmail` (`Optional.or(...)`) → l'`AuthenticationManager`
  (authentification Spring Security) accepte username OU email.
- Backend — `AuthService.login()` : relance de recherche après authentification par
  `findByUsername` avec repli sur `findByEmail` ; les `recordConnection` et
  `loadUserByUsername` utilisent ensuite `user.getUsername()` (l'identifiant canonique), la
  connexion est donc journalisée sous le username réel.
- Frontend — `Login.tsx` : le champ devient « Email ou identifiant » (placeholder
  « email ou identifiant ») ; le payload envoyé reste `{ username, password }` (le backend
  accepte l'email dans ce champ).
- `LoginRequest` / `LoginPayload` inchangés (aucune migration Flyway nécessaire).

**Vérification** : nouveau `AuthServiceTest.login_withEmail_resolvesUserByEmail` (connexion avec
l'email `jean.dupont@soc360.fr`, token généré depuis le username `jdupont`, connexion journalisée
sous `jdupont`) ; AuthServiceTest 34/34 ; backend 246 tests, seul échec pré-existant
`adminCannotDeleteActiveUser` (vert en isolation) ; frontend `Login.test.tsx` mis à jour
(placeholder), 130/130 tests, lint 0 erreur sur `Login.tsx`.

----------------------------------------------------------------------
### 2026-09-15 16:26:33 — ERROR.txt : échec deploiement GitHub Pages dev (tsc TS2741)

**Erreur** : exécution de `deploy_front_to_gh_pages.sh` bloquée au build :
`src/pages/CraDetail.test.tsx(134,43): error TS2741: Property 'id' is missing in type '{}' but
required in type '{ id: number; onClose?...; onChange?... }'`.

**Cause** : `CraDetail` exige la prop `id` (obligatoire dans sa signature ; l'application la passe
via `CraDetailRoute`) et le test `CraDetail.test.tsx` rendait `<CraDetail />` sans prop. Vitest
(esbuild) ne fait pas de type-check d'où un test vert malgré le code invalide pour `tsc -b`.

**Implémentation** : dans `soc360-front-react/src/pages/CraDetail.test.tsx`, le rendu de test passe
désormais `<CraDetail id={1} />` dans un `MemoryRouter` (imports `Routes`/`Route` supprimés).

**Vérification** : `npm run build:pages` (tsc -b + vite) OK ; `./deploy_front_to_gh_pages.sh dev`
réussi de bout en bout (lint, 130/130 tests, build, commit `docs/` + push) → site dev en ligne.
Commits front `37dbcb6` (correctif) et `1baa355` (build docs/) ; `ERROR.txt` vidé (compte rendu
archivé ici).

----------------------------------------------------------------------
### 2026-09-15 23:19:39 — Gestion des indisponibilités (01_TODO_01)

**Tâche** : les règles suivantes ont été convenues concernant la gestion des indisponibilités des consultants :

1. **Définition d'une indisponibilité** — une indisponibilité correspond à un intervalle de jours consécutifs, défini par une date de début et une date de fin, et associé à un même type d'indisponibilité. Elle peut s'étendre sur plusieurs mois, comme dans le cas d'un congé maternité.
2. **Soumission des indisponibilités** — le consultant peut soumettre plusieurs indisponibilités, même si le manager n'a pas encore répondu à une demande précédente.
3. **Contrôle des chevauchements** — lorsqu'un consultant soumet une indisponibilité, l'application vérifie que l'intervalle de dates ne chevauche pas une indisponibilité déjà soumise ou validée.
4. **Annulation d'une indisponibilité** — le consultant peut annuler une indisponibilité déjà soumise. Celle-ci repasse alors à l'état de brouillon et peut être modifiée ou soumise à nouveau.
5. **Visualisation dans le calendrier** — en cliquant sur un élément de la liste des indisponibilités, le consultant peut visualiser, dans la partie inférieure de l'écran, l'intervalle de dates correspondant dans un calendrier. Le calendrier peut afficher plusieurs mois afin de représenter les indisponibilités de longue durée ; les autres événements du consultant (congés, missions) doivent également être visibles ; l'intervalle de l'indisponibilité sélectionnée doit être clairement mis en évidence par un encadrement bleu.

**Implémentation**
- Backend (soc360-back-java) — nouvelle fonctionnalité dédiée `unavailability` (package `com.soc360.unavailability`) : entité `Unavailability`, `UnavailabilityRepository`, `UnavailabilityService`, `UnavailabilityController` (REST `/api/unavailabilities`), DTOs (`UnavailabilityDto`, `UnavailabilityRequest`, `RejectRequest`).
- Migration Flyway `V43__unavailability.sql` : table `unavailability` (`consultant_id`, `id_soc`, `type`, `start_date`, `end_date`, `status`, `comment`, `rejected_reason`, `submitted_at`, `validated_at`, `CHECK end_date >= start_date`, index sur `(id_soc)` et `(consultant_id)`) + INSERT des permissions `UNAVAILABILITY_*` pour les bases existantes.
- Enums : `UnavailabilityStatus` (DRAFT, SUBMITTED, VALIDATED, REJECTED), `UnavailabilityType` (CONGE_PAYE, CONGE_RTT, CONGE_NON_PAYE, CONGE_MALADIE, CONGE_MATERNITE) ; `Feature.UNAVAILABILITY` ajouté.
- Règles métier : le consultant peut soumettre plusieurs indisponibilités sans attendre la réponse du manager ; contrôle de chevauchement à la soumission (409 CONFLICT contre les SUBMITTED/VALIDATED) ; annulation d'une soumission → retour DRAFT puis modification/resoumission ; validation/rejet réservés aux rôles non-consultant (motif de rejet optionnel) ; suppression limitée aux brouillons/rejetés ; la soumission modifie dates/commentaire/type sans changement de statut.
- Isolation multi-société : `SecurityUtils.effectiveSocId` / `requireSocAccess` ; un consultant ne voit/crée/annule que ses propres indisponibilités.
- Notifications : à la soumission → managers/responsables de la soc ; à la validation/rejet → le consultant concerné (`NotificationRepository` + `EmailService`).
- `DataSeeder.seedPermissions` rendu idempotent (comble uniquement les combinaisons manquantes au lieu de tout réinitialiser) — corrige la régression qu'aurait causée l'INSERT de permissions de V43 sur une base vierge.
- Frontend (soc360-front-react) — page `/indisponibilites` (`pages/Unavailability.tsx`) : liste filtrable (texte + type, filière pagination `user.pageSize`), formulaire créer/éditer (type, dates, commentaire), actions Soumettre / Annuler (→ DRAFT) / Valider / Rejeter / Supprimer, sélecteur de consultant pour les managers ; module `api/unavailability.ts` ; types `UnavailabilityStatus/Types/Dto/Request` dans `api/types.ts` ; labels dans `lib/format.ts` ; route dans `App.tsx` ; entrée de navigation « Indisponibilités » dans `MainLayout`.
- Calendrier — `pages/UnavailabilityCalendar.tsx` : affichage multi-mois (jusqu'à 3 colonnes, avec marge d'un mois si l'intervalle <= 5 mois), intervalle sélectionné encadré en bleu, autres indisponibilités en violet, congés (CRA type CONGE) en ambre, jours de mission (CRA travaillés) en vert ; les événements existants (congés/missions) sont chargés via les endpoints CRA existants (utilisés par la sélection de consultant).

**Vérification** : `UnavailabilityServiceTest` (6 tests : liste isolée par consultant, création brouillon, chevauchement rejeté, annulation → DRAFT, validation par manager, édition d'un rejeté) ; backend 255 tests, seul échec pré-existant non lié `SecurityIntegrationTest.adminCannotDeleteActiveUser` (dépendant de l'ordre, vert en isolation, échoue aussi sans ces changements) ; frontend `lint` 0 erreur (warnings pré-existants sur docs/), `tsc -b` OK, 130/130 tests.

----------------------------------------------------------------------
### 2026-09-16 14:59:18 — Sauvegarde BDD : nombre maximum de fichiers de backup (01_TODO_01)

**Tâche** : sauvegarde de la BDD — le nombre maximum de fichiers SQL de backups est `x = 5`, défini dans le fichier paramètres du serveur.

**Implémentation**
- Backend — `application.yml` : la propriété de rétention `app.backup.retain` (défaut 30) est remplacée par `app.backup.max-files: ${BACKUP_MAX_FILES:5}` — maximum de fichiers de sauvegarde conservés, pilotable par la variable d'environnement `BACKUP_MAX_FILES`.
- Backend — `com.soc360.batch.DbBackupService` : le champ `retain` devient `maxFiles` (`@Value("${app.backup.max-files:5}")`) ; `retainBackups()` supprime les sauvegardes les plus anciennes (`.sql` H2 et `.dump` PostgreSQL confondus) au-delà de `maxFiles`.
- Isolation multi-société : sans objet (sauvegarde infra, aucun objet par société).
- Aucun changement Flyway ni frontend nécessaire.

**Vérification** : `DbBackupServiceTest` 5/5 (dont `backupH2_retainsOnlyConfiguredNumberOfFiles` avec `app.backup.max-files=2` → au plus 2 fichiers conservés après 4 sauvegardes) ; `mvn -q test -Dtest=DbBackupServiceTest` OK.

----------------------------------------------------------------------
### 2026-09-16 15:03:33 — Gestion des indisponibilités : revue et mise en conformité (01_TODO_02)

**Tâche** : revoir et éventuellement refaire la gestion des indisponibilités selon les règles convenues :
1. **Définition** — une indisponibilité est un intervalle de jours consécutifs (date de début + date de fin) associé à un même type ; elle peut s'étendre sur plusieurs mois (ex. congé maternité).
2. **Soumission** — le consultant peut soumettre plusieurs indisponibilités, même si le manager n'a pas répondu à une précédente.
3. **Chevauchements** — à la soumission, l'application vérifie que l'intervalle ne chevauche pas une indisponibilité déjà soumise ou validée.
4. **Annulation** — le consultant peut annuler une indisponibilité soumise : retour au brouillon, puis modification / nouvelle soumission possibles.
5. **Calendrier** — cliquer sur un élément de la liste affiche l'intervalle dans un calendrier en bas d'écran ; le calendrier peut montrer plusieurs mois, les autres événements (congés, missions) sont visibles, et l'intervalle sélectionné est encadré en bleu.

**Revue** : la fonctionnalité dédiée `unavailability` (table `unavailability`, package `com.soc360.unavailability`, page `/indisponibilites`) répond déjà aux 5 règles — intervalle/type (règle 1), soumissions multiples non bloquées (règle 2), contrôle de chevauchement à la soumission contre SUBMITTED/VALIDATED (règle 3), annulation soumise → DRAFT puis éditable/resoumettable (règle 4), calendrier multi-mois avec congés CRA (CONGE) et missions CRA visibles + encadrement bleu de l'intervalle sélectionné (règle 5). Trois faiblesses relevées et corrigées.

**Implémentation (corrections)**
- Frontend — `UnavailabilityCalendar.tsx` : l'arithmétique de dates (bornes des mois, énumération des jours de l'intervalle) utilisait des `Date` locales + `toISOString()`/`setDate()`, sensible aux changements d'heure (risque de jour dupliqué/omis aux passages DST). Remplacée par un calcul pur en composants `{y,m,d}` + `Date.UTC` (`parseDate`, `dateKey`, `addDays`, `daysBetween`, `monthWindow`) ; la clé de cellule du mois utilise `dateKey(year, month, day)`.
- Frontend — `Unavailability.tsx` : la création par un manager/admin exige désormais la sélection d'un consultant (message « Sélectionnez un consultant. ») au lieu de laisser la requête échouer côté serveur.
- Aucune modification de schéma (Flyway V43 déjà en place), aucune duplication du flux CRA `CONGE` existant (`/indispos`), isolation multi-société inchangée (`effectiveSocId`/`requireSocAccess`, un consultant ne voit que les siennes).

**Vérification**
- Backend — nouveau test `UnavailabilityServiceTest.submit_allowsMultipleNonOverlappingSubmissionsWhileOneIsPending` (règle 2) ; `UnavailabilityServiceTest` 7/7 ; suite complète 256 tests, seul échec pré-existant non lié `SecurityIntegrationTest.adminCannotDeleteActiveUser` (order-dependent, vert en isolation). `DbBackupServiceTest` 5/5 (tâche 01 précédente).
- Frontend — nouveau `src/pages/Unavailability.test.tsx` (5 tests : liste + calendrier de l'intervalle sélectionné [règle 5], soumission [règle 2], annulation retour brouillon [règle 4], validation manager, garde consultant à la création) ; suite complète 15 fichiers / 135 tests OK ; `lint` 0 erreur (warnings pré-existants sur docs/) ; `tsc -b` OK.

----------------------------------------------------------------------
### 2026-09-16 15:13:03 — Page CRA / Indispos : activation conditionnelle de « Valider tout » et « Invalider tout » (01_TODO_01)

**Tâche** : page CRA / Indispos — en tant que manager, le bouton « Valider tout » ne devient actif que s'il existe au moins un événement inactif ; le bouton « Invalider tout » ne devient actif que s'il existe au moins un événement actif.

**Implémentation**
- Frontend — `CraDetail.tsx` : suppression de `allEventsInvalid` (qui ne testait que « tout est invalide ») au profit de deux indicateurs sur les activités des jours : `hasInactiveEvent = days.some(d => d.activities.some(a => !a.valid))` et `hasActiveEvent = days.some(d => d.activities.some(a => a.valid))`.
- « Valider tout » : `disabled={!hasInactiveEvent}` — actif dès qu'au moins un événement est inactif (auparavant désactivé sur `cra.status === 'VALIDATED'`).
- « Invalider tout » : `disabled={!hasActiveEvent}` — actif dès qu'au moins un événement est actif (auparavant désactivé seulement quand tous les événements étaient invalides).
- Attributs `title` mis à jour : « aucun événement à valider » / « aucun événement à invalider ».
- Logique commune aux CRA et aux Indispos (même composant `CraDetail`), managers/responsables uniquement (`managerCanAct`).
- Aucun changement backend ni Flyway : le statut `valid` des activités provient déjà de l'API ; logique purement UI.

**Vérification** : `CraDetail.test.tsx` — test existant de bascule Valider/Invalider conservé (CRA validé ↔ CRA rejeté) et 2 nouveaux tests : coexistence d'événements actifs et inactifs → les deux boutons actifs ; absence de tout événement → les deux boutons inactifs. Frontend : `lint` 0 erreur (warnings pré-existants sur docs/), `tsc -b` OK, 15 fichiers / 137 tests.

----------------------------------------------------------------------
### 2026-09-16 15:40:49 — Page Indispo : validation/rejet avec commentaire + ajout de plage + historisation (01_TODO_01)

**Tâche** : page indispo — en tant que manager, je peux valider ou rejeter une demande d'indispo en ajoutant un commentaire avant d'envoyer la notification à mon consultant ; je peux aussi ajouter une autre plage d'indisponibilités ; tout doit être historisé (`date_modif_indispo`, `id_indispo`, `modifier`, `comment`, `nb_events_before`, `nb_events_after`).

**Implémentation**
- Backend — migration Flyway `V44__unavailability_history.sql` : table `unavailability_history` (`id`, `date_modif_indispo`, `id_indispo` FK → `unavailability` `ON DELETE CASCADE`, `modifier` FK → `user_account` `ON DELETE SET NULL`, `comment`, `nb_events_before`, `nb_events_after`) + index sur `id_indispo`.
- Backend — entité `UnavailabilityHistory` + `UnavailabilityHistoryRepository` (`findByUnavailabilityIdOrderByIdAsc`).
- Backend — `UnavailabilityService` : injection du repository d'historique ; `recordHistory(...)` appelé sur création (0 → N jours), modification (N → M), soumission, annulation de la soumission, validation et rejet ; `modifier` = utilisateur courant ; `comment` préfixé de l'action (« Création », « Modification : … », « Validation : … », « Rejet : … », « Soumission », « Annulation de la soumission »).
- Backend — `validate(Long id, String comment)` et `reject(Long id, String comment)` : le commentaire du manager est ajouté avant l'envoi de la notification et repris dans le corps de celle-ci (« Commentaire : … » / « Motif : … »).
- Backend — `history(Long id)` : lecture de l'historique avec contrôle d'accès `SecurityUtils.requireSocAccess` (isolation multi-société).
- Backend — DTOs : `DecisionRequest(comment)` (remplace `RejectRequest`) ; `HistoryDto(id, dateModifIndispo, unavailabilityId, modifierId, modifierName, comment, nbEventsBefore, nbEventsAfter)`.
- Backend — `UnavailabilityController` : `POST /{id}/validate` et `POST /{id}/reject` acceptent un corps `{ "comment": "…" }` ; nouveau `GET /{id}/history`.
- Frontend — `api/unavailability.ts` : `validate(id, comment)`, `reject(id, comment)`, `history(id)` ; `types.ts` : `UnavailabilityHistoryDto`.
- Frontend — `pages/Unavailability.tsx` : saisie d'un commentaire (prompt) avant validation/rejet, envoyée dans la notification ; bouton « Historique » par ligne + modale (date, modifié par, commentaire, nb d'événements avant/après) ; sélecteur de consultant dans le formulaire pour permettre au manager d'ajouter une autre plage d'indisponibilités pour n'importe quel consultant de sa société.
- Isolation multi-société : `requireSocAccess` sur la lecture de l'historique et sur l'indispo ; le consultant ne voit que ses propres données.

**Vérification** : backend — `UnavailabilityServiceTest` 11/11 (dont `validate_byManagerTransitionsToValidatedAndRecordsHistory`, `reject_byManagerKeepsCommentAndRecordsHistory`, `history_returnsEntriesForAccessibleUnavailability`) ; suite complète 260 tests, seul échec pré-existant non lié `SecurityIntegrationTest.adminCannotDeleteActiveUser` (order-dependent). Frontend — `Unavailability.test.tsx` 9/9 (dont validation avec commentaire, rejet avec motif, affichage de l'historique) ; suite 15 fichiers / 141 tests, `lint` 0 erreur, `tsc -b` OK.

----------------------------------------------------------------------
### 2026-09-16 18:36:37 — Filtre « consultant » (liste) sur les pages CRA et Indisponibilités (01_TODO_01)

**Tâche** : page CRA — il manque le filtre par consultant sous forme de liste : en tant que **manager** je ne vois que les consultants que je gère ; en tant que **responsable_soc** tous les consultants de la société sélectionnée ; en tant qu'**admin** tous les consultants. Idem pour la page Indisponibilités.

**Implémentation**
- Backend — `ConsultantService.filterList()` : liste des consultants (rôle CONSULTANT, actifs, triés par nom) selon le rôle de l'appelant via le helper existant `visibleUsers` : MANAGER → ses consultants (`findByManagerIdAndRole`) ; RESPONSIBLE_SOC → consultants de `SecurityUtils.currentSocId()` (société sélectionnée) ; ADMIN → tous les consultants (`findAll`).
- Backend — `ConsultantController` : nouveau `GET /consultants/filter-list` (pas de paramètre `socId` : le périmètre est résolu côté serveur selon le rôle, pas de fuite inter-société pour un non-admin).
- Frontend — `api/consultants.ts` : `filterList()`.
- Frontend — `pages/CraList.tsx` : le sélecteur de consultant (liste) est affiché pour tous les non-consultants (`Tous les consultants`) et alimenté par `filterList()` ; la sélection filtre les CRA par `consultantId` (en plus de la recherche texte) ; pagination réinitialisée au changement de filtre.
- Frontend — `pages/Unavailability.tsx` : le filtre consultant utilise désormais `filterList()` (au lieu de `summaries(socId)`), donc manager = son équipe, responsable_soc = société sélectionnée, admin = tous.
- Isolation multi-société : le périmètre est calculé côté serveur à partir du principal (`currentSocId`/rôle) ; un responsable_soc reste limité à sa société, un manager à son équipe.

**Vérification** : backend — 3 nouveaux tests `ConsultantServiceTest` (`filterList_managerReturnsOnlyManagedConsultants`, `filterList_responsableSocReturnsSocConsultantsOnly`, `filterList_adminReturnsAllConsultants`) ; `ConsultantServiceTest` 22/22 ; suite complète 264 tests, seul échec pré-existant non lié `SecurityIntegrationTest.adminCannotDeleteActiveUser`. Frontend — `CraList.test.tsx` 9/9, `Unavailability.test.tsx` 10/10 ; suite 15 fichiers / 145 tests, `lint` 0 erreur, `tsc -b` OK.

----------------------------------------------------------------------
### 2026-09-16 20:41:05 — Notifications du consultant : ouverture de l'objet concerné (01_TODO_01)

**Tâche** : notifications du consultant — lorsque le consultant clique sur une notification : (1) le nombre de notifications non lues doit décrémenter si elle était non lue ; (2) l'objet concerné (CRA, indispo, …) doit s'afficher (comme pour le manager).

**Implémentation**
- Point (1) : déjà en place — `NotificationBell.openItem()` appelle `notificationsApi.markRead(id)` (si non lue) et décrémente le compteur (`setUnread(n => max(0, n-1))`). Aucun changement nécessaire.
- Backend — `CraService` : les liens des notifications du consultant pointent désormais vers l'objet et non la liste. `notifyConsultant(...)` construit la cible `link + "/" + cra.getId()` (link = `/cras` ou `/indispos`) → `/cras/{id}` ou `/indispos/{id}`, exactement le format déjà utilisé par la notification « Indispo annulée » envoyée au manager. La notification de soumission au manager utilise aussi `/cras/{id}` / `/indispos/{id}`.
- Backend — `UnavailabilityService` : le lien devient `/indisponibilites?open={id}` (`link(unavailability)`), la page Indisponibilités n'ayant pas de route `:id` (elle affiche l'objet sélectionné en bas de liste).
- Frontend — `pages/Unavailability.tsx` : lit le paramètre `?open=` (`useSearchParams`) et sélectionne l'indisponibilité correspondante → l'objet (calendrier) s'affiche en bas.
- Frontend — pour les CRA/Indispos, les routes existantes `/cras/:id` et `/indispos/:id` (`CraDetailRoute`) affichent l'objet concerné.
- Le manager conserve son comportement existant ; le consultant obtient le même résultat.

**Vérification** : backend — `CraServiceTest.validate_notifiesConsultantWithObjectLink` (lien = `/cras/{id}`) ; `UnavailabilityServiceTest.validate_notifiesConsultantWithOpenLink` (lien = `/indisponibilites?open=1`) ; `CraServiceTest` 45/45, `UnavailabilityServiceTest` 13/13 ; suite complète 268 tests, seul échec pré-existant non lié `SecurityIntegrationTest.adminCannotDeleteActiveUser`. Frontend — test `ouvre l'indisponibilité ciblée via le paramètre ?open` ; suite 15 fichiers / 148 tests, `lint` 0 erreur, `tsc -b` OK.

----------------------------------------------------------------------
### 2026-09-16 21:32:04 — Règles de rattachement manager des utilisateurs (01_TODO_01)

**Tâche** : un user avec le rôle CONSULTANT doit avoir un manager, qui peut être un user de rôle MANAGER ou RESPONSIBLE_SOC ; un user avec le rôle MANAGER doit aussi avoir un manager (MANAGER ou RESPONSABLE_SOC). Un user qui a un manager peut faire des CRA et des Indispos.

**Implémentation**
- Backend — `ConsultantService.resolveManager()` : n'accepte plus que les comptes de rôle **MANAGER** ou **RESPONSIBLE_SOC** (ADMIN et CONSULTANT refusés, message « Le manager doit avoir le rôle MANAGER ou RESPONSIBLE_SOC »).
- Backend — `create()` (consultant) : manager obligatoire (`requireManagerAssigned(CONSULTANT, ...)`).
- Backend — `createPerson()` : manager obligatoire si le rôle est MANAGER ; optionnel si RESPONSIBLE_SOC.
- Backend — `update()` : manager obligatoire lorsque le rôle résultant est CONSULTANT ou MANAGER, et interdiction d'être son propre manager.
- Backend — `updatePerson()` : même contrôle de rattachement après changement de rôle.
- Backend — `managers()` (liste des managers possibles) : ne renvoie plus que les MANAGER/RESPONSIBLE_SOC (les CONSULTANT en étaient inclus à tort).
- Règle « un user qui a un manager peut faire des CRA / Indispos » : satisfaite de fait — tout CONSULTANT ayant désormais obligatoirement un manager, la création de CRA/Indispo reste possible (aucun changement nécessaire dans `CraService`).
- Frontend — `Consultants.tsx` : le champ « Manager » est requis pour les rôles CONSULTANT et MANAGER (message d'erreur si vide) ; l'utilisateur lui-même est exclu des options de manager.
- Isolation multi-société inchangée (`SecurityUtils.requireSocAccess` conservé).

**Vérification** : backend — 4 nouveaux tests (`create_consultant_withoutManager_throws`, `create_manager_withoutManager_throws`, `create_withConsultantAsManager_throws`, `update_selfAsManager_throws`) et tests existants adaptés (manager fourni) ; `ConsultantServiceTest` 26/26 ; `SecurityIntegrationTest` adapté (consultants créés avec manager) et vert en isolation ; suite complète 272 tests, seul échec pré-existant non lié `SecurityIntegrationTest.adminCannotDeleteActiveUser` (order-dependent). Frontend — suite 15 fichiers / 148 tests, `lint` 0 erreur, `tsc -b` OK.

----------------------------------------------------------------------
### 2026-09-16 21:34:38 — Modification du rôle d'un collaborateur (RESPONSIBLE_SOC / MANAGER) (01_TODO_02)

**Tâche** : un RESPONSIBLE_SOC peut modifier le rôle de l'un de ses collaborateurs en RESPONSIBLE_SOC, MANAGER ou CONSULTANT ; un MANAGER peut modifier le rôle de l'un de ses collaborateurs en MANAGER ou CONSULTANT.

**Implémentation**
- Backend — `ConsultantService.validateRoleChange(principal, cible, nouveauRole)` appelé par `update()` lorsque le rôle change réellement :
  - RESPONSIBLE_SOC → rôles autorisés {RESPONSIBLE_SOC, MANAGER, CONSULTANT} ;
  - MANAGER → rôles autorisés {MANAGER, CONSULTANT} (un manager ne peut donc pas promouvoir en RESPONSIBLE_SOC) ;
  - un utilisateur ne peut pas modifier son propre rôle (sauf ADMIN) ;
  - ADMIN sans restriction.
  Le contrôle de périmètre existant (`requireCanManage`) reste appliqué : un manager ne modifie que ses propres consultants.
- Backend — le rattachement manager introduit par la tâche précédente s'applique aussi : si le rôle résultant est MANAGER, un manager doit être fourni ; si le rôle résultant est CONSULTANT, un manager est également requis.
- Frontend — `Consultants.tsx` : la liste déroulante « Type de collaborateur » affiche les rôles autorisés selon le rôle de l'utilisateur connecté (RESPONSIBLE_SOC : 3 rôles ; MANAGER : CONSULTANT/MANAGER ; sinon le rôle courant uniquement). L'option « Consultant » est désormais proposée aussi en édition (elle ne l'était qu'à la création), permettant de rétrograder un collaborateur en consultant. Le manager courant reste sélectionnable comme manager d'un collaborateur (filtre limité à l'auto-rattachement).
- Isolation multi-société : `SecurityUtils.requireSocAccess` conservé.

**Vérification** : backend — 3 nouveaux tests (`update_responsibleSocCanSetManagerRole`, `update_managerCannotSetResponsibleRole`, `update_cannotChangeOwnRole`) ; `ConsultantServiceTest` 29/29 ; suite complète 275 tests, seul échec pré-existant non lié `SecurityIntegrationTest.adminCannotDeleteActiveUser` (order-dependent). Frontend — suite 15 fichiers / 148 tests, `lint` 0 erreur, `tsc -b` OK.

----------------------------------------------------------------------
### 2026-09-17 10:54:24 — Périmètre de consultation des CRA / Indisponibilités selon le rôle (01_TODO_01)

**Tâche** : en tant que manager, consulter la liste des indisponibilités en ne voyant que celles de son équipe ; idem pour les CRA ; en tant que RESPONSIBLE_SOC, voir les CRA / indisponibilités de toute la société sélectionnée.

**Implémentation**
- Backend — `UnavailabilityService.list` : nouveau périmètre par rôle.
  - **MANAGER** : ne renvoie que les indisponibilités de son équipe et les siennes — identifiants calculés via `managerScopeIds` (lui-même + `userAccountRepository.findByManagerId`), requête `findByConsultantIdInOrderByStartDateDesc`. Un `consultantId` hors de ce périmètre → 403 « Ce consultant n'appartient pas à votre équipe ».
  - **RESPONSIBLE_SOC / ADMIN** : comportement inchangé (société sélectionnée via `effectiveSocId`/`currentSocId`, ou toutes les sociétés pour l'admin).
  - **CONSULTANT** : ses propres indisponibilités.
- Backend — `UnavailabilityRepository.findByConsultantIdInOrderByStartDateDesc(Collection<Long>)`.
- CRA : le manager consulte déjà les CRA de son équipe (`CraService.findByManagerYear`, + ses propres CRA fusionnés côté frontend) ; le RESPONSIBLE_SOC consulte toute la société sélectionnée (`findBySocYear` via `effectiveSocId`). Aucun changement nécessaire.
- Isolation multi-société : le périmètre manager passe par la relation manager→collaborateur ; le RESPONSIBLE_SOC reste borné à `currentSocId()`.

**Vérification** : backend — 2 nouveaux tests `UnavailabilityServiceTest` (`list_managerSeesOnlyTeamAndSelf`, `list_managerFilteringForeignConsultant_isForbidden`) ; `UnavailabilityServiceTest` 18/18 ; suite complète 282 tests, seul échec pré-existant non lié `SecurityIntegrationTest.adminCannotDeleteActiveUser` (order-dependent). Frontend — suite 15 fichiers / 150 tests, `lint` 0 erreur, `tsc -b` OK.

----------------------------------------------------------------------
### 2026-09-17 12:00:09 — Uniformisation des champs de création/modification d'utilisateur (01_TODO_01)

**Tâche** : uniformiser les champs pour la création / modification d'utilisateur dans l'IHM — un consultant, un manager et un responsable_soc sont tous des salariés et doivent donc disposer des mêmes champs.

**Implémentation**
- Frontend — `Consultants.tsx` : suppression des conditions `form.role === 'CONSULTANT'` qui masquaient les champs « salarié ». Les champs **Poste, Nationalité, Date d'embauche, Date de naissance, N° de sécurité sociale, Salaire de base, Devise** sont désormais affichés pour **tous les rôles** (CONSULTANT, MANAGER, RESPONSIBLE_SOC), à la création comme en modification. La section « Compte utilisateur » de création reste inchangée (les managers/responsables ont besoin d'un compte, le consultant l'a en option).
- Backend — aucun changement nécessaire : `applyRhs` (utilisé par `create`, `createPerson` et `update`) applique déjà position / date d'embauche / date de naissance / n° sécurité sociale / salaire / devise / nationalité / contact d'urgence pour tous les rôles, et `toDto` renvoie déjà ces champs quel que soit le rôle.

**Vérification** : frontend — `lint` 0 erreur, `tsc -b` OK, suite 15 fichiers / 150 tests. Backend non modifié.

----------------------------------------------------------------------
### 2026-09-17 12:18:18 — Filtre Année-Mois (calendrier) sur les pages CRA et Indisponibilités (01_TODO_01)

**Tâche** : ajouter un filtre (Année-Mois) de type date calendar, à côté de la liste des utilisateurs, pour les interfaces d'affichage des CRA et des indisponibilités.

**Implémentation**
- Frontend — `pages/CraList.tsx` : ajout d'un champ `<input type="month">` (Année-Mois) à côté du sélecteur de consultant et de la recherche. La sélection pilote la période affichée (`openPeriod(year, month)`), en cohérence avec le sélecteur de période existant (mois/année + ←/auj/→).
- Frontend — `pages/Unavailability.tsx` : ajout d'un champ `<input type="month">` à côté du sélecteur de consultant. La liste est filtrée sur les indisponibilités **chevauchant le mois sélectionné** (bornes `monthBounds` calculées en UTC, comparaison `startDate`/`endDate`), en combinaison avec la recherche ; la pagination est réinitialisée au changement de mois.
- Backend : aucun changement (filtrage côté IHM).

**Vérification** : frontend — nouveau test `Unavailability.test.tsx` « filtre par mois » ; suite 15 fichiers / 151 tests, `lint` 0 erreur, `tsc -b` OK.

----------------------------------------------------------------------
### 2026-09-17 15:14:28 — Boutons de navigation mensuelle de la liste des CRA (01_TODO_01)

**Tâche** : changer le label des boutons de navigation entre les mois pour la liste des CRA — remplacer « auj » par « Aujourd'hui » ; élargir un peu la largeur du label « Période : ».

**Implémentation**
- Frontend — `pages/CraList.tsx` : le bouton de retour au mois courant affiche désormais **« Aujourd'hui »** (au lieu de « auj ») ; le titre d'aide « Revenir au mois actuel » est conservé.
- Frontend — `pages/CraList.tsx` : le libellé « Période : » est enveloppé dans un `<span class="min-w-[4.5rem]">` et le `<label>` passe en `whitespace-nowrap`, élargissant légèrement le libellé.
- Backend : aucun changement.

**Vérification** : frontend — `lint` 0 erreur, `tsc -b` OK, suite 15 fichiers / 152 tests.

----------------------------------------------------------------------
### 2026-09-17 16:24:09 — Filtre Année-Mois des pages CRA / Indisponibilités (01_TODO_01)

**Tâche** : page CRA / Indisponibilités — remettre le filtre année-mois afin de bien voir les année-mois, en espérant que cela fonctionne dans tous les navigateurs connus si possible.

**Implémentation**
- Frontend — `components/ui.tsx` : nouveau composant `MonthInput` réutilisable. Il détecte la prise en charge de `<input type="month">` (sonde `document.createElement('input')` mémoïsée) et rend le champ natif année-mois si disponible ; sinon il retombe sur un `<input type="text" inputMode="numeric" placeholder="AAAA-MM" pattern="\d{4}-\d{2}">`, garantissant un filtre année-mois exploitable dans tous les navigateurs.
- Frontend — `pages/CraList.tsx` : le filtre « Filtrer par mois » repasse de `type="date"` à `MonthInput` ; la valeur stockée reste `AAAA-MM` (plus de suffixe `-01`) et continue de piloter l'année affichée, le filtrage (`c.year`/`c.month`) et la réinitialisation de la pagination.
- Frontend — `pages/Unavailability.tsx` : idem, le filtre « Filtrer par mois » utilise `MonthInput` et filtre les indisponibilités chevauchant le mois sélectionné (`monthBounds`).
- Frontend — `pages/CraList.test.tsx` / `pages/Unavailability.test.tsx` : valeurs des tests de filtre ajustées au format année-mois (`2026-07`, `2026-11`).
- Backend : aucun changement (filtrage côté IHM).

**Vérification** : frontend — `lint` 0 erreur, `tsc -b` OK, suite 15 fichiers / 152 tests, `vite build` OK.

----------------------------------------------------------------------
### 2026-09-17 16:24:09 — Libellé du retour au mois courant sur la page CRA (01_TODO_02)

**Tâche** : page CRA — remplacer « Aujourd'hui » par « Mois Courant ».

**Implémentation**
- Frontend — `pages/CraList.tsx` : le bouton de retour à la période courante affiche désormais **« Mois Courant »** (au lieu de « Aujourd'hui ») ; le titre d'aide devient « Revenir au mois courant ».
- Backend : aucun changement.

**Vérification** : frontend — `lint` 0 erreur, `tsc -b` OK, suite 15 fichiers / 152 tests, `vite build` OK.

----------------------------------------------------------------------
### 2026-09-17 16:39:33 — Libellé « Mois : » sur la page CRA (01_TODO_01)

**Tâche** : page CRA — remplacer « Période : » par « Mois : ».

**Implémentation**
- Frontend — `pages/CraList.tsx` : le libellé du sélecteur de période est renommé de « Période : » en **« Mois : »** (le `<span class="min-w-[4.5rem]">` d'alignement est conservé).
- Backend : aucun changement.

**Vérification** : frontend — `lint` 0 erreur, `tsc -b` OK, suite 15 fichiers / 153 tests, `vite build` OK.

----------------------------------------------------------------------
### 2026-09-17 16:39:33 — Historique du CRA (01_TODO_02)

**Tâche** : historique du CRA manquant — on doit avoir : date modif ; modifié par ; commentaire ; statut avant ; statut après.

**Implémentation**
- Backend — migration Flyway `V45__cra_history.sql` : table `cra_history` (`id`, `date_modif`, `cra_id` FK `cra` ON DELETE CASCADE, `modifier` FK `user_account` ON DELETE SET NULL, `comment` VARCHAR(500), `status_before` VARCHAR(20) nullable, `status_after` VARCHAR(20) NOT NULL) + index `idx_cra_history_cra`.
- Backend — `CraHistory` (entité JPA), `CraHistoryRepository.findByCraIdOrderByIdAsc`, `CraDtos.CraHistoryDto(id, dateModif, craId, modifierId, modifierName, comment, statusBefore, statusAfter)`.
- Backend — `CraService` : helpers `recordHistory(...)`, `recordStatusChange(...)` et `currentUserOrNull()`. Chaque changement est tracé : `createForMonth` (Création, statut avant null), `save` (Modification), `submit` (Soumission), `validate` (Validation), `reject` (Rejet), `markPendingSend` (mise en attente d'envoi), `sendIndispo` (validation/rejet), `cancel` (retrait → brouillon / annulation → annulé) ; `setActivityValid` / `setRangeValid` via `recordStatusChange` (enregistré uniquement si le statut change).
- Backend — `CraController` : nouvel endpoint `GET /api/cras/{id}/history`, protégé par l'isolation multi-société (`SecurityUtils.requireSocAccess` sur la société du consultant, comme `/{id}/exchanges`).
- Frontend — `api/types.ts` (`CraHistoryDto`), `api/cras.ts` (`history(id)`), `pages/CraDetail.tsx` : le bouton « Historique » ouvre l'historique du CRA (tableau **Date modif / Modifié par / Commentaire / Statut avant / Statut après**), à la place de l'ancien affichage des échanges.
- Frontend — `pages/CraDetail.test.tsx` : nouveau test « affiche l'historique des modifications du CRA ».

**Vérification** : backend — `CraServiceTest` 48/48 (dont `history_recordsStatusTransitions`, `history_recordsReject`) ; suite complète 284 tests, seul échec pré-existant non lié `SecurityIntegrationTest.adminCannotDeleteActiveUser` (order-dependent). Frontend — suite 15 fichiers / 154 tests, `lint` 0 erreur, `tsc -b` OK, `vite build` OK.

----------------------------------------------------------------------
### 2026-09-17 16:41:44 — Bouton Historique par CRA dans la liste (01_TODO_03)

**Tâche** : page CRA — ajouter un bouton « Historique » pour chaque élément du CRA dans la liste.

**Implémentation**
- Frontend — nouveau composant réutilisable `components/CraHistoryModal.tsx` : affiche l'historique d'un CRA/Indispo (chargement via `crasApi.history(craId)`, tableau **Date modif / Modifié par / Commentaire / Statut avant / Statut après**).
- Frontend — `pages/CraDetail.tsx` : utilise désormais `CraHistoryModal` (le modal d'historique local est supprimé, l'état `history`/`historyLoading` et l'appel API sont délégués au composant).
- Frontend — `pages/CraList.tsx` : chaque ligne de CRA dispose d'un bouton **« Historique »** dans la colonne Actions ; le clic ouvre `CraHistoryModal` pour ce CRA (`isIndispo` déduit du type `CONGE`).
- Frontend — `pages/CraList.test.tsx` : nouveau test « ouvre l'historique d'un CRA depuis la liste » (mock `crasApi.history`).
- Backend : aucun changement.

**Vérification** : frontend — suite 15 fichiers / 154 tests, `lint` 0 erreur, `tsc -b` OK, `vite build` OK.

----------------------------------------------------------------------
### 2026-09-17 17:03:49 — Statut CRA recalculé à la consultation et renommage SEMI_VALID → VALREJ (01_TODO_01)

**Tâche** : page CRA — un CRA au statut « Validé » peut avoir des événements non validés (vieux CRA mal validé) ; en tant que manager (ou responsable_soc) qui le consulte, le statut doit être remis en cohérence : `VALIDE` (tous les événements validés), `REJET` (aucun validé), `VALREJ` (au moins un validé et au moins un invalidé). Le statut `SEMI_VALID` est remplacé par `VALREJ`.

**Implémentation**
- Backend — `common/enums/CraStatus.java` : le statut `SEMI_VALID` est renommé **`VALREJ`**.
- Backend — migration Flyway `V46__cra_status_valrej.sql` : `UPDATE cra SET status = 'VALREJ' WHERE status = 'SEMI_VALID';` (les données existantes sont migrées).
- Backend — `CraService.recomputeStatusFromEvents` produit désormais `VALIDATED` (tous validés) / `VALREJ` (mixte) / `REJECTED` (aucun validé) ; nouveau `syncStatusFromEvents(cra)` appelé par `getById` : à la consultation, si le CRA/Indispo n'est ni en brouillon ni soumis, le statut est recalculé depuis les événements et le changement est historisé (« Mise à jour du statut selon les événements »). Aucun changement si le statut est déjà cohérent.
- Frontend — `api/types.ts` (`CraStatus`), `lib/format.ts` (libellé **« Partiellement validé »**, badge info), `pages/CraDetail.tsx` et `pages/IndispoList.tsx` : renommage des références `SEMI_VALID` → `VALREJ`.
- Boutons « Valider tout » / « Invalider tout » (déjà pilotés par les événements, `hasInactiveEvent` / `hasActiveEvent`) conformes : tous validés → « Valider tout » inactif et « Invalider tout » actif ; tous invalidés → l'inverse ; mixte → les deux actifs.

**Vérification** : backend — nouveau test `CraServiceTest.getById_recomputesInconsistentStatusFromEvents` ; `CraServiceTest` 49/49 ; suite complète 285 tests, seul échec pré-existant non lié `SecurityIntegrationTest.adminCannotDeleteActiveUser` (order-dependent). Frontend — suite 15 fichiers / 154 tests, `lint` 0 erreur, `tsc -b` OK, `vite build` OK.

----------------------------------------------------------------------
### 2026-09-18 12:34:06 — soc-39 : activité paramétrable le week-end / les jours fériés (01_TODO_01)

**Tâche** : rendre paramétrable le fait qu'une activité puisse être ajoutée durant le week-end
ou un jour férié. Dans l'interface d'ajout/modification d'une activité, ajouter une case
« Activité possible le week-end » et une case « Activité possible les jours fériés ». Par
défaut non cochées → ajout interdit le week-end et les jours fériés, sauf si le manager (ou
responsable_soc/admin) les passe à vrai.

**Implémentation**
- Backend — migration Flyway `V47__activity_weekend_holiday.sql` : ajout des colonnes
  `weekend_allowed` et `holiday_allowed` (BOOLEAN NOT NULL DEFAULT false) à la table `activity`.
- Backend — entité `Activity` : champs `weekendAllowed` / `holidayAllowed` (défaut false).
- Backend — `ActivityDtos` : `ActivityDto` et `ActivityRequest` exposent `weekendAllowed` /
  `holidayAllowed` ; `ActivityService.apply` les persiste (null → false) et `toDto` les renvoie.
- Backend — `CraService.save` : nouvelle règle par activité — un événement sur un jour
  `WEEKEND` exige `activity.weekendAllowed`, sur un jour `PUBLIC_HOLIDAY` exige
  `activity.holidayAllowed` (messages « L'activité « … » n'est pas autorisée le week-end /
  les jours fériés (date) »). Remplace l'ancien blocage spécifique aux Indispos le week-end :
  la règle s'applique désormais à tout CRA/Indispo et est pilotée par l'activité.
- Frontend — `Activities.tsx` : deux cases à cocher « Activité possible le week-end » et
  « Activité possible les jours fériés » (défaut décochées) dans le formulaire de création/
  modification ; `types.ts` (`ActivityDto`/`ActivityRequest`) mis à jour.
- Frontend — `CraDetail.tsx` : helper `activitiesForDay(day)` (filtre `weekendAllowed` pour un
  jour WEEKEND, `holidayAllowed` pour un jour PUBLIC_HOLIDAY) ; le bouton « + » apparaît sur
  les jours WORKED/WEEKEND/PUBLIC_HOLIDAY uniquement si au moins une activité est autorisée ;
  l'ajout choisit par défaut une activité autorisée et affiche un message explicite sinon ;
  les listes déroulantes des vues calendrier, ligne et modale d'événement sont filtrées par jour.
- Isolation multi-société : inchangée (les activités sont déjà bornées par `id_soc`, `CraService`
  opère sur le CRA/consultant contrôlé par `requireSocAccess`), aucun impact.

**Vérification** : backend — nouveaux tests `CraServiceTest.save_weekendActivityInCRA_isRejectedWhenNotAllowed`,
`save_holidayActivityInCRA_isAllowedWhenHolidayAllowed`, `save_holidayActivityInCRA_isRejectedWhenNotHolidayAllowed`
(+ `save_weekendActivityInCRA_isAllowed` adapté : activité `weekendAllowed=true` ;
`save_weekendActivity_isRejected` Indispo conservé) ; `ActivityServiceTest` 5/5
(`create_persistsWeekendAndHolidayFlags`, `create_defaultsWeekendAndHolidayFlagsToFalse`) ;
`CraServiceTest` 52/52 ; suite complète 290 tests, seul échec pré-existant non lié
`SecurityIntegrationTest.adminCannotDeleteActiveUser` (order-dependent). Frontend — `lint` 0 erreur,
15 fichiers / 154 tests, `tsc -b` OK, `vite build` OK.

----------------------------------------------------------------------
### 2026-09-18 12:49:11 — CRA : ajout possible le week-end / jours fériés + message info si l'activité ne l'autorise pas (01_TODO_01)

**Tâche** : lorsqu'on ajoute un nouveau CRA, par défaut on peut ajouter des activités le week-end
et les jours fériés ; mais si l'activité que l'on veut ajouter ne l'autorise pas, un message
d'information s'affiche (« Cette activité n'autorise pas … »).

**Contexte** : la tâche soc-39 précédente masquait le bouton « + » et filtrait les listes
d'activités sur les jours WEEKEND/PUBLIC_HOLIDAY, ce qui rendait l'ajout impossible sans
explication. Cette tâche remplace ce comportement silencieux par un affichage ouvert + un
message d'information.

**Implémentation** (frontend uniquement — la règle métier backend `CraService.save` de soc-39 est conservée)
- Frontend — `CraDetail.tsx` : suppression du filtrage `activitiesForDay` ; le bouton « + » du
  calendrier apparaît à nouveau sur tous les jours WORKED/WEEKEND/PUBLIC_HOLIDAY ; les listes
  déroulantes (calendrier, vue « Ligne » et modale d'événement) proposent toutes les activités.
- Frontend — nouveau helper `activityDayRestriction(dayType, activity)` : renvoie
  « Cette activité n'autorise pas le week-end. » (jour WEEKEND sans `weekendAllowed`) ou
  « Cette activité n'autorise pas les jours fériés. » (jour PUBLIC_HOLIDAY sans `holidayAllowed`).
- Frontend — bandeau d'information (bleu) en haut du CRA/Indispo dès qu'un événement viole la
  règle ; message inline (ambre) sous la ligne concernée dans la modale d'événement.
- Frontend — `doSave()` bloque l'enregistrement/soumission tant qu'une violation existe (le
  message d'information est déjà affiché), ce qui évite l'erreur rouge du backend.
- Backend : aucun changement (`Activity.weekendAllowed` / `holidayAllowed` et le contrôle dans
  `CraService.save` restent la source de vérité).
- Isolation multi-société : sans objet (UI, aucune nouvelle requête).

**Vérification** : frontend — nouveau test `CraDetail.test.tsx` « affiche un message info si
l'activité choisie n'autorise pas le week-end » (ouverture d'un CRA brouillon, ajout d'un
événement un samedi avec une activité `weekendAllowed=false` → message affiché) ; suite
15 fichiers / 155 tests, `lint` 0 erreur, `tsc -b` OK, `vite build` OK. Backend non modifié
(dernière suite connue : 290 tests, seul échec pré-existant `adminCannotDeleteActiveUser`).

----------------------------------------------------------------------
### 2026-09-18 12:53:29 — CRA : message info en dialog et activité non ajoutée si elle n'autorise pas le jour (01_TODO_01)

**Tâche** : lorsqu'on ajoute un nouveau CRA, par défaut on peut ajouter des activités le week-end
et les jours fériés ; mais si l'activité que l'on veut ajouter ne l'autorise pas, un message
d'information s'affiche **dans un dialog** (« Cette activité n'autorise pas … ») et **l'activité
en question ne doit pas être ajoutée**.

**Implémentation** (frontend uniquement — la règle métier backend `CraService.save` de soc-39 est conservée)
- Frontend — `CraDetail.tsx` : remplacement du bandeau bleu et du message inline de la tâche
  précédente par un vrai **dialog d'information** (`Modal` titre « Information » + bouton OK),
  porté par l'état `restrictionDialog`.
- Frontend — `updateActivity` : toute modification d'`activityId` est validée par
  `activityDayRestriction(dayType, activity)` ; si l'activité n'est pas autorisée le jour
  (week-end / jour férié), le dialog s'affiche et la sélection n'est **pas appliquée** (la valeur
  précédente est conservée).
- Frontend — `addActivity` : ne choisit par défaut que parmi les activités autorisées ce jour-là
  (`allowedForDay`) ; si aucune activité n'est autorisée, le dialog s'affiche et **aucun événement
  n'est ajouté**.
- Frontend — `doSave()` : si une violation subsiste (données existantes), le dialog s'affiche et
  l'enregistrement/soumission est bloqué.
- Backend : aucun changement (`Activity.weekendAllowed` / `holidayAllowed` et le contrôle dans
  `CraService.save` restent la source de vérité).
- Isolation multi-société : sans objet (UI, aucune nouvelle requête).

**Vérification** : frontend — test `CraDetail.test.tsx` « affiche un message info si l'activité
choisie n'autorise pas le week-end » adapté : ouverture d'un CRA brouillon, clic « + » sur un
samedi puis « + Ajouter un événement » avec une activité `weekendAllowed=false` → dialog
« Information » affiché, message affiché, et la modale du jour reste sur « Aucun événement pour
ce jour. » (activité non ajoutée). Suite 15 fichiers / 155 tests, `lint` 0 erreur, `tsc -b` OK,
`vite build` OK. Backend non modifié.

----------------------------------------------------------------------
### 2026-09-18 13:03:34 — CRA : sélection explicite de l'activité puis dialog info si non autorisée (01_TODO_01)

**Tâche** : lorsqu'on ajoute un nouveau CRA, par défaut on peut ajouter des activités le week-end
et les jours fériés. Flow attendu : clic sur « + » → une dialog s'ouvre pour ajouter un événement ;
clic sur « ajouter un événement » → on **sélectionne une activité** ; si l'activité sélectionnée
n'est pas autorisée ce jour-là, un **dialog d'information** s'affiche (« cette activité n'autorise
pas ») et **l'activité ne doit pas être ajoutée**.

**Implémentation** (frontend uniquement — la règle métier backend `CraService.save` de soc-39 est conservée)
- Frontend — `CraDetail.tsx`, `addActivity` : création d'une ligne d'événement **sans activité
  présélectionnée** (`activityId: ''`) — l'utilisateur choisit explicitement l'activité dans la
  liste déroulante ; plus aucune activité n'est ajoutée automatiquement.
- Frontend — `updateActivity` : au changement d'activité, `activityDayRestriction(dayType, activity)`
  vérifie l'autorisation week-end / jour férié ; en cas de refus le **dialog « Information »**
  s'affiche et la sélection **n'est pas appliquée** (la valeur reste vide).
- Frontend — `dayTotal` / `allowedDaysFor` / `updateActivity` : nouvelle fonction `activityDays(a)`
  qui ne compte que les lignes ayant une activité sélectionnée, afin qu'une ligne vide n'inflate
  pas le total du jour ni la complétude.
- Frontend — le calendrier ne rend plus de « chip » pour les lignes sans activité (évite
  « Activité inconnue ») ; les vues « Ligne » et la modale gardent la ligne avec le sélecteur
  « Activité… ».
- Frontend — `doSave()` : si une violation subsiste (données antérieures), le dialog s'affiche et
  l'enregistrement/soumission est bloqué.
- Backend : aucun changement (`Activity.weekendAllowed` / `holidayAllowed` et le contrôle dans
  `CraService.save` restent la source de vérité).
- Isolation multi-société : sans objet (UI, aucune nouvelle requête).

**Vérification** : frontend — test `CraDetail.test.tsx` « affiche un message info si l'activité
choisie n'autorise pas le week-end » adapté au flow : clic « + » sur un samedi, clic « + Ajouter
un événement » (ligne vide), puis sélection d'une activité `weekendAllowed=false` dans la liste →
dialog « Information » affiché, message affiché, et le sélecteur reste sur la valeur vide
(activité non ajoutée). Suite 15 fichiers / 155 tests, `lint` 0 erreur, `tsc -b` OK, `vite build` OK.

----------------------------------------------------------------------
### 2026-09-18 13:04:24 — CRA : si l'activité sélectionnée autorise le jour, elle est ajoutée (01_TODO_02)

**Tâche** : si l'activité sélectionnée autorise le week-end / jour férié, elle doit être ajoutée.

**Implémentation** : déjà couvert par la tâche précédente (01_TODO_01) — au changement d'activité,
`updateActivity` applique `activityId` dès que `activityDayRestriction` ne renvoie rien : la ligne
d'événement passe de « Activité… » à l'activité choisie, le total du jour (`activityDays`) et le
« chip » du calendrier se mettent à jour. Aucune modification de code de production nécessaire ;
ajout d'un test de non-régression.

**Vérification** : frontend — nouveau test `CraDetail.test.tsx` « ajoute l'activité sélectionnée
si elle autorise le week-end » : clic « + » sur un samedi, « + Ajouter un événement », sélection
d'une activité `weekendAllowed=true` → aucun dialog « Information » et le sélecteur vaut
l'activité choisie. Suite 15 fichiers / 156 tests, `lint` 0 erreur, `tsc -b` OK, `vite build` OK.

----------------------------------------------------------------------
### 2026-09-18 13:11:02 — Menu consultant « Mes Activités » (lecture seule) (01_TODO_01)

**Tâche** : ajouter un menu au consultant afin qu'il voie en lecture seule les activités qui lui
sont affectées. Menu « Mes Activités », placé juste avant le menu « CRA ».

**Implémentation** (frontend uniquement)
- Frontend — nouvelle page `pages/MyActivities.tsx` (route `/mes-activites`) : liste **lecture
  seule** (aucune action Modifier/Supprimer) des activités affectées au consultant connecté.
  Données chargées via `activitiesApi.findAll({ socId: user.socId, consultantId: user.id })` puis
  filtrées sur `a.consultant?.id === user.id` (n'affiche pas les activités partagées de la société
  ni celles d'autres consultants). Colonnes : Activité, Type, Projet, Période, Tarif,
  « Week-end / Jours fériés » (badges **explicites « Week-end : Oui/Non » et « Jours fériés :
  Oui/Non »** indiquant si l'activité peut être saisie ces jours-là), Statut.
- Frontend — `layout/MainLayout.tsx` : `NavItem` accepte `roles?: Role[]` ; `NavSection` filtre
  sur le rôle en plus de `HIDDEN_FOR_CONSULTANT`. Nouvel item « Mes Activités »
  (`/mes-activites`, icône activités, `roles: ['CONSULTANT']`) inséré **juste avant « CRA »** dans
  la section « Activité » — finalement **réservé au seul rôle CONSULTANT** (retiré pour
  responsable_soc, manager et admin).
- Frontend — `pages/Activities.tsx` (« Activités & tarifs ») : ajout de la colonne
  « Week-end / Jours fériés » dans la liste — badges explicites « Week-end : Oui/Non » et
  « Jours fériés : Oui/Non » d'après `weekendAllowed` / `holidayAllowed` de chaque activité.
- Frontend — `App.tsx` : import `MyActivities` + route `/mes-activites` (avant `/cras`), accessible
  à tout utilisateur authentifié (pas de `NotConsultantRoute`).
- Backend — aucun changement : `GET /api/activities` borne déjà par société via
  `SecurityUtils.effectiveSocId` (le consultant ne peut pas cibler une autre société) ; le filtre
  « affectées à moi » est appliqué côté IHM.
- Isolation multi-société : la requête passe par `effectiveSocId` (société du consultant imposée
  pour un non-admin) ; aucune fuite inter-société.

**Vérification** : frontend — nouveau `pages/MyActivities.test.tsx` : affiche uniquement l'activité
affectée au consultant (les activités partagée et d'un autre consultant sont exclues), affiche
« Week-end : Oui » / « Jours fériés : Non » selon les droits de l'activité, aucune action
Modifier/Supprimer, et l'appel API est bien `{ socId: 5, consultantId: 2 }`. Suite
16 fichiers / 157 tests, `lint` 0 erreur, `tsc -b` OK, `vite build` OK.

----------------------------------------------------------------------
### 2026-09-18 13:39:27 — soc-50 : un manager ne peut ajouter une activité que pour ses propres consultants (01_TODO_01)

**Tâche** : en tant que manager, je ne dois pouvoir ajouter des activités que pour mes propres
consultants. En particulier, je ne peux pas ajouter à l'élément « Aucun » (activité sans
consultant).

**Implémentation**
- Backend — `ActivityService.apply` (utilisé par `create` et `update`) :
  - si le principal est un **MANAGER** et que `consultantId` est null → `403` « Un manager ne peut
    ajouter une activité que pour ses propres consultants » ;
  - si `consultantId` est fourni, le consultant doit avoir pour manager l'utilisateur courant,
    sinon `403` « Ce consultant n'est pas rattaché à vous » ;
  - contrôle d'isolation `SecurityUtils.requireSocAccess(socId)` ajouté (ignoré pour les appels
    internes/tests sans contexte de sécurité) ;
  - helpers `isManagerPrincipal()` / `requireSocAccessIfAuthenticated(...)`.
- Frontend — `pages/Activities.tsx` : pour un MANAGER, la liste des consultants provient de
  `consultantsApi.managed()` (ses propres consultants) au lieu de `summaries(socId)` ; le champ
  devient obligatoire (`Consultant *`, message « Sélectionnez un de vos consultants. ») et
  l'option **« Aucun » est retirée** (remplacée par un placeholder « Sélectionner… »).
  **Correction ultérieure** : la règle est étendue au RESPONSIBLE_SOC (le bug a été reproduit en
  local avec un compte RESPONSIBLE_SOC) — le champ consultant est obligatoire et l'option
  « Aucun » est retirée ; seule l'ADMIN conserve l'option « Aucun » (activité partagée).
- Isolation multi-société : `requireSocAccess` sur la société de l'activité ; un manager reste
  borné à sa société et à son équipe.

**Vérification** : backend — 5 tests `ActivityServiceTest`
(`create_managerWithoutConsultant_isForbidden`, `create_managerWithForeignConsultant_isForbidden`,
`create_managerWithOwnConsultant_succeeds`, `create_responsibleSocWithoutConsultant_isForbidden`,
`create_responsibleSocWithConsultant_succeeds`) ; `ActivityServiceTest` 10/10 ; suite complète
295 tests, seul échec pré-existant non lié `SecurityIntegrationTest.adminCannotDeleteActiveUser`
(order-dependent). Frontend — `pages/Activities.test.tsx` : manager → `managed()` appelé, pas de
« Aucun » ; responsable_soc → `summaries(5)` appelé, pas de « Aucun » non plus ; admin → « Aucun »
présent ; suite 17 fichiers / 160 tests, `lint` 0 erreur, `tsc -b` OK, `vite build` OK.

**Incident local (18/09 16:08)** : malgré le correctif MANAGER, un compte **RESPONSIBLE_SOC**
(`mzamouneisi`) a créé en local une activité `consultant_id = NULL` (id 438, « aa »). Le contrôle
ne couvrait que le rôle MANAGER. Correctif appliqué : `ActivityService` bloque désormais
`consultantId == null` pour **MANAGER et RESPONSIBLE_SOC** (message « Une activité doit être
rattachée à un consultant : « Aucun » n'est pas un consultant ») ; le contrôle de rattachement
manager→consultant reste spécifique au MANAGER. Front : `needsConsultant = MANAGER ||
RESPONSIBLE_SOC`. `ActivityServiceTest` 10/10, front 160/160.

----------------------------------------------------------------------
### 2026-09-18 15:44:52 — soc-50 (rappel) : « Aucun » n'est pas un consultant, le manager ne peut pas y ajouter (01_TODO_01)

**Tâche** : re-formulation de soc-50 — en tant que manager, ne pouvoir ajouter des activités que
pour ses propres consultants ; ne pas pouvoir ajouter à l'élément de la liste portant le libellé
« Aucun », car « Aucun » n'est pas un nom de consultant (cela signifie « aucun consultant »).

**Constat** : demande déjà satisfaite par la tâche soc-50 précédente (commits backend `8780c46`,
frontend `fc56a80`) — pour un MANAGER, l'option « Aucun » est absente du sélecteur de consultant
(remplacée par un placeholder « Sélectionner… »), le champ est obligatoire et le backend refuse
tout `consultantId` null ou rattaché à un autre manager. Aucune modification de code supplémentaire
n'a donc été nécessaire ; vérification de non-régression effectuée.

**Vérification** : `ActivityServiceTest` 8/8 (dont `create_managerWithoutConsultant_isForbidden`,
`create_managerWithForeignConsultant_isForbidden`, `create_managerWithOwnConsultant_succeeds`) ;
`Activities.test.tsx` 2/2 (manager → pas d'option « Aucun » et `managed()` appelé ; responsable_soc
→ option « Aucun » présente). `01_TODO_01` (doublon) supprimé.

----------------------------------------------------------------------
### 2026-09-18 16:39:33 — soc-48 : filtrer les activités par consultant et par type (01_TODO_02)

**Tâche** : en tant que manager, filtrer les activités et afficher les activités d'un de mes
consultants — filtre par consultant via une liste, et filtre par type d'activité.

**Implémentation** (frontend uniquement — `GET /api/activities` supporte déjà `typeId` et
`consultantId`, et `GET /api/consultants/filter-list` est déjà borné par rôle)
- Frontend — `pages/Activities.tsx` : barre de filtres au-dessus de la liste :
  - **Consultant** (`Select`) alimenté par `consultantsApi.filterList()` — périmètre serveur selon
    le rôle : MANAGER → ses consultants (+ lui-même), RESPONSIBLE_SOC → consultants de la société
    sélectionnée, ADMIN → tous. Option par défaut « Tous les consultants ».
  - **Type d'activité** (`Select`) alimenté par `activityTypesApi.findAll(workingSocId)`. Option
    par défaut « Tous les types ».
  - Bouton **« Réinitialiser »** affiché dès qu'un filtre est actif.
- Filtrage côté IHM (`filtered`) : `a.consultant?.id` pour le consultant, `a.type?.id` pour le
  type ; la `Table` paginée clampée affiche le résultat ; nouvel état vide « Aucun résultat » quand
  des filtres excluent toutes les activités (distinct de l'état « Aucune activité » initial).
- Le sélecteur du **formulaire** d'ajout/édition reste inchangé (`managed()` pour le manager,
  `summaries(socId)` pour le responsable_soc) afin de respecter la règle soc-50 (consultant
  obligatoire pour MANAGER/RESPONSIBLE_SOC).
- Isolation multi-société : `filterList()` est borné côté serveur (manager = son équipe,
  responsable_soc = sa société, admin = tout) ; aucune fuite inter-société.

**Vérification** : frontend — nouveau test `Activities.test.tsx` « filtre les activités par
consultant et par type » (2 activités ; filtre consultant → seule celle du consultant choisie ;
filtre type → seule celle du type choisi ; `filterList()` appelé) ; suite 17 fichiers / 161 tests,
`lint` 0 erreur, `tsc -b` OK, `vite build` OK. Backend non modifié.

----------------------------------------------------------------------
### 2026-09-18 17:04:11 — soc-49 : périmètre de consultation/gestion des activités par rôle (01_TODO_01)

**Tâche** : consulter la liste des activités de mes consultants. En tant que manager, ne pas
pouvoir consulter (ni gérer) les activités des consultants hors de mon équipe (les consultants
dont je suis le manager). Le RESPONSIBLE_SOC peut voir toutes les activités de la société
sélectionnée ; l'ADMIN peut voir toutes les activités de l'application.

**Implémentation** (backend — `ActivityService`)
- `findAll(socId, typeId, consultantId)` : pour un **MANAGER**, nouveau périmètre via
  `managerActivities(...)` :
  - sans `consultantId` → uniquement les activités dont le consultant a l'utilisateur courant
    pour manager (les activités partagées et celles des autres consultants sont exclues) ;
  - avec `consultantId` (lui-même ou un de ses consultants) → activités partagées + celles de ce
    consultant (comportement nécessaire à la page CRA) ;
  - un `consultantId` hors équipe → `403` « Ce consultant ne fait pas partie de votre équipe ».
  RESPONSIBLE_SOC et ADMIN conservent le comportement précédent (société sélectionnée / toutes).
- `findById`, `update`, `delete` : nouveau contrôle `requireCanManage(activity)` — un MANAGER ne
  peut consulter/modifier/supprimer que les activités de ses consultants (les activités partagées
  ou d'un autre consultant → `403` « Accès refusé : cette activité ne fait pas partie de votre
  périmètre ») ; RESPONSIBLE_SOC borné à sa société (`requireSocAccess`) ; ADMIN accès total.
- Helpers `isManagedByCurrentManager(...)`, `isSelfOrManagedConsultant(...)`, `requireCanManage(...)`.
- Isolation multi-société : `requireCanManage` appelle `requireSocAccessIfAuthenticated` sur la
  société de l'activité ; le périmètre manager reste borné à son équipe.
- Frontend : aucun changement nécessaire — la liste `Activities.tsx` est alimentée par `findAll`
  (désormais filtrée côté serveur pour le manager), et les filtres soc-48 s'appliquent dessus.

**Vérification** : backend — 5 nouveaux tests `ActivityServiceTest`
(`findAll_manager_returnsOnlyTeamActivities`, `findAll_managerForeignConsultant_isForbidden`,
`findAll_managerOwnConsultant_returnsSharedAndHis`, `update_managerForeignActivity_isForbidden`,
`delete_managerForeignActivity_isForbidden`) ; `ActivityServiceTest` 15/15 ; suite complète
300 tests, seul échec pré-existant non lié `SecurityIntegrationTest.adminCannotDeleteActiveUser`
(order-dependent).

----------------------------------------------------------------------
### 2026-09-18 18:43:20 — ERROR.txt : sauvegarde PostgreSQL échoue sur Azure (`pg_dump` introuvable)

**Erreur (log serveur Azure, profil prod)** :
`com.soc360.batch.DbBackupService : Echec de la sauvegarde de la base`
`java.io.IOException: Cannot run program "pg_dump": error=2, No such file or directory`
→ l'image Docker `eclipse-temurin:17-jdk-alpine` ne contient pas le client PostgreSQL ; la
sauvegarde planifiée ne peut pas s'exécuter sur Azure Container Apps.

**Correctif**
- `Dockerfile` : installation du client PostgreSQL aligné sur le serveur Azure (PostgreSQL 17) :
  `apk add --no-cache postgresql17-client` (repli 18 puis client par défaut) + mise dans le PATH
  des binaires `pg_dump` / `pg_restore` / `psql` / `pg_isready` (symlinks depuis
  `/usr/libexec/postgresql*/` si nécessaire).
- Backend — `DbBackupService` : le binaire est configurable
  (`app.backup.pg-dump`, défaut `pg_dump`) ; si le binaire est absent, on journalise un message
  clair (« Installez le client PostgreSQL … ») au lieu d'une stack trace, et aucun fichier partiel
  n'est conservé (`Files.deleteIfExists` en cas d'échec de `pg_dump`).
- `application.yml` : nouvelle propriété `app.backup.pg-dump: ${BACKUP_PG_DUMP:pg_dump}`.
- Aucun changement Flyway ni frontend.

**Remarque** : le répertoire de sauvegarde par défaut `./data/backups` est éphémère dans le
conteneur Azure ; pour conserver les sauvegardes, définir `BACKUP_DIRECTORY` vers un volume monté
(Azure Files) ou ajouter un export externe (hors périmètre de cette erreur).

**Vérification** : `DbBackupServiceTest` 5/5 ; suite complète 300 tests, seul échec pré-existant
non lié `SecurityIntegrationTest.adminCannotDeleteActiveUser` (order-dependent). `ERROR.txt` vidé.

----------------------------------------------------------------------
### 2026-09-18 18:45:12 — Logs du serveur : bouton « Copier » à droite du nom de fichier (01_TODO_01)

**Tâche** : page « Logs du serveur » — ajouter un bouton à droite du nom du fichier de log qui
permet de copier dans le presse-papiers les lignes affichées en mémoire (copier-coller).

**Implémentation** (frontend uniquement)
- Frontend — `pages/Logs.tsx` : bouton **« Copier »** (`InlineButton`) placé à droite du nom du
  fichier de log, à **20 px** de celui-ci (`gap-2` du conteneur + `ml-3`). Il copie exactement les
  lignes actuellement affichées, donc **après application du filtre** (`visible.join('\n')`).
  - `copyText` : `navigator.clipboard.writeText` si disponible, sinon repli par
    `textarea` + `document.execCommand('copy')` (contextes non sécurisés).
  - Retour visuel « Copié ! » pendant 2 s ; bouton désactivé si aucune ligne visible.
- Backend : aucun changement.

**Vérification** : frontend — nouveau test `Logs.test.tsx` « copie les lignes affichées dans le
presse-papiers » (mock `navigator.clipboard.writeText` ; clic sur « Copier » → appel avec
`ligne-a\nligne-b` et libellé « Copié ! ») ; suite 17 fichiers / 162 tests, `lint` 0 erreur,
`tsc -b` OK, `vite build` OK.

----------------------------------------------------------------------
### 2026-09-21 09:56:44 — soc-51 : internationalisation (i18n) de la plateforme (01_TODO_01)

**Tâche** : en tant qu'utilisateur, pouvoir modifier la langue de la plateforme. Prendre en
compte le formatage des heures/dates et la devise. Par défaut, la langue est celle du navigateur.

**Implémentation**
Backend (préférence persistée par utilisateur) :
- Migration Flyway `V48__user_language.sql` : `ALTER TABLE user_account ADD COLUMN language VARCHAR(10);`
  (nullable → `null` = langue du navigateur).
- `UserAccount` : champ `language` ; `UserDtos.UserDto` : `language` ; `UserDtos.LanguageRequest`.
- `AuthService.updateLanguage(userId, language)` : normalise en minuscules, accepte `fr`/`en`
  (`ALLOWED_LANGUAGES`), vide/`null` = retour au défaut navigateur, sinon `400`. `toDto` expose
  `language`.
- `AuthController` : `PUT /api/auth/me/language`.

Frontend (React 19 / Vite) :
- `src/i18n/messages.ts` : dictionnaires `fr`/`en`, langues supportées, locales (`fr-FR`, `en-US`).
- `src/i18n/index.tsx` : `I18nProvider` + `useI18n` (langue résolue = préférence utilisateur >
  `localStorage` `soc360.language` > `navigator.language`, repli `fr`) ; `setLanguage(lang | null)`
  met à jour le `localStorage`, `document.documentElement.lang` et persiste côté backend via
  `authApi.updateLanguage` + `refreshMe`.
- `src/i18n/LanguageSelector.tsx` : sélecteur de langue (en-tête et page de connexion).
- `src/lib/format.ts` : `setFormatLocale(locale)` ; `formatDate`/`formatDateTime`/`formatMoney`/
  `formatSize`/`monthLabel`/`monthShort` utilisent la locale active (`Intl`), unités `o/Ko/Mo`
  vs `B/KB/MB`. Défaut inchangé (`fr-FR`).
- `main.tsx` : `I18nProvider` monté sous `AuthProvider`/`SocProvider`.
- `api/types.ts` + `api/auth.ts` : `UserDto.language` et `updateLanguage`.
- Traductions câblées : `layout/MainLayout.tsx` (navigation, groupes, horloge, en-tête),
  `pages/Login.tsx`, `pages/Settings.tsx` (nouvelle section « Langue de l'application » +
  préférences) et composants partagés `components/ui.tsx` (Chargement/Actualiser) et
  `components/data.tsx` (pagination, état vide, fermeture modale). Les autres pages restent en
  français et pourront être migrées progressivement.

**Vérification** : backend — 3 nouveaux tests `AuthServiceTest` (`updateLanguage_supportedLanguage_isSaved`,
`updateLanguage_blank_resetsToBrowserDefault`, `updateLanguage_unsupported_throwsBadRequest`) ;
`AuthServiceTest` 37/37 ; suite complète 303 tests, seul échec pré-existant non lié
`SecurityIntegrationTest.adminCannotDeleteActiveUser` (order-dependent).
Frontend — nouveaux tests `src/i18n/i18n.test.tsx` (4) et `format.test.ts` (3, formatage localisé) ;
18 fichiers / 170 tests, `lint` 0 erreur, `tsc -b` OK, `vite build` OK.

----------------------------------------------------------------------
### 2026-09-21 12:18:16 — soc-51 : table de langues en base, gestion admin et import/export JSON (01_TODO_01)

**Tâche** : l'utilisateur peut modifier la langue de la plateforme. Langues gérées : FR, EN, AR.
Chaque chaîne en dur devient une clé dans une table `langue` (`id, key, FR, EN, AR`) ; pour
ajouter une langue on ajoute une colonne. Seul l'ADMIN peut ajouter une langue ; il peut exporter
la table en JSON local et l'importer (insert/update par clé).

**Implémentation**
Backend :
- Migration Flyway `V49__app_language.sql` : table `app_language (id, msg_key unique, fr, en, ar)`
  + seed des **87 clés** i18n existantes (FR/EN depuis `messages.ts`, AR rédigé), ex.
  `common.save` = Enregistrer / Save / حفظ.
- `com.soc360.i18n.LanguageService` (JdbcTemplate, pas d'entité JPA → `ddl-auto: validate` intact) :
  `availableLanguages()` (colonnes via `information_schema.columns`, ordre fr/en/ar puis colonnes
  ajoutées), `messages(lang)`, `bundle(lang)`, `adminBundle()`, `upsert(key, translations)`,
  `delete(id)`, `addLanguage(code)` (`ALTER TABLE app_language ADD COLUMN <code> TEXT`, code
  `^[a-z]{2,8}$` validé), `export()` / `importPayload()` (insert/update par clé, langues inconnues
  ignorées).
- `I18nController` : `GET /api/public/i18n?lang=` (permitAll, utilisé avant connexion) →
  `{ defaultLanguage, languages, language, messages }`.
- `LanguageAdminController` (`/api/admin/i18n`, `@PreAuthorize("hasRole('ADMIN')")`) :
  `GET` liste, `POST`/`PUT /{key}` upsert, `DELETE /{id}`, `POST /languages` (ajout de colonne),
  `GET /export`, `POST /import`.
- `AuthService.updateLanguage` valide désormais contre les langues réellement disponibles
  (`LanguageService.isSupportedLanguage`) ; `null`/vide = langue du navigateur.

Frontend :
- `api/i18n.ts` : client des endpoints public + admin.
- `i18n/index.tsx` : l'`I18nProvider` charge le bundle depuis le backend à chaque changement de
  langue et le fusionne par-dessus les messages statiques (repli FR/EN hors ligne) ; expose
  `languages` dynamiques, `rtl`, `preference`. `detectBrowserLanguage` reconnaît fr/en/ar.
- `i18n/messages.ts` : `Language` devient une chaîne (langues dynamiques), ajout de l'arabe
  (`العربية`, locale `ar`, `isRtl`), `languageLabel`.
- Direction RTL : `document.documentElement.dir = rtl ? 'rtl' : 'ltr'` dans le provider.
- `pages/Languages.tsx` (route `/langues`, ADMIN) : liste des langues, ajout d'une langue (colonne),
  édition/suppression des traductions par clé, ajout de clé, **export JSON** (téléchargement) et
  **import JSON** (fichier local). Lien de menu « Langues » dans Administration.
- `Login` et `Settings` utilisent la liste de langues renvoyée par le backend ; le sélecteur
  d'en-tête est dynamique.

**Vérification** : backend — nouveau `LanguageIntegrationTest` 8/8 (bundle fr/en, repli langue
inconnue, upsert insert/update, clé vide refusée, ajout de colonne + traduction, doublon/code
invalide refusés, export→import) ; `AuthServiceTest` 37/37 ; suite complète 311 tests, seul échec
pré-existant non lié `SecurityIntegrationTest.adminCannotDeleteActiveUser` (order-dependent).
Frontend — `src/i18n/i18n.test.tsx` enrichi (traductions distantes prioritaires) ; 18 fichiers /
171 tests, `lint` 0 erreur, `tsc -b` OK, `vite build` OK.

**Remarque** : toutes les chaînes déjà internationalisées sont dans la table. L'extraction des
nombreuses chaînes encore en dur dans les autres pages reste à faire progressivement : elles
pourront être ajoutées comme clés via l'écran ADMIN (import JSON / saisie), le repli statique
garantissant le fonctionnement hors ligne. L'ajout d'une langue est le seul DDL d'exécution,
explicitement demandé par la story.

----------------------------------------------------------------------
### 2026-09-21 12:38:23 — soc-51 : page Langues, sélection dans une liste filtrée + remplissage automatique (01_TODO_01)

**Tâche** : page Langues — remplacer l'input texte d'ajout de langue par une **liste de toutes
les langues connues** avec un **input de filtre** au-dessus pour choisir le bon élément ;
ajouter un bouton permettant de **remplir la traduction** de la langue nouvellement ajoutée
(en s'appuyant sur une API).

**Implémentation**
Frontend :
- `src/i18n/languages.ts` : catalogue des langues connues (~95 codes ISO avec libellés natifs),
  `findKnownLanguage()` et `filterKnownLanguages(query)` (recherche par code ou libellé).
- `pages/Languages.tsx` : l'ajout de langue se fait désormais via un **champ de filtre** + une
  **liste déroulante** (`<select multiple size>`) des langues connues non encore présentes, puis
  le bouton « Ajouter la langue » ; la sélection courante et le nombre de résultats sont affichés.
- Chaque langue ajoutée (hors `fr`) dispose d'un **bouton « remplir »** (baguette) qui appelle
  `POST /api/admin/i18n/languages/{code}/autofill` avec la langue source `fr` et affiche le
  nombre de traductions/valeurs recopiées, puis recharge la table.

Backend :
- `LanguageService.autofillLanguage(code, source)` : pour chaque clé dont la cellule cible est
  vide, tente une traduction via un **fournisseur HTTP configurable**
  (`app.i18n.translate-url`, vide par défaut) appelé avec `RestClient` (format MyMemory
  `?q=…&langpair=source|target`), sinon **replie en recopiant** la valeur source ; renvoie
  `{ language, source, translated, copied }`. Garde-fous : code/langue source valides, source ≠ cible.
- `POST /api/admin/i18n/languages/{code}/autofill` (`LanguageAdminController`, ADMIN) ;
  `application.yml` : `app.i18n.translate-url: ${I18N_TRANSLATE_URL:}`.

**Vérification** : backend — `LanguageIntegrationTest` 12/12 (dont `autofillLanguage_fillsEmptyCellsFromSource`
et `autofillLanguage_sameSourceOrUnknownTarget_isRejected`) ; suite complète 315 tests, seul échec
pré-existant non lié `SecurityIntegrationTest.adminCannotDeleteActiveUser` (order-dependent).
Frontend — 18 fichiers / 171 tests, `lint` 0 erreur, `tsc -b` OK, `vite build` OK.

----------------------------------------------------------------------
### 2026-09-21 13:02:44 — soc-51 : page Langues, liste sur 5 lignes, bouton de remplissage et lib JS de traduction (01_TODO_01)

**Tâche** (3 sous-parties, chacune « go ») :
- liste « Langues connues » : nombre de lignes apparent **5** ;
- bouton « Remplir les traductions » : **à droite de la liste**, marge **20 px**, **même largeur
  que la liste à gauche** ;
- ajouter une **lib JS** permettant de se connecter à un site de traduction et de remplir la
  colonne de la langue ajoutée.

**Implémentation** (frontend uniquement)
- `pages/Languages.tsx` : `<select size={5}>` pour la liste des langues connues. Dans la section
  « Remplir les traductions », le bouton est placé à droite du sélecteur avec `ml-5` (20 px) et
  `w-64` (même largeur que la liste) ; conteneur sans `gap` pour que la marge fasse exactement 20 px.
- Nouvelle dépendance **`translate@3.1.0`** (lib JS navigateur, moteur Google) et module
  `src/lib/translate.ts` exposant `translateTexts(texts, from, to)` : traduction par lots avec
  5 requêtes en parallèle, résultats indexés par clé, échecs/valeurs vides ignorés.
- `autofillLanguage(code)` : récupère le bundle FR (`GET /api/public/i18n`) et la table admin,
  ne traite que les clés dont la colonne cible est vide, traduit FR→`code` via la lib puis
  enregistre le tout en **une requête** `POST /api/admin/i18n/import` ; message récapitulatif
  (clés traduites / échecs), puis rechargement de la table et du bundle i18n.
- Nouveau test `src/lib/translate.test.ts` (résultats par clé, échecs et valeurs vides ignorés).

**Vérification** : frontend — 19 fichiers / 173 tests, `lint` 0 erreur, `tsc -b` OK, `vite build` OK.
Backend non modifié.

----------------------------------------------------------------------
### 2026-09-21 15:00:20 — soc-51 : extraction des chaînes en dur et remplacement par des clés i18n (01_TODO_01)

**Tâche** : remplacer toutes les chaînes de caractères en dur par quelque chose du style
`{t('...')}` (`...` = clé de la chaîne), identifier d'abord toutes les chaînes en dur de
l'application et mettre chaque clé dans la table `langue`. `go`.

**Implémentation**
- `src/i18n/translate.ts` : nouvelle fonction **`tr(key, params?)` hors hook** (état global alimenté
  par `I18nProvider` via `setTranslationState`), utilisée pour keyer les chaînes partout sans devoir
  injecter `useI18n` dans chaque composant. Le provider remonte son sous-arbre par une clé
  `language:bundleVersion` pour refléter la langue et le chargement des traductions distantes.
- **Codemod TypeScript-AST** (`ts.createSourceFile`, exécuté hors dépôt) qui :
  - extrait les nœuds `JsxText` (mono- et multi-lignes, espaces normalisés) et les attributs UI
    `title / placeholder / aria-label / alt / label / description / subtitle`, ainsi que les
    arguments de `confirm()` / `alert()` ;
  - génère des clés `<Fichier>.<slug>` uniques (en réservant les clés déjà présentes) ;
  - remplace par `{tr('clé')}` (ou `tr('clé')` dans les appels) et ajoute l'import `tr`.
  Résultat : **761 remplacements / 716 clés** dans 44 fichiers `.tsx`.
- `src/i18n/messages.generated.ts` : table FR générée (`EXTRACTED_FR`, 716 entrées), fusionnée
  dans `MESSAGES.fr` **et** `MESSAGES.en` (repli hors ligne : l'anglais retombe sur le français
  tant qu'il n'est pas traduit).
- Migration Flyway `V50__app_language_extracted_strings.sql` : `INSERT INTO app_language (msg_key, fr)`
  des 716 nouvelles clés (EN/AR laissés vides, à remplir via l'écran ADMIN « Remplir les traductions »).
- Chaînes dynamiques/données (`ROLE_LABELS`, `THEMES`, catégories, messages d'erreur API, libellés
  de format) non traitées dans ce lot : elles restent en français et pourront être keyées ultérieurement.

**Vérification** : frontend — 19 fichiers / 173 tests, `lint` 0 erreur, `tsc -b` OK, `vite build` OK.
Backend — `LanguageIntegrationTest` 12/12 (migration V50 appliquée par Flyway).

----------------------------------------------------------------------
### 2026-09-21 16:05:16 — soc-51 : la page Langues reste en français quand la langue est English (01_TODO_01)

**Tâche** : sur la page Langues, la chaîne « Langues et traductions » s'affiche encore en français
alors que la langue English est sélectionnée → les chaînes en dur n'ont pas toutes été internationalisées.

**Diagnostic** : les chaînes étaient bien keyées (`tr('Languages.langues.et.traductions')`), mais
les colonnes **EN (et AR) de la table étaient vides** pour les 716 clés extraites ; le repli
statique mappait l'anglais sur le français, d'où l'affichage français en mode English.

**Implémentation**
- Génération des **traductions EN et AR** des 716 clés via un service de traduction (endpoint
  Google `translate_a/single`, script hors dépôt, 0 échec).
- `src/i18n/messages.generated.ts` : ajout de `EXTRACTED_EN` et `EXTRACTED_AR` (738 clés chacune,
  caractères de largeur nulle nettoyés) ; `messages.ts` mappe désormais
  `en: { ...en, ...EXTRACTED_EN }` et `ar: { ...EXTRACTED_AR }` (plus de repli anglais → français).
- Migration Flyway `V52__app_language_extracted_translations.sql` : `UPDATE ... SET en, ar` des
  716 clés (base fraîche : V50/V51 posent le FR, V52 complète ; base dev existante : V52 s'applique).
- Migration `V53__app_language_dynamic_messages.sql` : 22 nouvelles clés (`languages.*`) pour les
  messages dynamiques de la page Langues (confirmations, erreurs, compteurs) avec FR/EN/AR.
- `pages/Languages.tsx` : remplacement des dernières chaînes en dur des handlers
  (`window.confirm`, `setMessage`, `setError`, `aria-label`, sélection) par `tr('languages.*', {…})`.

**Vérification** : frontend — 19 fichiers / 173 tests, `lint` 0 erreur, `tsc -b` OK, `vite build` OK.
Backend — `LanguageIntegrationTest` 12/12 (migrations V52/V53 appliquées par Flyway).
En mode English, la page Langues et les 738 chaînes extraites s'affichent désormais en anglais
(les migrations s'appliquent au prochain redémarrage du backend ; le repli statique assure déjà
l'affichage EN).

**Résiduel** : les messages dynamiques des autres pages (erreurs API `throw`, `confirm`/`alert`
templated), les libellés de données (`ROLE_LABELS`, statuts, `THEMES`, catégories de documents/
notes de frais) et les chaînes des modules `.ts` (format, api) restent en français.

----------------------------------------------------------------------
### 2026-09-21 16:11:28 — soc-51 : page Langues, compteur de chaînes et numéro de ligne (01_TODO_01)

**Tâche** : sur la page Langues — afficher à droite de « Chaînes traduites » le **nombre de
chaînes** (`<nb_chaines>`) ; ajouter une **colonne virtuelle** en début de table contenant le
**numéro de ligne** (à partir de 1) ; `nb_chaines` = nombre total de lignes de la table.

**Implémentation** (frontend uniquement — `pages/Languages.tsx`)
- Titre de la carte remplacé par une ligne `flex` : « Chaînes traduites » suivi, à droite, du
  compteur `entryCount = bundle.entries.length`.
- Nouvelle première colonne **`#`** (alignée à droite, chiffres tabulaires) affichant `index + 1`
  pour chaque ligne (`bundle.entries.map((entry, index) => …)`).
- Les conditions vide/non-vide utilisent désormais `entryCount`.

**Vérification** : frontend — 19 fichiers / 173 tests, `lint` 0 erreur, `tsc -b` OK, `vite build` OK.
Backend non modifié.

----------------------------------------------------------------------
### 2026-09-21 16:16:23 — soc-51 : page Langues, filtre de recherche et pagination (01_TODO_01)

**Tâche** : table des chaînes traduites — ajouter un **filtre de recherche** pour retrouver une
ligne, et **paginer** avec le nombre de lignes défini dans la préférence utilisateur « Lignes par page ».

**Implémentation** (frontend uniquement — `pages/Languages.tsx`)
- Champ **« Rechercher une ligne »** au-dessus du tableau : filtre insensible à la casse et aux
  accents, porte sur la **clé** et sur les **valeurs traduites** (toutes langues) ; affichage
  `résultats / total` quand une recherche est active.
- **Pagination** réutilisant `Pagination` (`components/data.tsx`) avec `pageSize = user.pageSize`
  (« Lignes par page ») ; la page revient à 0 quand la recherche ou la taille change ; message
  « aucun élément » si la recherche ne retourne rien.
- La colonne **`#`** conserve une numérotation **globale** (poursuivie d'une page à l'autre :
  `safePage * pageSize + index + 1`).
- Nouvelles clés statiques FR/EN/AR `languages.searchRows` / `languages.searchPlaceholder`
  (`src/i18n/messages.ts`, objet `ar` ajouté).

**Vérification** : frontend — 19 fichiers / 173 tests, `lint` 0 erreur, `tsc -b` OK, `vite build` OK.
Backend non modifié.

----------------------------------------------------------------------
### 2026-09-21 16:43:10 — soc-51 : formatage date/heure et devise selon la langue (01_TODO_01)

**Tâche** : au changement de langue, l'application doit prendre en compte le **formatage
heure/date** et la **devise** ; par défaut la langue est celle du navigateur. Vérifier d'abord
si c'est déjà implémenté, sinon l'implémenter.

**Vérification préalable** : déjà partiellement implémenté — `lib/format.ts` (`setFormatLocale`,
`formatDate`, `formatDateTime`, `formatMoney`, `monthLabel`, `monthShort`) et l'appel
`setFormatLocale(locale)` à chaque changement de langue dans `I18nProvider` ; langue par défaut =
`detectBrowserLanguage()` (`navigator.language`). Manquaient : des **contournements** dans
plusieurs composants (`MONTHS_FR` en dur, `toLocaleDateString/String('fr-FR'…)`) et la gestion
de l'**arabe** (mois retombaient en français).

**Implémentation** (frontend)
- `lib/format.ts` : ajout des tableaux `MONTHS_AR`/`MONTHS_AR_SHORT` et prise en charge de `ar`
  dans `setFormatLocale` ; nouveau `formatNumber()` (locale active).
- Remplacement des `MONTHS_FR[…].slice(0,3)`/`MONTHS_FR.map(…)` par `monthLabel()`/`monthShort()`
  et des `toLocaleDateString/toLocaleString('fr-FR'…)` par `getFormatLocale()` dans :
  `CraDetail`, `CraList`, `Facturation`, `Holidays`, `IndispoCalendar`, `IndispoList`, `Missions`,
  `NoteFraisList`, `UnavailabilityCalendar`, `Consultants`.
- Devise : `formatMoney(value, currency)` formatait déjà via `Intl` selon la locale active
  (symbole/position/séparateurs) — aucun changement nécessaire.
- Tests : `format.test.ts` complété (mois/nombres en arabe, `getFormatLocale`).

**Vérification** : frontend — 19 fichiers / 175 tests, `lint` 0 erreur, `tsc -b` OK, `vite build` OK.
Backend non modifié.

**Résiduel** : abréviations de jours des calendriers (`['Lun',…]` dans `CraDetail`/`Holidays`)
restent en français.

----------------------------------------------------------------------
### 2026-09-22 11:32:01 — soc-53 : uniformiser les boîtes de dialogue info / oui-non (01_TODO_01)

**Tâche** : uniformiser toutes les boîtes de dialogue (info, oui/non, saisie) — ne plus utiliser
celles du navigateur (`window.confirm`, `window.alert`, `window.prompt`). Boîtes centrées
(600 × 500 max), ascenseur vertical si texte long, boutons arrondis avec hover, de même largeur,
jamais pleine ligne.

**Implémentation** (frontend uniquement)
- Nouveau composant `src/components/dialog.tsx` : `DialogHost` (rendu via portal) + API impérative
  `dialog.info / success / warning / error / confirm / prompt`. File d'attente, résolution par
  Promise, fermeture par Échap / clic sur le fond, largeur 600 px, corps `max-h-[500px]` +
  `overflow-y-auto` (ascenseur si texte long), deux boutons `w-32` arrondis avec hover (Annuler +
  Confirmer), icône/couleur selon la variante et option `danger` (bouton rouge).
- `src/main.tsx` : montage de `<DialogHost />` dans l'`I18nProvider` (App intacte).
- Nouvelles clés statiques FR/EN `dialog.*` (`src/i18n/messages.ts`) : `ok`, `confirm`,
  `confirmTitle`, `infoTitle`, `successTitle`, `warningTitle`, `errorTitle`, `input`.
- Remplacement de tous les `window.confirm/alert/prompt` (24 fichiers) : pages Activities,
  ActivityTypes, Clients, Consultants, CraDetail, CraList, Documents, Facturation, FichePaie,
  Holidays, IndispoList, Languages, Messages, NoteFraisList, Profile, Projects, Register,
  SocAdmin, Suppliers, Support, Tables, Unavailability et `soc/AddSocModal` ; les handlers
  concernés passent en asynchrone (`await dialog.confirm/prompt`), les erreurs en
  `void dialog.error(...)`.
- Tests adaptés (CraList, NoteFraisList, Unavailability, Register) : montage de `DialogHost` et
  interaction avec la boîte (saisie + bouton Confirmer/Supprimer) au lieu des stubs globaux.

**Vérification** : frontend — 19 fichiers / 175 tests, `lint` 0 erreur, `tsc -b` OK, `vite build` OK.
Backend non modifié.

----------------------------------------------------------------------
### 2026-09-22 15:46:08 — soc-58 : uniformiser les boutons (01_TODO_01)

**Tâche** : ajouter dans la page Paramètres la config des boutons de l'application — largeur des
boutons à texte court (OK, Cancel, Yes…) = 50 px, largeur des boutons à texte long (Export PDF
Client, Nouvelle note de frais, Supprimer tous les événements…) = 100 px, couleur du bouton save
= bleu, du bouton delete = rouge, etc. Boutons arrondis avec hover, appliqués à toute l'app.

**Implémentation**
- Backend — Flyway `V54__user_button_settings.sql` : colonnes `button_small_width` (défaut 50),
  `button_large_width` (défaut 100), `button_save_color` (défaut `#1d48eb`),
  `button_delete_color` (défaut `#dc2626`) sur `user_account`.
- Backend — `UserAccount` (champs + valeurs par défaut), `UserDtos.UserDto` + nouveau
  `ButtonSettingsRequest`, `AuthService.updateButtonSettings` (validation largeurs 20–400 px et
  couleurs `#RRGGBB`), `AuthController` `PUT /api/auth/me/button-settings`. `toDto` enrichi.
- Frontend — `api/types.ts` (UserDto) + `api/auth.ts` (`updateButtonSettings`).
  `AuthContext` applique les variables CSS `--btn-small-width`, `--btn-large-width`,
  `--btn-save-color`, `--btn-delete-color` sur `<html>` ; valeurs par défaut dans `index.css`.
- Frontend — `components/ui.tsx` : `Button` (variantes primary/save, danger/delete, yellow, green,
  neutral ; largeur `min-width` = petit/grand selon la longueur du libellé, surchargeable via
  `size`) et `InlineButton` (variantes neutral/danger/primary) — coins arrondis (`rounded-xl`),
  hover (`brightness`), min-width configurables.
- Frontend — `pages/Settings.tsx` : section « Boutons » (largeur petit/texte long, couleurs
  Enregistrer/Supprimer, aperçu) + nouvelles clés i18n `settings.buttons.*` (FR/EN).
- Frontend — `components/dialog.tsx` : boutons Annuler/Confirmer alignés sur les variables
  (largeur + couleur save/delete). Boutons destructifs de toutes les pages (Activities,
  ActivityTypes, Clients, Consultants, CraDetail, CraList, Documents, FichePaie, IndispoList,
  Languages, Messages, NoteFraisList, Profile, Projects, SocAdmin, Suppliers, Tables,
  Unavailability) passés en `variant="danger"`.

**Vérification** : backend — `mvn test` OK (315 tests ; seul échec = `SecurityIntegrationTest.
adminCannotDeleteActiveUser`, pré-existant et order-dependent, OK en isolation). Frontend —
19 fichiers / 175 tests, `lint` 0 erreur, `tsc -b` OK, `vite build` OK.

------------------
### 2026-09-24 11:33:38 — soc-51 : internationalisation des textes restants (01_TODO_01)

**Tâche** (contenu de 01_TODO_01) : internationaliser les derniers textes codés en dur des pages
Activités / Types d'activité, Projets, Sociétés, Clients, Collaborateurs et Dashboard (libellés
« Nouveau/Nouvelle… », en-têtes de colonnes, boutons Modifier/Supprimer/Annuler/Enregistrer/Créer,
libellés d'actions et messages d'erreur), et traduire à la volée les valeurs issues de la base
(rôles utilisateur, statuts actif/active, week-end / jours fériés, statut CRA du dashboard) via un
cache mis à jour à chaque changement de langue (lib `translate`).

**Implémentation**
- Frontend — nouvelle infra de traduction dynamique : `src/lib/dynamicTranslate.ts` (cache par
  langue, file d'attente + `translateTexts`, abonnements, `setDynamicLanguage`) et
  `src/lib/useDynamicTranslate.ts` (hook `useSyncExternalStore`). Branchée dans `I18nProvider`
  (`setDynamicLanguage(language)` à chaque changement de langue).
- Frontend — 105 nouvelles clés i18n dans `src/i18n/messages.generated.ts` (FR/EN/AR) : namespace
  `common.*` (new, edit, delete, create, disable, enable, active, inactive, status, type, period,
  role, company, history, importCsv, unexpectedError, yes, no, notes, search…) + clés par page
  (`ActivityTypes.*`, `Projects.*`, `Activities.*`, `SocAdmin.*`, `Clients.*`, `Consultants.*`,
  `Dashboard.chez`).
- Frontend — internationalisation des pages ActivityTypes, Projects, Activities, SocAdmin,
  Consultants, Clients et Dashboard (libellés, modales et dialogues de confirmation via
  `tr(key, { params })`).
- Frontend — traduction dynamique des valeurs BDD : rôles (`ROLE_LABELS`) et statuts CRA / notes
  de frais du Dashboard via `useDynamicTranslate` ; week-end / jours fériés et actif/inactif via
  les clés `common.yes`/`common.no`/`common.active`/`common.inactive`.
- Backend — Flyway `V56__i18n_remaining_strings.sql` : 105 `INSERT INTO app_language (msg_key, fr,
  en, ar)` correspondant aux nouvelles clés (aucune modification de schéma).
- Tests — nouveau `src/lib/dynamicTranslate.test.ts` (3 tests).

**Vérification** : frontend — `npm run lint` OK (0 erreur), `npm test` 20 fichiers / 178 tests OK,
`tsc -b` + `vite build` OK. Backend — `mvn test` 315 tests, seul échec pré-existant et
order-dependent `SecurityIntegrationTest.adminCannotDeleteActiveUser` (OK en isolation). Flyway
V56 appliqué avec succès en H2.

------------------
### 2026-09-24 15:23:14 — soc-59 : informations professionnelles du consultant (01_TODO_01)

**Tâche** : page Consultants/Collaborateurs — ajouter l'adresse du consultant (saisie libre ou
recherche d'adresse valide en France), les informations professionnelles (statut professionnel,
Position, Coefficient, Matricule), le mode de paiement et le TJM interne, ainsi qu'une case à
cocher « Consultant salarié » conditionnant l'affichage du statut pro / position / coefficient /
matricule (servira ensuite au calcul du solde de congés des seuls salariés).

**Implémentation**
- Backend — Flyway `V61__consultant_professional_info.sql` : colonnes `address` (255),
  `statut_professionnel` (60), `position_professionnelle` (30), `coefficient` (20),
  `matricule` (30), `mode_paiement` (30), `tjm_interne` DECIMAL(12,2), `salarie` BOOLEAN
  NOT NULL DEFAULT FALSE sur `user_account`.
- Backend — `UserAccount` (champs + getters/setters), `ConsultantDtos.ConsultantDto` et
  `ConsultantRequest` (champs ajoutés en fin de record), `ConsultantService.applyRhs`
  (affectation), `toDto` et `snapshot` (historique). `ConsultantImportService` et
  `ConsultantServiceTest` mis à jour pour les nouveaux composants de record.
- Backend — Flyway `V62__i18n_consultant_pro_info.sql` : 11 clés FR/EN/AR (`Consultants.adresse`,
  `adresse.placeholder`, `adresse.rechercher`, `adresse.aucun`, `statut.professionnel`,
  `position`, `coefficient`, `matricule`, `mode.paiement`, `tjm.interne`, `salarie`).
- Frontend — `api/types.ts` : `ConsultantDto`/`ConsultantRequest` enrichis. `messages.generated.ts`
  : 11 clés FR/EN/AR.
- Frontend — `pages/Consultants.tsx` : état de formulaire étendu (adresse + champs pro), champ
  Adresse avec saisie libre + bouton « Rechercher une adresse » interrogeant l'API Base Adresse
  Nationale (`api-adresse.data.gouv.fr`) et affichant une liste de suggestions cliquables ; champs
  Mode de paiement (défaut « Virement ») et TJM interne ; case à cocher « Consultant salarié » qui
  affiche conditionnellement Statut professionnel, Position, Coefficient et Matricule. Les champs
  conditionnels ne sont envoyés (non nuls) que si le consultant est salarié.
- Isolation multi-société inchangée : création/mise à jour passent toujours par `requireSocAccess`
  côté service.

**Vérification** : frontend — `oxlint` 0 erreur, `npm test` 20 fichiers / 178 tests OK,
`tsc -b` + `vite build` OK. Backend — `mvn test` 315 tests, seul échec pré-existant et
order-dependent `SecurityIntegrationTest.adminCannotDeleteActiveUser` (OK en isolation) ; Flyway
V61/V62 appliqués avec succès en H2.
