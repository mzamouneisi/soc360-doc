11/08/2026
renomer comme leurs repos sur github : 
    soc360-backend en soc360-back-java
    soc360-frontend en soc360-front-react 

11/08/2026
mettre le nom du user connecté, son role et le btn Quitter 
dans l'entete, en haut à droite 

11/08/2026
quand je click sur le user connected, j'affiche le profil

11/08/2026
comme ADMIN n'a pas de société, on remplace dans l'entete, la liste 
"Espace de Travail" par le btn Dashboard 

11/08/2026
sous soc360-doc : 
generer une doc fonctionelle du projet : DOC_USER.docx
une doc technique du backend : DOC_BACKEND.docx
une doc technique du frontend : DOC_FRONTEND.docx

11/08/2026
le projet soc360 (front et back et doc) gere les activitées de sociétés et pas seulement des esn.
donc on doit remplacer le service "ajouter une esn" par "ajouter une société".
on doit modifier la table client afin qu'elle contienne un id_societe et un champ notes de type text afin de rajouter infos specifique de ce client pour la société en cours.

11/08/2026
remplacer la table et objet esn par soc (table, objet, FK/colonnes id_esn -> id_soc)
remplacer la colonne logo_path par logo
supprimer les colonnes de la table client qui se trouvent deja dans soc
(la table client contient maintenant id_soc + notes + les colonnes specifiques)

11/08/2026
generer un script sh dans le backend kill_backend.sh qui va stopper le processus du backend.

11/08/2026
ajouter l'entité et la page fournisseur comme pour le client
     (table supplier + package com.soc360.supplier + API /api/suppliers + page Fournisseurs /fournisseurs + migration V11)

12/08/2026
contexte : 
dans les tables client et fournisseur, il y a la colonne id_soc : elle pointe vers la société de ce client / fournisseur.
on doit donc ajouter une colonne id_soc_parent : elle pointe vers la société parent de ce client / fournisseur.

exemple : 
la societe esn (Eisi-Consulting) à un client SFR.
Société parent = ESI CONSULTING

go

12/08/2026
dans la page profil
quand je veux supprimer une société :
    on me demande de confirmer la suppression
    si je click sur oui : le systeme verifie s'il y a des objets liés à cette société :
    si oui : les lister dans une dialog avec un btn supprimer pour chaque objet lié.
    si non, supprimer la soc

go

12/08/2026
le responsable_soc peut modifier, supprimer les sociétés qu'il a ajouté via la page de son profil.
go

12/08/2026
table soc : 
ajouter id_user : le user connecté qui a ajouté cette société.

ajouter : histo_soc : id, id_soc, id_user, date_maj, infos_avant, infos_apres, diff 
table qui trace les maj de soc 

go

12/08/2026
dans la page : inscrire une nouvelle societe :
apres recherche : 
adresse mail : <username>@<domaine_site_web>
exemple : 
username : ieddinemansour
site_web  : www.eisiconsulting.com

alors adresse mail : ieddinemansour@eisiconsulting.com
go

12/08/2026
page inscription soc : 
si nom de société renseignée, apres rechercher, mettre a jour le champ site web par le bon, si pas connu, mettre :
www.<nom_soc>.com 
si gerant est trouvé ou renseigné : 
mettre a jour prenom et nom de l'administrateur 
(ces champs à deplacer vers le haut en dessous du gérant)
(champ adresse mail a mettre sous username)
mettre a jour champ username : <first_letter_of prenom>nom 
(a deplacer en dessous des champs prenom et nom)

go

12/08/2026
toujours dans la page : inscription d'une societe :
s'il y a plusieurs sociétés dans le résultat du btn rechercher 
alors proposer une liste de choix dans une boite de dialog 
go

12/08/2026
ajouter dans l'ihm (qui seront pre remplit par le btn rechercher) :
l'adresse de la societe 
activite_principale
categorie_entreprise
date_creation
date_fermeture
dirigeants (gérant)

go

12/08/2026
modifier le script run_dev.sh dans le backend afin de redemarrer le backend 
si il y a un changement dans les sources 
go

12/08/2026
ajouter d'autres colonnes dans la table soc afin d'avoir pas mal d'infos sur une société.
une colonne texte : infos_web : le btn rechercher remplit d'abord dans ce champ. puis remplir dans les autres :
code siret
code naf
gerant
adresse
ville
pays 
...
go

12/08/2026
dans la page : inscrire une nouvelle societe :
ajouter un btn juste apres le input du siret : 
son action : chercher sur le web les infos de la société via son nom et/ou son siret.
remplir les autres champs par ces infos.
go

12/08/2026
completer eventuellement les tests unitaires
executer 
corriger les erreurs eventuelles 
go

12/08/2026
il faut unifier les 2 pages pour ajouter une nouvelle société : 
1. lorsque je ne suis pas connecté 
2. lorsque je suis connecté : je garde mon username, password, email 

go

12/08/2026
corriger l'erreur TransientObjectException lors de la suppression d'une société :
- le code actuel dans SocService.delete() ne gérait pas les entités enfants (HistoSoc)
- ajout de la suppression des enregistrements HistoSoc avant la suppression de la Soc
- modification de SocServiceTest pour ajouter les mocks manquants

go

13/08/2026
corriger le Server Error lors de la suppression d'une société (TransientObjectException) :
la vraie cause : quand l'utilisateur connecté (ou d'autres comptes) est lié à la société
via user_account.id_soc ou user_soc, la société marquée DELETED reste référencée par
ces entités MANAGED au moment du flush -> le contrôle Hibernate CHECK_ON_FLUSH
(CascadingActions$9) lève TransientObjectException "persistent instance references an
unsaved transient instance of Soc".
- SocService.delete() détache maintenant la société de tous les users :
    user_account.id_soc mis à NULL (findBySocId) + retrait de user_soc
    (findBySocIdInSocs + getSocs().remove(soc)) avant de supprimer la Soc
- UserAccountRepository : ajout de findBySocId(Long) et findBySocIdInSocs(Long)
- test d'intégration SocDeleteIntegrationTest (3 cas : owner lié à la soc,
  admin sans lien, autre user lié uniquement via user_soc)

go

13/08/2026
je suis connecté en tant que RESPONSABLE_SOC
je vais dans ma page profil
je vois que j'ai deux sociétés
je veux supprimer l'une d'elle
je click sur supprimer
je confirme
le code doit m'avertir que je vais supprimer avec tous les objets liés en me les affichant dans une dialog 500x500
je dois confirmer la suppression de tout
- backend : SocService.deleteWithDependencies() supprime la société + tous les objets liés
  (payments, subscriptions, activités, types d'activités, projets, fournisseurs, clients,
  consultants) dans l'ordre FK-safe + détache les users + les histo_soc.
  Repos : ajout de deleteAllBySocId (et deleteAllBySubscriptionSocId pour Payment)
  endpoint : DELETE /api/socs/{id}/all
- frontend : socs.api.removeWithDependencies(id) ; la page profil affiche une dialog 500x500
  qui avertit « cette société sera supprimée avec tous ses objets liés », liste les objets
  liés (avec leur type) et un bouton « Tout supprimer » qui confirme la suppression de tout.
- test d'intégration deleteWithDependencies_removesAllLinkedObjects (SocDeleteIntegrationTest)

go

13/08/2026
corriger l'erreur « Vous ne pouvez gérer que les sociétés que vous avez ajoutées »
lors de la suppression (simple ou « Tout supprimer ») d'une société par le responsable_soc :
- cause : AuthService.addSoc() (POST /api/auth/me/socs, page « ajouter une société » quand
  on est connecté) ne positionnait jamais soc.id_user (createdByUser) -> la société n'avait
  aucun créateur -> ensureOwnerOrAdmin() (SocService) levait une erreur 403 à chaque gestion
  (modification / suppression) de cette société.
- fix : addSoc() fait maintenant soc.setCreatedByUser(user) avant le save.
- pour les sociétés déjà existantes sans créateur (créées via addSoc avant ce correctif) :
  ensureOwnerOrAdmin() autorise le responsable_soc dont la société fait partie de
  user_account.user_soc (fallback sur le lien de l'utilisateur), en plus du créateur explicite.
- tests ajoutés : AuthServiceTest.addSoc vérifie createdByUser ;
  SocServiceTest : delete_legacySocWithoutCreator_linkedViaUserSocs_succeeds,
  delete_legacySocWithoutCreator_notLinked_forbidden ;
  SocDeleteIntegrationTest : deleteSoc_legacySocWithoutCreator_linkedViaUserSocs_succeeds.

go

13/08/2026
corriger l'erreur d'intégrité lors de « Tout supprimer » d'une société :
violation FK fk_cra_consultant : la table cra (et autres) référence consultant.id.
- les tables référençant consultant / activity / soc n'étaient pas supprimées avant
  leurs parents dans deleteLinkedObjects() :
    cra_day_activity (activity_id + cra_day_id)
    cra_day (cra_id)
    cra (consultant_id)
    note_frais_line (note_frais_id)
    note_frais (consultant_id + id_soc)
    fiche_paie (consultant_id)
    cra_configuration (id_soc)
    client.id_soc_parent (référence une autre société comme soc parent)
- ajout des méthodes de purge (JPQL @Modifying) :
  CraDayActivityRepository.deleteByConsultantSocId, CraDayRepository.deleteByConsultantSocId,
  CraRepository.deleteByConsultantSocId, NoteFraisLineRepository.deleteBySocId,
  NoteFraisRepository.deleteBySocId, FichePaieRepository.deleteByConsultantSocId,
  CraConfigurationRepository.deleteBySocId, ClientRepository.detachSocParent
- deleteLinkedObjects() supprime maintenant dans l'ordre FK-safe :
  cra_day_activity -> cra_day -> cra -> note_frais_line -> note_frais -> fiche_paie ->
  cra_configuration -> detach soc_parent -> payments -> subscriptions -> activities ->
  activity_types -> projects -> suppliers -> clients -> consultants -> (deleteSoc)
- tests : deleteWithDependencies_removesAllLinkedObjects enrichi (cra, cra_day,
  cra_day_activity, note_frais, note_frais_line, fiche_paie, cra_configuration) ;
  nouveau deleteSoc_detachesSocParentOnClientsOfOtherSocs
  (SocDeleteIntegrationTest : 6 tests OK)

GO

13/08/2026
corriger le deploy du front afin qu'il utilise le backend sur azure.
mon front : https://mzamouneisi.github.io/soc360-front-react/login
au login : POST https://esn360-backend-v2-dev.whiteforest-96ad5fb7.francecentral.azurecontainerapps.io/api/auth/login 401 (Unauthorized)
- diagnostic : le front GitHub Pages est déjà correctement configuré et à jour :
  - le site live (main/docs) sert le build qui embarque l'URL du backend Azure dev
    (index-DJO06bG8.js contient https://esn360-backend-v2-dev...azurecontainerapps.io)
  - branche main == origin/main (deploy "GitHub Pages : build docs/" poussé)
  - CORS backend dev vérifié : Access-Control-Allow-Origin: https://mzamouneisi.github.io (admis)
  le 401 vient donc de la base Azure : aucun compte ne correspond (SEED_DEMO_DATA=false
  sur le backend dev -> même admin/Admin123! n'existe pas).
- fix :
  - env.server.sh (bloc psgsql_azure dev) : SEED_DEMO_DATA=true par défaut
    (remis à false en prod : le bloc psgsql_azure_prod reste inchangé)
  - DataSeeder : si seedDemoData et base non vide, on garantit quand même l'existence
    du compte admin de démo (admin/Admin123!) via ensureAdmin() (upsert par username) ;
    si base vide, seedDemo() complet comme avant.
  - (suite) ensureAdmin() réinitialise désormais aussi le mot de passe d'un ÉVENTUEL
    compte admin pré-existant (base Azure contenant d'anciennes données esn360) :
    admin est toujours connectable avec Admin123! quand SEED_DEMO_DATA=true.
  - DOC_DEPLOY_FRONT_TO_GH_PAGES.md : ajout d'un point « Dépannage » pour le cas
    401 Unauthorized au login.
- à faire pour activer : redéployer le backend dev (deploy_app_docker.sh) puis se
  connecter avec admin / Admin123!.

go

13/08/2026
je suis responsable ou manager d'une soc
et je suis connecté avec une société S
Alors je ne dois pas voir les objets (consultants, clients, ...) d'autres société

go
- backend : isolation par société appliquée au niveau controller :
  - SecurityUtils : effectiveSocId(Long) (ADMIN conserve le socId demandé, null = tout ;
    non-admin ignore le paramètre et utilise currentSocId()) et requireSocAccess(Long)
    (non-admin : l'objet doit appartenir à socIds ou socId par défaut, sinon 403).
  - endpoints filtrés par effectiveSocId : clients, suppliers, projects, consultants,
    activities, activity types, CRA (findByMonth/export), note de frais (findBySocYear/stats),
    fiche paie (findBySoc), cra_configuration, batch (export/import).
  - endpoints protégés par requireSocAccess (getById) : projects, consultants, activities,
    CRA, notes de frais, fiches de paie, documents, socs.
  - SupportController : non-admin forcé à mine=true.
  - SocController.findAll -> SocService.findAllAccessible() (non-admin : union socId + socIds) ;
    findById -> requireSocAccess(id).
  - services : CraService/NoteFraisService/FichePaieService.requireConsultantSocAccess,
    CraService.socIdOfConsultant, FichePaieService.requireFicheSocAccess, SocService.findAllAccessible().
  - frontend inchangé : le header X-SOC-Id est déjà envoyé et le backend force la SOC.
- tests : SecurityIntegrationTest (13 tests OK) :
  responsableSoc_cannotSeeConsultantsOfAnotherSoc, responsibleSoc_cannotSeeClientsOfAnotherSoc,
  manager_cannotSeeOtherSocObjects (clients, projects, suppliers d'une autre soc invisibles).
- mvn test : 164 tests, 1 échec pré-existant et indépendant
  (AuthServiceTest.registerSoc_createsSocSubscriptionAdminAndSendsVerificationEmail :
  vérifie seedDefaultsFor appelé par registerSoc alors que seul addSoc l'appelle).

13/08/2026 23:40:09
le responsable_soc peut etre un manager
- périmètre validé avec l'utilisateur : RESPONSIBLE_SOC assignable comme manager —
  dans le formulaire consultant, le RESPONSIBLE_SOC doit pouvoir être sélectionné
  (et notifié) comme manager d'un consultant, pas seulement apparaître dans la liste.
- backend : ConsultantService.managers(socId) retourne désormais l'union de :
  - findBySocIdAndRoleInAndActiveTrue(socId, [CONSULTANT, MANAGER, RESPONSIBLE_SOC])
    (liés par la société par défaut user_account.id_soc)
  - findBySocIdInSocs(socId) filtré sur active + rôle dans [CONSULTANT, MANAGER,
    RESPONSIBLE_SOC] (liés via la table de jointure user_soc — cas d'un RESPONSIBLE_SOC
    multi-sociétés dont la soc n'est pas la soc par défaut, cf. AuthService.attachSoc)
  - dédoublonnage par id (une même personne peut être liée aux deux manières).
- notifications : inchangé — à la soumission d'un CRA, CraService.submit notifie déjà
  le manager du consultant par mail quel que soit son rôle ; la note de frais n'a pas
  de notification. Un RESPONSIBLE_SOC assigné comme manager est donc notifié comme
  un MANAGER, pas seulement visible dans la liste.
- résolution du manager à l'affectation (ConsultantService.resolveManager) acceptait
  déjà les rôles CONSULTANT / MANAGER / RESPONSIBLE_SOC : aucune contrainte à lever.
- frontend : inchangé — le champ manager du formulaire consultant appelle déjà
  GET /api/consultants/managers (retenu en variable de state) et affiche tous les
  managers retournés (id/fullName/position), y compris un RESPONSIBLE_SOC.
- tests (ConsultantServiceTest, 16 OK) :
  - managers_includesResponsibleSoc (mise à jour : stub findBySocIdInSocs)
  - managers_includesResponsibleSocLinkedViaUserSocs (nouveau)
  - managers_deduplicatesUsersFoundViaBothSocAndUserSocs (nouveau)
- mvn test : 166 tests, 1 échec pré-existant et indépendant (AuthServiceTest
  registerSoc_createsSocSubscriptionAdminAndSendsVerificationEmail, inchangé).

14/08/2026 00:57:44
un RESPONSABLE_SOC peut etre lié à plusieurs socId, avec une socId favorite qui est
selectionnée à la connexion, et une socIdSelected lorsqu'il sélectionne une société
pour travailler ; la société favorite peut être définie dans la page de son profil
(4 boutons par société : select, favorite, modifier, supprimer).
- backend AuthService :
  - mySocs() retourne désormais l'union de la société favorite (user_account.id_soc)
    et des sociétés liées (user_soc), dédoublonnée par id, triée par nom. Un compte
    dont le défaut id_soc n'est pas dans user_soc voit donc sa société dans la liste.
  - addSoc ne pose plus la favorite : il ajoute la société à user_soc et ne la rend
    favorite QUE si l'utilisateur n'en avait aucune (user.getSoc() == null).
  - setFavoriteSoc(Long socId) : ne change la favorite que si la société est la
    favorite courante ou est dans user_soc (sinon 403), met à jour user_account.id_soc,
    ajoute au besoin la société à user_soc, sauvegarde et retourne l'utilisateur.
- backend AuthController : PUT /api/auth/me/socs/{socId}/favorite,
  @PreAuthorize hasRole('RESPONSIBLE_SOC').
- frontend :
  - api/auth.ts : authApi.setFavoriteSoc(socId) -> PUT /auth/me/socs/{socId}/favorite.
  - SocContext : expose favoriteSocId/favoriteSoc (user.socId) et setFavoriteSoc
    (appelle l'API, refreshMe() puis refreshSocs()) ; refreshSocs priorise à la
    connexion : favorite (id_soc) > sélection mémorisée > première société.
  - SocSelector : chaque société a un bouton étoile pour la définir comme favorite
    (rendue/disponible seulement pour RESPONSIBLE_SOC).
  - Profile : 4 boutons par société (sélectionner, favorite, modifier, supprimer),
    étoile et nom colorés pour la favorite ; sélection d'une société de travail.
  - Consultants : la liste Manager n'était plus vide : le formulaire utilisait
    user.socId (null pour un compte lié uniquement via user_soc) ; il utilise
    désormais la société sélectionnée (useSoc.selectedSocId) sinon user.socId.
- tests : AuthServiceTest (30, ajout de 4 tests) :
  - mySocs_returnsSortedListOfCurrentUserSocs (existant)
  - mySocs_includesFavoriteSocWhenNotInUserSocs (ajouté)
  - setFavoriteSoc_switchesFavoriteAmongLinkedSocs (ajouté)
  - setFavoriteSoc_rejectsSocNotLinkedToUser (ajouté)
- mvn test : 170 tests, 1 échec pré-existant et indépendant (AuthServiceTest
  registerSoc_createsSocSubscriptionAdminAndSendsVerificationEmail, inchangé).
- frontend : lint oxlint OK (aucune erreur source ; warnings docs/ pré-existants),
  vitest 110 tests OK, tsc + vite build OK.

14/08/2026 12:05:49
en tant que RESPONSABLE_SOC (et donc aussi en tant que manager), quand il ajoute un
client ou un fournisseur, le formulaire contient une partie « société » (choisir une de
ses sociétés ou en inscrire une nouvelle inline) + les parties notes, contact, tel,
email, actif.
- frontend : AddSocModal extrait de SocSelector.tsx en composant partagé
  src/soc/AddSocModal.tsx avec un callback onCreated(soc). SocSelector l'importe
  (comportement inchangé).
- Clients.tsx et Suppliers.tsx : pour un non-admin (RESPONSIBLE_SOC), le formulaire
  affiche un select « Société * » alimenté par useSoc().socs (mySocs : sociétés liées,
  favorite incluse) pré-rempli avec la société sélectionnée (useSoc.selectedSocId sinon
  user.socId), complété d'un bouton « + Inscrire une société » qui ouvre AddSocModal ;
  à la création, la nouvelle société est automatiquement sélectionnée dans le formulaire.
  ADMIN : select de toutes les sociétés inchangé. La liste (findAll) est filtrée selon
  la société sélectionnée (selectedSocId) et non plus user.socId seul.
- backend (isolation multi-soc obligatoire) :
  - ClientService.create/update et SupplierService.create/update vérifient
    SecurityUtils.requireSocAccess sur la société cible (et sur la soc de l'objet
    existant en update) ; un non-admin ne peut plus créer/modifier un objet pour une
    société qui n'est pas la sienne.
- tests : ajout de create_nonAdmin_otherSoc_forbidden dans ClientServiceTest et
  SupplierServiceTest + helpers authenticateAsAdmin/authenticateAsResponsible
  (UserPrincipal + SecurityContextHolder).
- mvn test : 172 tests, 1 échec pré-existant et indépendant (AuthServiceTest
  registerSoc_createsSocSubscriptionAdminAndSendsVerificationEmail, inchangé).
- frontend : lint oxlint OK, vitest 110 tests OK, tsc + vite build OK.

14/08/2026 12:38:26
1) quand on ajoute une société en la cherchant (bouton Rechercher) et qu'il y a plusieurs
résultats, on affiche une dialog 600x600 pour choisir la société.
2) quand on ajoute un client (ou fournisseur), sélection de la société associée par
recherche, liste ou ajout ; une fois sélectionnée, on met à jour le nom du client par le
nom de la société, le contact par nom+prénom du gérant, l'email par
<nom>@<domaine_soc>.com, le tel par la recherche ; bouton « Voir » pour revoir la société.
- backend : SocLookupDtos.CompanyLookup + SocLookupService exposent désormais un champ
  tel (téléphone de l'entreprise, champs telephone/tel de l'API recherche-entreprises).
- frontend :
  - AddSocModal : dialog de choix de société 600x600 (h-[600px] w-[600px], liste
    scrollable, compteur de résultats, bouton Annuler).
  - api/auth.ts : CompanyLookup.tel ajouté.
  - nouveau composant src/soc/SocPicker.tsx : sélection de la société associée via
    - recherche (nom/SIRET -> 1 résultat auto-sélectionné, plusieurs -> dialog 600x600 ;
      si la société existe déjà dans mes socs elle est sélectionnée, sinon addSoc la crée
      et la sélectionne) ;
    - liste de mes sociétés (useSoc.socs, select) ;
    - ajout (AddSocModal réutilisé, onCreated sélectionne la nouvelle société).
    Un bouton « Voir » ouvre un récapitulatif (nom, SIRET, gérant, tel, email dérivé,
    site, adresse). Une pastille indique la société retenue + l'email dérivé.
  - Clients.tsx / Suppliers.tsx : section « Société associée * » -> SocPicker
    (admin : select global + société parent inchangés). applyCompany pré-remplit le
    formulaire : name = nom société, contactName = gérant, contactEmail =
    <slug-nom>@<domaine-web>.com, contactPhone = tel trouvé par la recherche.
- tests : lint oxlint OK, vitest 110 tests OK, tsc + vite build OK.
- mvn test : 172 tests, 1 échec pré-existant et indépendant (AuthServiceTest
  registerSoc_createsSocSubscriptionAdminAndSendsVerificationEmail, inchangé).

14/08/2026 14:01:42
corriger erreur console front (contexte : click nouveau client -> taper sfr -> rechercher ->
la dialog était fermée ; console : <form> cannot be a descendant of <form>).
- cause : le formulaire de recherche du SocPicker (<form onSubmit>) était imbriqué dans le
  <form> du modal client (HTML invalide) : le submit remontait au formulaire parent et
  fermait la dialog.
- fix :
  - SocPicker : suppression du <form> interne -> <div> ; le bouton « Rechercher » devient
    type="button" onClick={() => void search()} ; la recherche se déclenche aussi sur Entrée
    dans le champ (onKeyDown, preventDefault) ; search() ne prend plus d'événement.
  - components/data.tsx : Modal rendu via createPortal(document.body) au lieu d'être imbriqué
    dans l'arbre DOM -> tout contenu de modal (dont le <form id="add-soc-form"> d'AddSocModal
    ouvert depuis le SocPicker) ne peut plus se retrouver imbriqué dans un formulaire parent.
- vérifs : lint oxlint OK (plus d'import FormEvent), vitest 110 tests OK, tsc + vite build OK.

14/08/2026 14:11:16
corriger erreur backend run.log : ajout d'une société trouvée par la recherche (SocPicker)
échouait avec "Value too long for column description CHARACTER VARYING(500)" (JSON de 4680

2026-08-18 22:23:59
lorsque je rajoute un nouveau client
id_soc = l'id de la societe que je recherche
id_soc_parent : id de la societe selected : la societe dans laquelle on travaille.
go

2026-08-18 22:23:59
dans la page Clients
lorsque je click sur modifier
je dois aussi modifier le champ societe parent (societe)
go
caractères, ex. ALTICE siren 351094883).
- cause : dans SocPicker.settleCompany, l'auto-création de la société envoyait le JSON complet
  du lookup (company.infosWeb) dans le champ description (VARCHAR(500)) au lieu du champ
  infosWeb (colonne TEXT infos_web, sans limite).
- fix : src/soc/SocPicker.tsx -> description: company.infosWeb remplacé par
  infosWeb: company.infosWeb dans l'appel addSoc (AddSocModal utilisait déjà le bon champ).
- vérifs : lint oxlint OK, vitest 110 tests OK, tsc + vite build OK.

14/08/2026 14:38:14
quand je veux ajouter un client, je cherche une société :
- soit via la liste,
- soit via la page « Ajouter une société »,
et cette recherche doit être au début de la page client.
si je sélectionne via la liste, je mets à jour les autres champs (nom du client, ...).
si j'ajoute une société, je dois l'ajouter à la liste, la sélectionner dans la liste et
mettre à jour les autres champs.
- frontend :
  - SocPicker (src/soc/SocPicker.tsx) : nouvelle fonction settleSoc(socId) — à la
    sélection d'une société depuis la liste (select) ou juste après l'ajout via
    AddSocModal, elle appelle GET /socs/{id} (socsApi.getById), construit un
    CompanyLookup à partir du détail de la société (lookupFromSoc : name, infosWeb,
    siret, codeNaf, gerant, categorieEntreprise, website, adresse) puis appelle
    onCompany(lookup) pour pré-remplir les champs du client/fournisseur (nom = nom de
    la société, contact = gérant, email dérivé du site web, ...). En cas d'échec de
    getById, onCompany(null) est appelé (aucune sur-écriture).
  - AddSocModal onCreated : après la création, la nouvelle société est ajoutée à la
    liste (addSoc de SocContext la recharge via mySocs), sélectionnée et settleSoc
    met à jour les champs avec les infos de la société créée.
  - Clients.tsx et Suppliers.tsx : le bloc « Société associée * » (SocPicker) est
    désormais placé en tête du formulaire (au-dessus du nom du client/fournisseur),
    comme demandé « au début de la page client ».
- vérifs : lint oxlint OK (warnings docs/ pré-existants), vitest 110 tests OK,
  tsc + vite build OK.

14/08/2026 14:47:07
corriger erreur backend run.log : suppression d'un client échouait avec
DataIntegrityViolationException SQLState 23503 « violation de contrainte
fk_project_client » (delete from client where id=?).
- cause : ClientService.delete() faisait clientRepository.deleteById(id) sans gérer
  les projets liés : la colonne project.client_id est NOT NULL et référencée par la FK
  fk_project_client -> la suppression d'un client ayant des projets violait l'intégrité
  référentielle et remontait en « Erreur interne du serveur » (500) au lieu d'une
  suppression propre.
- fix : purge des dépendances avant suppression (même convention que
  ConsultantService.delete -> purge) :
  - ProjectRepository : ajout de deleteByClientId(Long) (JPQL @Modifying
    « delete from Project p where p.client.id = :clientId »).
  - ClientService.delete() : appelle d'abord projectRepository.deleteByClientId(id)
    puis clientRepository.deleteById(id). Transactionnel : les deux opérations sont
    atomiques.
- vérifs : mvn test -Dtest=ClientServiceTest -> 12 tests OK (dont 2 delete :
  delete_deletesById vérifie l'ordre purge puis delete, delete_purgesLinkedProjectsThenDeletesClient) ;
  mvn test complet -> 173 tests, 1 échec pré-existant et indépendant
  (AuthServiceTest.registerSoc_createsSocSubscriptionAdminAndSendsVerificationEmail, inchangé).

14/08/2026 23:15:30
réorganiser plus simplement, rôle admin + responsable_soc : ajouter le menu Sociétés :
voir toutes les sociétés de l'application ; rechercher par nom, siret, gerant ;
ajouter ; modifier les sociétés qu'on a ajoutées (historisation user + date) ;
supprimer les sociétés qu'on a ajoutées et non utilisées par des consultants.
- backend :
  - SocService.findAllAccessible() : retourne désormais toutes les sociétés pour
    ADMIN et RESPONSIBLE_SOC (interrogations du dashboard/listes inchangées pour les autres).
  - SocService.delete() : blocage si la société est encore utilisée par des consultants
    (consultantRepository.findBySocIdAndActiveTrue non vide) -> ApiException.conflict
    « Impossible de supprimer : cette société est utilisée par des consultants ».
  - update/create : l'historisation par user + dateMaj via HistoSoc était déjà en place
    (audit() appelé dans create/update).
- frontend :
  - api/socs.ts : ajout de l'interface SocRequest + socsApi.create (POST /api/socs).
  - api/types.ts : AddSocPayload.mine?, SocDto.dateCreation/dateFermeture.
  - SocAdmin.tsx : réécrit en page CRUD des sociétés (liste, recherche nom/SIRET/gérant,
    « Géré par moi », modif avec modal complète, suppression avec vérif dépendances et
    confirmation « Tout supprimer ») ; l'ancienne page abonnements/paiements est retirée.
  - AddSocModal : prop submit (création ADMIN via socsApi.create, RESPONSIBLE_SOC via addSoc).
- écran : menu « Sociétés » disponible pour ADMIN et RESPONSIBLE_SOC (MainLayout).
- vérifs : tsc + vite build OK, vitest 110 tests OK, mvn SocServiceTest 14 tests OK,
  mvn complet 173 tests (1 échec pré-existant et indépendant, voir ci-dessus).

14/08/2026 23:15:30
pour ajouter un client ou fournisseur, on ne fait que sélectionner la société.
Puis mettre à jour les autres champs.
- Clients.tsx et Suppliers.tsx : le bloc « Société associée * » (SocPicker avec
  recherche + ajout de société) est remplacé par un simple Select des sociétés
  (Mes sociétés pour non-admin, toutes les sociétés pour ADMIN).
- à la sélection, les champs nom/contact sont auto-remplis depuis les données de la
  société (socToCompanyLookup -> applyCompany), puis l'utilisateur met à jour le reste.
- SocPicker.tsx retiré (plus utilisé) ; nouveau helper src/soc/socLookup.ts.
- openCreate : pour RESPONSIBLE_SOC, présélection + auto-remplissage depuis la société
  courante (selectedSocId / user.socId).
- vérifs : tsc + vite build OK, vitest 110 tests OK.

14/08/2026 23:15:30
lorsqu'on rajoute une société, on doit déclarer si c'est la sienne ou pas.
D'où deux menus : « Mes sociétés » et « Toutes les sociétés ».
- backend : AddSocRequest.mine (Boolean, défaut true) ; AuthService.addSoc() : si
  mine=false, la société est créée (Soc + subscription + seeds) mais PAS liée au compte
  (ni user_soc, ni soc par défaut). Si mine=true (défaut), comportement inchangé.
- frontend : AddSocModal ajoute le case à cocher « C'est ma société » (visible
  uniquement sur le chemin addSoc, c.-à-d. quand submit n'est pas fourni) ; le flag
  mine est envoyé dans addSoc.
- menu : deux entrées sous « Sociétés » pour ADMIN et RESPONSIBLE_SOC :
  « Mes sociétés » (route /soc) et « Toutes les sociétés » (route /soc/toutes),
  SocAdmin prend une prop scope='mine'|'all' et filtre « Mes sociétés » par les
  sociétés liées au compte (sauf ADMIN qui voit toutes).
- vérifs : tsc + vite build OK, vitest 110 tests OK, mvn AuthServiceTest (seul échec =
  pré-existant registerSoc…), mvn complet 173 tests (1 échec pré-existant).

17/08/2026 10:30:49
régression validation par mail pour l'inscription d'une nouvelle société (1ère fois) :
le lien de validation doit confirmer le mot de passe et refaire un nouveau mot de passe
(qui peut être le même) afin de valider l'inscription de la nouvelle société et de son responsable.
- backend :
  - UserDtos.VerifyEmailRequest : ajout de currentPassword + newPassword (constructeur
    compact conservant le cas token seul).
  - AuthService.verifyEmail(token, currentPassword, newPassword) : vérifie que le mot de
    passe actuel (saisi à l'inscription) correspond (passwordEncoder.matches) sinon 400
    « Mot de passe actuel incorrect » ; met à jour le mot de passe (peut être le même),
    active le compte, mustChangePassword=false, passwordChangedAt=now, marque le token utilisé
    puis retourne un JWT (AuthResponse) comme avant.
  - AuthController : POST /api/auth/verify-email transmet currentPassword + newPassword.
- frontend :
  - api/auth.ts : authApi.verifyEmail(token, currentPassword, newPassword).
  - VerifyEmail.tsx : plus de validation auto au chargement ; formulaire « Mot de passe
    actuel » + « Nouveau mot de passe » + « Confirmer le nouveau mot de passe » qui soumet
    verifyEmail ; bloc « Renvoyer l'email » conservé ; redirection vers / après succès.
- tests : AuthServiceTest (31) — verifyEmail_success vérifie désormais le nouveau mot de
  passe, l'activation, mustChangePassword=false ; ajout de verifyEmail_wrongCurrentPassword.
- vérifs : mvn -Dtest=AuthServiceTest -> 31 tests (seul échec = pré-existant
  registerSoc_createsSocSubscriptionAdminAndSendsVerificationEmail, inchangé) ;
  frontend : oxlint OK (warnings docs/ pré-existants), vitest 110 tests OK, tsc + vite build OK.

17/08/2026 13:12:00
menu Utilisateurs (role ADMIN)
. on peut modifier chaque utilisateur 
. on peut supprimer un utilisateur (si inactif) avec sa societe 
- backend :
  - UserController : ajout PUT /api/users/{id} (ADMIN) → AuthService.updateUser ;
    DELETE /api/users/{id} refuse désormais un compte actif (« désactivez-le d'abord »)
    et supprime la société liée via SocService.deleteWithDependencies (cascade déjà
    existante, réutilisée) avant de supprimer l'utilisateur ; garde self-delete et
    compte admin conservées.
  - AuthService.updateUser : garde d'unicité email (409 si email déjà pris par un autre compte).
  - tests : SecurityIntegrationTest — adminCanDeleteRegularUser adapté (désactive le compte
    via PUT avant suppression), ajout de adminCannotDeleteActiveUser (400) et
    adminCanUpdateUser (PUT OK).
- frontend :
  - api/users.ts : usersApi.update(id, UpdateUserRequest) (PUT).
  - Users.tsx : bouton « Modifier » (modal : prénom, nom, email, téléphone, rôle, société,
    compte actif) + bouton « Supprimer » désactivé tant que le compte est actif, avec modal
    de confirmation avertissant de la suppression de la société et de ses objets liés.
- vérifs : mvn -Dtest=AuthServiceTest,SecurityIntegrationTest -> 47 tests (1 échec pré-existant
  inchangé) ; frontend : oxlint OK, vitest 110 tests OK, tsc + vite build OK.
erreur backend run.log à l'inscription d'une société : « Valeur trop longue pour la
colonne username CHARACTER VARYING(50) » (SQLState 22001) — username de 56 caractères.
- cause : le champ « Gérant » de la page d'inscription contenait plusieurs personnes
  séparées par une virgule (« Cécile Paulette Poinin-Auraye, Mickaël Flavien Poinin-Auraye »).
  Register.tsx::updateAdministrator concaténait `<1re lettre prénom> + nom…` sans
  supprimer la virgule ni les caractères accentués -> username
  « icecilepaulettepoinin-auraye,mickaelflavienpoinin-auraye » (56) > VARCHAR(50),
  et AuthService.registerSoc (AuthService.java:142) ratait l'insertion de user_account.
- fix : sanitizeUsername() dans Register.tsx — normalise NFD (retire les accents),
  garde uniquement [a-z0-9_.-], tronque à 50 caractères max avant setUsername.
- vérifs : oxlint OK, vitest 110 tests OK, tsc + vite build OK.

17/08/2026 14:36:11
augmenter la taille de font de l'info de la date du dernier deploiement x2
- MainLayout.tsx : la date de déploiement (ligne « Déploiement : {__BUILD_DATE__} ») passe de
  text-[10px] à text-[20px] (x2).
- vérifs : oxlint OK, vitest 110 tests OK, tsc + vite build OK ; déployé (build docs/ commit 00ba1bd).

17/08/2026 14:36:11
le lien de validation par mail pour inscrire une nouvelle société n'affiche pas le bon
formulaire sur github pages (en local c'est bon, mais pas sur github pages !!)
- cause racine : sur GitHub Pages, un lien direct type .../auth/verify-email?token= n'existe pas
  en tant que fichier -> le site sert docs/404.html qui stocke l'URL dans sessionStorage
  (clé soc360.redirect) puis redirige vers la racine. Le bundle se charge alors, et App.tsx
  créait le router (createBrowserRouter) À L'ÉVALUATION du module, donc AVANT que le corps de
  main.tsx ne restaure l'URL via history.replaceState. Or replaceState ne déclenche pas
  d'événement popstate -> le router restait sur la route / (protégée) -> redirection vers /login.
  En local, Vite sert index.html sur toutes les routes, donc aucune restauration nécessaire.
- fix : la restauration de l'URL (lecture + suppression de soc360.redirect + history.replaceState)
  est déplacée de src/main.tsx vers un <script> inline dans index.html, exécuté AVANT le
  chargement du bundle -> createBrowserRouter capture alors la bonne URL (/auth/verify-email).
- fichiers : index.html (script inline ajouté), src/main.tsx (bloc de restauration supprimé).
- vérifs : oxlint OK, vitest 110 tests OK, tsc + vite build OK ; déployé (build docs/ commit
  00ba1bd) ; en ligne : index.html contient le script inline et 404.html est servi sur
  .../auth/verify-email?token= (curl vérifié).

17/08/2026 15:35:00
l'url de validation d'inscription d'une nouvelle société et nouveau resp soc sur github pages
ne donne pas la bonne page de validation :
url : https://mzamouneisi.github.io/soc360-front-react//auth/verify-email?token=...
contenu de la page : « 404 / Page introuvable » (composant React NotFound)
- cause racine restante : le lien de l'email contient un DOUBLE SLASH
  (https://mzamouneisi.github.io/soc360-front-react//auth/verify-email?token=...).
  La valeur FRONTEND_URL (env.server.sh:86) finit par un slash final
  (« …/soc360-front-react/ ») et AuthService concaténait frontendUrl + "/auth/verify-email?token="
  -> //auth. Après restauration de l'URL, react-router stripBasename renvoie « //auth/verify-email »
  (il ne retire qu'un seul slash du basename), qui ne matche pas la route /auth/verify-email
  -> le router tombe sur la route catch-all '*' -> NotFound.
  (Le fix précédent 14:36:11 — restauration AVANT création du router — était nécessaire mais
  insuffisant : il restaurait une URL à double slash qui ne matche toujours pas la route.)
- fix frontend (soc360-front-react) :
  - index.html : le <script> inline de restauration normalise maintenant le pathname
    (url.pathname = url.pathname.replace(/\/{2,}/g, '/')) avant history.replaceState
    -> //auth/verify-email devient /auth/verify-email. Résout tous les liens déjà envoyés.
  - vérifs : oxlint OK, vitest 110 tests OK, tsc + vite build OK ; déployé (build docs/
    commit 6d85711, bundle index-Bwo8k3cT.js) ; testé en Chrome headless sur l'URL réelle
    à double slash -> le formulaire VerifyEmail (« Mot de passe actuel » / « Nouveau mot de
    passe ») s'affiche, plus de « Page introuvable ».
- fix backend (soc360-back-java) :
  - AuthService.java : nouveau helper baseFrontendUrl() qui retire les slashes finaux
    (frontendUrl.replaceAll("/+$", "")) ; utilisé pour construire les liens
    /auth/verify-email?token= et /auth/reset-password?token= -> plus aucun lien à double slash
    dans les futurs emails.
  - vérifs : mvn compile OK ; mvn -Dtest=AuthServiceTest -> 31 tests, seul échec =
    pré-existant registerSoc_createsSocSubscriptionAdminAndSendsVerificationEmail.
  - NON DÉPLOYÉ sur Azure (redéploiement backend à faire).

17/08/2026 16:23:30
menu Activités & tarifs :
le btn Gérer les types : doit afficher la liste des types d'activitées de la société existants
et la possibilité de rajouter, de rechercher, de supprimer, modifier
- backend :
  - ActivityTypeService : findAllIncludingInactive(socId) (tous les types, actifs et inactifs,
    ordre par libellé) ; delete(id) — vérifie assertCanManage(soc) puis bloque la suppression si
    le type est utilisé par des activités (ApiException.conflict) pour respecter la FK
    fk_activity_type ; ActivityRepository : findByTypeId(Long).
  - ActivityTypeController : GET /api/activity-types accepte désormais le paramètre `all`
    (true = inclure les types inactifs) ; ajout DELETE /api/activity-types/{id}
    (@PreAuthorize ADMIN ou RESPONSIBLE_SOC).
- frontend (Activities.tsx) :
  - la modale « Gérer les types » affiche tous les types (actifs + inactifs) via
    activityTypesApi.findAll(socId, { all: true }).
  - ajout d'un champ de recherche « Rechercher un type… » (filtre sur libellé/code).
  - par type : boutons Modifier, Désactiver (ou Réactiver si inactif) et Supprimer
    (suppression définitive avec confirmation) ; après chaque action, rechargement des deux
    listes (types actifs pour le formulaire d'activité + liste complète de la modale).
  - api/activities.ts : activityTypesApi.findAll(socId, opts) + activityTypesApi.delete(id).

lorsque je veux ajouter une nouvelle activité, j'ai toujours la liste des types d'activités vide !!
donc je suis bloqué !
- cause racine : AuthService.registerSoc() (inscription d'une nouvelle société via la page
  publique) créait la société + l'admin mais n'appelait jamais
  defaultActivityTypesSeeder.seedDefaultsFor(soc) ni defaultClientProjectSeeder.seedDefaultsFor(soc)
  (seuls addSoc et SocService.create le faisaient) -> la société inscrite ne possédait aucun type
  d'activité -> le sélecteur « Type * » de « + Nouvelle activité » restait vide.
- fix :
  - AuthService.registerSoc() : ajout de defaultActivityTypesSeeder.seedDefaultsFor(soc) et
    defaultClientProjectSeeder.seedDefaultsFor(soc) juste après socRepository.save(soc).
  - migration V17__activity_type_defaults_backfill.sql : backfill en SQL des 6 types par défaut
    (DEV, MAINT, SUPPORT, MEETING, MANAGEMENT, REST) + une activité par défaut (tarif 0) pour
    les sociétés existantes qui n'ont aucun type d'activité (mêmes valeurs que
    DefaultActivityTypesSeeder).
- tests : AuthServiceTest.registerSoc_createsSocSubscriptionAdminAndSendsVerificationEmail
  vérifie désormais aussi defaultActivityTypesSeeder.seedDefaultsFor (le test pré-existant
  « registerSoc n'appelle pas seedDefaultsFor » est corrigé) ; ActivityTypeServiceTest :
  findAllIncludingInactive_returnsAllTypesForSoc, delete_deletesTypeNotUsedByActivities,
  delete_typeUsedByActivities_throwsConflict, delete_otherSoc_forbidden, delete_notFound_throws.
- vérifs : mvn test -> 181 tests, seul échec = adminCannotDeleteActiveUser (échec d'ordre
  d'isolation PRÉ-EXISTANT, présent aussi sur le code d'origine : 2 échecs avant ce correctif
  (registerSoc + adminCannotDeleteActiveUser) -> 1 après) ; frontend : oxlint OK (warnings
  docs/ pré-existants), vitest 110 tests OK, tsc + vite build OK.

17/08/2026 17:07:00
faire un menu a part : Type d'Activités :
on affiche la liste des Type d'Activités de la societe selected
on peut ajouter, modifier, supprimer.

lorsqu'on ajoute une société, on s'assure qu'elle a au moins les types d'activités suivants :
MISSION, INTER_CONTRAT, CONGE_RTT, CONGE_PAYE, CONGE_NON_PAYE, FORMATION, DEV_INTERNE,
COMMERCIAL, SUPPORT
- backend :
  - DefaultActivityTypesSeeder : les types par défaut d'une nouvelle société sont désormais
    les 9 types requis (MISSION, INTER_CONTRAT, CONGE_RTT, CONGE_PAYE, CONGE_NON_PAYE,
    FORMATION, DEV_INTERNE, COMMERCIAL, SUPPORT) au lieu de DEV/MAINT/SUPPORT/MEETING/
    MANAGEMENT/REST ; une activité par défaut (tarif 0) reste créée par type.
  - migration V18__activity_type_company_defaults.sql : garantit que chaque société existante
    possède au moins ces 9 types (INSERT des codes manquants par société, sans toucher aux
    autres) + une activité par défaut (tarif 0) pour chaque type créé par le backfill.
  - SecurityIntegrationTest.creatingSoc_seedsDefaultActivityTypes : attendu passé de 6 à 9
    (taille des types seedés par la création d'une société).
- frontend :
  - nouvelle page dédiée src/pages/ActivityTypes.tsx : liste des types d'activités de la
    société sélectionnée (selectedSocId, sinon user.socId), actifs et inactifs ; recherche ;
    bouton « + Nouveau type » et actions Modifier / Désactiver-Réactiver / Supprimer dans un
    tableau, avec modal de création/édition (code, libellé FR, libellé EN, couleur).
  - route /types-activites (App.tsx, wrap NotConsultantRoute) + entrée de menu « Types
    d'activités » sous Gestion (MainLayout, visible pour tous sauf CONSULTANT).
  - Activities.tsx : la modale « Gérer les types » et son bouton sont retirés (la gestion est
    désormais dans le menu dédié) ; l'import de ActivityTypeRequest et le state inutilisés
    supprimés.
- vérifs : mvn test -> 181 tests, seul échec = adminCannotDeleteActiveUser PRÉ-EXISTANT
  (échec d'ordre d'isolation, présent aussi sur le code d'origine, indépendant de ce correctif) ;
  frontend : oxlint OK (warnings docs/ pré-existants), vitest 110 tests OK, tsc + vite build OK.

2026-08-17 17:17:20
err_back : run.log du backend : le démarrage échoue en compilation
(4 erreurs javac, « COMPILATION ERROR »), le backend ne démarre pas.
- erreur : /…/src/main/java/com/soc360/activity/Activity.java:[6,22] package com.soc360.esn
  does not exist ; lignes 41/18 « cannot find symbol: class Soc ».
- cause : Activity.java importait `com.soc360.esn.Soc` alors que le package `esn` a été
  renommé en `soc` depuis longtemps (tous les autres fichiers du package activity importent
  `com.soc360.soc.Soc` : ActivityService, ActivityType, ActivityTypeService,
  DefaultActivityTypesSeeder). Ce mauvais import était resté dans le working tree (modif
  locale non committée) et cassait la compilation.
- fix : src/main/java/com/soc360/activity/Activity.java : `import com.soc360.esn.Soc;` ->
  `import com.soc360.soc.Soc;`.
- vérifs : mvn -q compile OK (0 erreur) ; mvn test complet -> 181 tests, seul échec =
  adminCannotDeleteActiveUser PRÉ-EXISTANT (échec d'ordre d'isolation, indépendant de ce fix,
  déjà documenté) ; frontend non concerné (aucun changement).

2026-08-17 17:36:00
une activité s'appuie sur :
- un type d'activité
- un projet chez un client
- une description de cette activité
- conception : une activité est désormais rattachée à un type (déjà en place), à un
  projet de la société (nouveau) et conserve sa description (+ nom, prix, devise, actif).
- backend :
  - migration V19__activity_project.sql : `ALTER TABLE activity ADD COLUMN project_id BIGINT`
    + contrainte FK -> project(id) (colonne nullable : les activités existantes/par défaut
    n'ont pas de projet).
  - Activity.java : ajout `@ManyToOne Project project` (@JoinColumn name = "project_id").
  - ActivityDtos : nouveau record `ProjectRef(id, name, clientName)` ; `ActivityDto` expose
    `project` ; `ActivityRequest` accepte `projectId`.
  - ActivityService (apply) : si `projectId` fourni, résout le projet et vérifie qu'il
    appartient à la même société que l'activité (isolation multi-société) ; toDto renvoie le
    ref projet avec le nom du client.
  - Liaison des activités par défaut au projet interne « Activités internes » : les seeders
    créent désormais d'abord le client+projet interne puis les types/activités
    (DataSeeder, SocService.create, AuthService.registerSoc + registerDirect). Sinon les
    activités par défaut n'auraient pas de projet.
- frontend :
  - src/api/types.ts : ActivityDto.project {id, name, clientName} ; ActivityRequest.projectId.
  - src/pages/Activities.tsx : chargement des projets de la société (projectsApi) ; champ
    « Projet * » (select avec « nom — client ») dans le formulaire ; colonne « Projet »
    (nom + client) dans le tableau ; validation « Nom, type et projet sont obligatoires » ;
    pré-remplissage à l'édition.
- vérifs : mvn test -> 181 tests, seul échec = adminCannotDeleteActiveUser PRÉ-EXISTANT
  (échec d'ordre d'isolation, indépendant) ; frontend : oxlint OK (warnings docs/
  pré-existants), vitest 110 tests OK, tsc + vite build OK.

2026-08-17 18:32:47
err_back : run.log du backend : le démarrage échoue sur le checksum de la migration V20
- erreur : « Migration checksum mismatch for migration version 20 — Applied to database :
  2101231905, Resolved locally : -1063061325 » -> le backend ne démarre pas.
- cause : V20__remove_default_activities.sql avait été MODIFIÉE (ajout d'un garde-fou
  « AND NOT EXISTS (SELECT 1 FROM cra_day_activity ...) ») APRÈS son application sur la base
  -> Flyway calcule le checksum (CRC32 par ligne UTF-8, sans les séparateurs de fin de ligne,
  BOM filtré) et détecte une divergence entre le fichier et ce qui a été appliqué.
- règle appliquée : ne jamais éditer une migration déjà appliquée ; toute évolution du schéma
  passe par une NOUVELLE migration (la suivante est V22).
- fix : V20__remove_default_activities.sql restaurée à son contenu original (sans le
  garde-fou) -> checksum recalculé = 2101231905, conforme à la base.
- vérifs : run.log -> démarrage OK à 17:52:23 (« Successfully validated 21 migrations »,
  « Current version of schema public: 21 ») ; mvn test -> 181 tests, seul échec =
  adminCannotDeleteActiveUser PRÉ-EXISTANT ; frontend non concerné.

2026-08-17 18:32:47
menu CRA :
on a le choix de 2 types d'affichages (en deux onglets) :
- onglet calendar (par defaut)
- onglet ligne (comme actuellement)

on doit pouvoir :
- remplir pardefaut l'activité courante
- supprimer tous evenements ajoutés d'un coup
- modifier / supprimer un evenement

on travaille en jours : 1 , 0.5
max par evenement = 1

un cra valide doit avoir tous les evenements à nb_jour_total = 1

un btn pour envoyer le cra à valider par son manager
il faut d'abord que le cra soit valide avant de l'envoyer.

le manager peut valider ou rejeter le cra avec un commentaire.
- backend :
  - migration V22__cra_day_activity_days.sql : ajout de la colonne `days NUMERIC(6,2) NOT NULL DEFAULT 1`
    sur `cra_day_activity` (le CRA travaille désormais en jours).
  - CraDayActivity : nouveau champ `days` (BigDecimal) ; CraDtos :
    CraDayActivityDto + CraDayActivityRequest exposent/acceptent `days`.
  - CraService.save : chaque événement doit représenter 0,5 ou 1 jour (max 1 jour par événement),
    sinon 400 (« Un événement doit représenter 0,5 ou 1 jour »).
  - CraService.submit : le CRA doit être complet — chaque jour WORKED doit totaliser exactement
    1 jour, sinon 400 (« Le CRA est incomplet : le jour <date> doit totaliser 1 jour »).
  - toDto renvoie `days`.
- frontend (CraDetail.tsx) :
  - deux onglets : « Calendrier » (défaut) et « Ligne » (vue tableau comme avant).
  - onglet Calendrier : grille mensuelle (cases vides en tête, en-têtes Lun..Dim) ; chaque jour
    travaillé affiche ses événements (pastille couleur + nom + ×jours) et un badge du total ;
    bouton + pour ouvrir la modale d'événements du jour ; jours non travaillés grisés avec leur
    type ; pastille rouge « à compléter » quand le total du jour ≠ 1.
  - modale d'événements du jour : choix type de jour, liste des événements (activité + jours
    0,5/1 + commentaire), ajout / modification / suppression d'un événement, total du jour.
  - bouton « Supprimer tous les événements » (avec confirmation) — vide tous les jours d'un coup.
  - événement ajouté pré-rempli avec l'activité courante (active, dont la période couvre
    aujourd'hui, sinon première activité active) — « remplir par défaut l'activité courante ».
  - unité jours : select 0,5 j / 1 j (max 1 jour par événement).
  - validité : le CRA est valide si chaque jour travaillé totalise exactement 1 jour ; sinon
    bandeau rouge listant le nombre de jours incomplets ; le bouton « Envoyer pour validation »
    est désactivé tant que le CRA n'est pas valide (et le backend rejette aussi la soumission).
  - validation/rejet par le manager avec commentaire déjà en place, inchangée.
- tests :
  - CraServiceTest : submitAndValidate_updatesStatus remplit désormais tous les jours WORKED
    (1 journée d'activité par jour) avant de soumettre ; nouveau submit_rejectsIncompleteCra
    (soumission d'un CRA vide rejetée 400).
- vérifs : mvn test -> 182 tests, seul échec = adminCannotDeleteActiveUser PRÉ-EXISTANT
  (échec d'ordre d'isolation, indépendant) ; frontend : oxlint OK (warnings docs/ pré-existants),
  vitest 110 tests OK, tsc + vite build OK.

2026-08-17 17:42:00
les activités ne doivent pas exister par défaut
- les activités ne sont plus seedées par défaut : seuls les types d'activités sont créés.
- backend :
  - DefaultActivityTypesSeeder : ne crée plus d'activité par type (suppression du bloc de
    création d'activity + de la dépendance ProjectRepository). Ne seed plus que les 9 types.
  - migration V20__remove_default_activities.sql : supprime les activités par défaut déjà
    créées (ancien seeder + backfill V18 : nom = label_fr du type, prix 0, sans description),
    sauf celles déjà référencées par un CRA (évite un conflit de FK).
  - ordre des seeders inchangé en pratique : DataSeeder/SocService/AuthService seedent le
    client+projet interne puis les types ; sans activité par défaut, l'ordre n'a plus d'impact.
- vérifs : mvn test -> 181 tests, seul échec = adminCannotDeleteActiveUser PRÉ-EXISTANT ;
  frontend inchangé.

2026-08-17 17:42:00
une activité a une date début et une date fin. fin n'est pas obligatoire.
  si fin == null, alors pas de fin
- backend :
  - migration V21__activity_dates.sql : `ALTER TABLE activity ADD COLUMN start_date DATE;
    ADD COLUMN end_date DATE;` (toutes deux NULLables).
  - Activity.java : champs LocalDate startDate / endDate.
  - ActivityDtos : ActivityDto et ActivityRequest exposent startDate/endDate (nullables).
  - ActivityService.apply : valide que endDate >= startDate quand les deux sont fournies
    (« La date de fin ne peut pas précéder la date de début ») ; toDto renvoie les dates.
- frontend :
  - src/api/types.ts : ActivityDto.startDate/endDate, ActivityRequest.startDate/endDate.
  - src/pages/Activities.tsx : champs date de début + date de fin (optionnelle) dans le
    formulaire ; validation endDate < startDate ; colonne « Période » (début → fin, « → »
    ouvert si pas de fin) dans le tableau.
- vérifs : mvn test -> 181 tests, seul échec = adminCannotDeleteActiveUser PRÉ-EXISTANT ;
  frontend : oxlint OK (warnings pré-existants), vitest 110 tests OK, tsc + vite build OK.

2026-08-17 22:05:00
modifier activité et ses formulaires :
un manager ajoute une activité à un consultant :
donc on doit avoir consultant_id dans la table activité
- backend :
  - migration V23__activity_consultant.sql : `ALTER TABLE activity ADD COLUMN consultant_id BIGINT`
    + contrainte FK -> consultant(id) (colonne nullable : les activités générales n'ont pas de consultant).
  - Activity.java : ajout `@ManyToOne Consultant consultant` (@JoinColumn name = "consultant_id").
  - ActivityDtos : nouveau record `ConsultantRef(id, firstName, lastName)` ; `ActivityDto` expose
    `consultant` ; `ActivityRequest` accepte `consultantId`.
  - ActivityService (apply) : si `consultantId` fourni, résout le consultant et vérifie qu'il
    appartient à la même société que l'activité (isolation multi-société) ; toDto renvoie le ref.
  - ActivityService.findAll(socId, typeId, consultantId) : accepte un filtre par consultantId.
  - ActivityController : paramètre optionnel `consultantId` sur GET /api/activities ; les POST /
    PUT / DELETE sont désormais accessibles aussi au rôle MANAGER (« un manager ajoute une activité »).
  - ActivityRepository : findByConsultantId(Long).
- frontend :
  - src/api/types.ts : ActivityDto.consultant {id, firstName, lastName} ;
    ActivityRequest.consultantId (nullable).
  - src/api/activities.ts : params {socId, typeId, consultantId}.
  - src/pages/Activities.tsx : canEdit élargi à MANAGER ; chargement des consultants de la
    société (consultantsApi.summaries) ; champ « Consultant » (select « Aucun »/consultants) dans
    le formulaire ; colonne « Consultant » (prénom nom) dans le tableau ; pré-remplissage à
    l'édition et envoi de consultantId dans le payload.
- tests : ConsultantServiceTest inchangé (16 OK) ;

2026-08-17 22:05:00
chaque consultant doit avoir automatiquement un certain nombre d'activites :
'INTER_CONTRAT', 'CONGE_RTT', 'CONGE_PAYE', 'CONGE_NON_PAYE', 'FORMATION'
avec projet : proj_interne
client : la societe mere
- backend :
  - ActivityService : ensureDefaultActivitiesForConsultant(Consultant) — crée, pour chaque type
    requis de la société (codes INTER_CONTRAT, CONGE_RTT, CONGE_PAYE, CONGE_NON_PAYE, FORMATION)
    non encore affecté à ce consultant, une activité (nom = labelFr, prix 0, EUR, active) rattachée
    au projet interne de la société (proj_interne, sans description).
  - ActivityTypeRepository : findBySocIdAndCodeIn(socId, codes) ; ActivityRepository :
    existsByConsultantIdAndTypeId(consultantId, typeId).
  - ConsultantService.create : après le save, appel de ensureDefaultActivitiesForConsultant
    (couvre aussi l'import CSV batch qui passe par create).
  - migration V24__consultant_default_activities.sql : backfill pour les consultants existants
    (INSERT par consultant × type requis manquant, créé_at/updated_at = CURRENT_TIMESTAMP).
  - migration V25__default_activities_internal_project.sql : rattache les activités par défaut
    (V24) au projet interne (proj_interne ou « Activités internes ») quand elles n'en ont pas.
  - DefaultClientProjectSeeder : le projet interne d'une nouvelle société est maintenant nommé
    « proj_interne » (compatibilité conservée avec l'ancien nom « Activités internes »).
  - SecurityIntegrationTest : valeur attendue « Activités internes » -> « proj_interne ».
  - DataSeeder : après seed des types, ensureDefaultActivitiesForConsultant est appelé pour
    chaque consultant actif de la société de démo.
- vérifs : mvn test -> 182 tests, seul échec = adminCannotDeleteActiveUser PRÉ-EXISTANT
  (échec d'ordre d'isolation, indépendant) ; run_dev.sh redémarrée OK (migrations V23→V25
  validées/appliquées) ; frontend : oxlint OK (warnings docs/ pré-existants), vitest 110 tests OK,
  tsc + vite build OK.

2026-08-17 22:30:00
lorsqu'on ouvre un cra (new ou old), le consultant peut remplir d'un coup tous les jours du mois
(sauf weekend, jours feries, jours de congés, jours deja rensegné)
par l'activité qui lui est associée sur ce mois.
- frontend (CraDetail.tsx) :
  - résolution de l'activité du mois (monthActivity) : parmi les activités de la liste, on
    privilégie l'activité liée au consultant du CRA (consultant.id == cra.consultantId) qui couvre
    le mois (active + startDate <= fin du mois + endDate >= début du mois), sinon la première
    activité active du consultant, sinon toute activité active couvrant le mois, sinon
    currentActivity (activité courante existante).
  - bouton « Remplir tout le mois » (visible quand le CRA est éditable, à côté de « Supprimer tous
    les événements ») : pour chaque jour WORKED sans événement, ajoute un événement de 1 jour avec
    l'activité du mois (monthActivity) ; un message d'erreur s'affiche si aucune activité n'est
    disponible ou si tous les jours travaillés sont déjà renseignés.
  - les jours WEEKEND / PUBLIC_HOLIDAY / LEAVE / SICK_LEAVE / OTHER et les jours déjà remplis
    (activities.length > 0) sont laissés intacts.
- vérifs : oxlint OK (warnings docs/ pré-existants), vitest 110 tests OK, tsc + vite build OK.

2026-08-17 22:30:00
augmenter x2 la taille de font des caracteres des jours du calendrier et ses cellules d'un cra ouvert
- ANNULEE : à la demande de l'utilisateur, tout le calendrier du CRA reste à la taille normale
  (12 px : numéro du jour text-xs, activités text-xs, libellés et totaux text-[11px]).
  Une première passe appliquait ×2 (jour 24px, chip 22px, libellés 18px, puis tout à 24px), puis
  elle a été entièrement annulée et remise à 12 px. Aucun changement résiduel n'est conservé.

2026-08-17 23:11:00
mettre la taille de font pour toute l'application en paramètre 14px :
il faut un menu paramètres pour la modifier par utilisateur (défaut 14px)
- backend :
  - migration V26__user_font_size.sql : `ALTER TABLE user_account ADD COLUMN font_size INT NOT NULL DEFAULT 14`.
  - UserAccount : champ `int fontSize = 14` (@Column nullable=false).
  - UserDtos : UserDto expose `fontSize` (int) ; nouveau record FontSizeRequest(int fontSize).
  - AuthService : updateFontSize(userId, fontSize) — borne 10..32 px (400 sinon), sauvegarde et
    retourne le UserDto ; toDto renvoie fontSize.
  - AuthController : PUT /api/auth/me/font-size (tout utilisateur connecté).
- frontend :
  - src/api/types.ts : UserDto.fontSize (number).
  - src/api/auth.ts : authApi.updateFontSize(fontSize) -> PUT /auth/me/font-size.
  - src/auth/AuthContext.tsx : effet qui applique la taille au niveau racine
    `document.documentElement.style.fontSize = '<fontSize>px'` (défaut 14 si absent) -> toute
    l'application (Tailwind rem-based) est redimensionnée globalement dès la connexion et après
    chaque changement.
  - nouvelle page src/pages/Settings.tsx : menu « Paramètres » (route /parametres, entrée sous
    « Espace » dans MainLayout) avec un select 10..24 px, bouton Enregistrer, aperçu en direct et
    messages d'erreur/succès ; refreshMe() après sauvegarde.
- vérifs : mvn test -> 182 tests, seul échec = adminCannotDeleteActiveUser PRÉ-EXISTANT
  (échec d'ordre d'isolation, indépendant) ; run_dev.sh redémarrée OK (V26 validée/appliquée) ;
  frontend : oxlint OK (warnings docs/ pré-existants), vitest 110 tests OK, tsc + vite build OK.

2026-08-18 11:04:26
dans la page CRA, ajouter au btn 'Remplir tout le mois' une liste contenant les activités
correspondant au mois afin de choisir la bonne activité à remplir avec
- frontend (src/pages/CraDetail.tsx) :
  - le clic sur « Remplir tout le mois » n'applique plus directement une activité unique :
    il ouvre désormais une modale (FillMonthModal) listant les activités correspondant au mois.
  - monthActivities : remplace l'ancien memo monthActivity (qui ne renvoyait qu'une seule
    activité). Il retourne la liste des activités actives dont la période chevauche le mois du CRA
    (active + startDate <= fin du mois + endDate >= début du mois), en privilégiant celles liées au
    consultant du CRA (consultant.id == cra.consultantId) ; sinon toutes les activités du mois.
  - FillMonthModal : modale avec pastille de couleur + nom + période (début → fin) par activité ;
    un message s'affiche si aucune activité ne correspond au mois.
- vérifs : oxlint OK (warnings docs/ pré-existants), vitest 110 tests OK, tsc + vite build OK.

2026-08-18 11:17:41
_err_back : run.log du backend — DataIntegrityViolationException lors de l'enregistrement d'un CRA
déjà rempli : « violation de contrainte fk_cda_cra_day » (delete from cra_day where cra_id=?).
- cause : CraService.save() supprimait les jours du CRA via craDayRepository.deleteByCraId(id)
  (delete JPQL en masse sur cra_day) SANS supprimer d'abord les événements associés
  (cra_day_activity). La FK fk_cda_cra_day (cra_day_activity.cra_day_id -> cra_day.id) bloquait
  donc le delete des jours : le CRA échouait au 2e enregistrement (quand il contient déjà des
  événements), le delete en masse court-circuitant le cascade/orphanRemoval de CraDay.activities.
- fix :
  - CraDayActivityRepository : ajout de deleteByCraId(Long) (JPQL @Modifying
    « delete from CraDayActivity cda where cda.craDay.cra.id = :craId »).
  - CraService : injection de CraDayActivityRepository ; dans save(), suppression dans l'ordre
    FK-safe : craDayActivityRepository.deleteByCraId(id) PUIS craDayRepository.deleteByCraId(id)
    avant de recréer les jours (même convention que deleteByConsultantSocId déjà utilisé par
    SocService.deleteWithDependencies).
- tests : nouveau test d'intégration non transactionnel CraSaveIntegrationTest
  (save_twiceWithActivities_replacesEventsWithoutFkViolation) qui enregistre 2 fois de suite un CRA
  rempli d'événements (reproduit la FK violation avant le fix) + nettoyage @AfterEach en ordre
  FK-safe (cra -> cra_configuration -> activity -> activity_type -> consultant -> soc) pour ne pas
  polluer la base H2 partagée des tests.
- vérifs : mvn test -> 183 tests, seul échec = adminCannotDeleteActiveUser PRÉ-EXISTANT
  (échec d'ordre d'isolation, indépendant de ce correctif) ; frontend non concerné (aucun changement).

2026-08-18 12:10:25
lorsque le consultant envoie un CRA à valider, le manager ne reçoit pas de notification :
il doit la recevoir.
- diagnostic : la notification in-app (entité Notification, cloche) était déjà créée par
  CraService.submit() pour le manager (consultant.getManager() -> findByEmail -> notify()), vérifié
  par un test d'intégration. En revanche aucun EMAIL n'était envoyé au manager, et le badge de la
  cloche n'était pas rafraîchi en temps réel (l'effect ne lisait le compteur non-lus qu'au montage).
- fix backend (soc360-back-java) :
  - CraService : injection d'EmailService ; dans submit(), en plus de la notification in-app, envoi
    d'un email au manager (objet « CRA soumis — <mois> <année> », corps « Bonjour <manager>,
    <consultant> a soumis son CRA pour validation. »). Le code lit désormais manager =
    consultant.getManager() une seule fois (le nom du manager, auparavant calculé mais inutilisé,
    sert au corps de l'email).
  - test : CraServiceTest.submit_notifiesManager (intégration) — crée un manager (UserAccount
    MANAGER), un consultant rattaché à ce manager, remplit et soumet le CRA, puis vérifie qu'une
    notification non lue existe pour le manager (countByUserIdAndReadFalse == 1).
- fix frontend (soc360-front-react) :
  - NotificationBell.tsx : le compteur de notifications non lues est désormais pollé toutes les
    30 s (setInterval + clearInterval dans l'effect), en plus du chargement au montage -> le badge
    du manager se met à jour quand un consultant soumet un CRA, sans rechargement de page.
- vérifs : mvn test -> 184 tests, seul échec = adminCannotDeleteActiveUser PRÉ-EXISTANT (inchangé) ;
  frontend : oxlint OK (warnings pré-existants SocContext/AuthContext), vitest 110 tests OK,
  tsc + vite build OK.

2026-08-18 12:39:13
on doit historiser les échanges de validation/rejet entre consultant et son manager :
date_heure | sender | receiver | comment ; un btn « Historique » affiche dans une dialog la liste
des échanges ; une fois le CRA validé, on remplace le dernier commentaire par :
« validé par <manager> le <date_heure> ».
- backend (soc360-back-java) :
  - migration V27__cra_exchange.sql : table cra_exchange (cra_id FK -> cra ON DELETE CASCADE,
    sender/receiver VARCHAR(120) NOT NULL, comment VARCHAR(500), created_at/updated_at/created_by).
  - CraExchange (entité), CraExchangeRepository (findByCraIdOrderByCreatedAtAsc).
  - CraDtos : record CraExchangeDto(id, dateTime, sender, receiver, comment).
  - CraService : recordExchange() appelé dans submit() (consultant -> manager, comment null),
    reject() (manager -> consultant, comment = motif) et validate() (manager -> consultant,
    comment = « validé par <manager> le <date_heure> ») ; validate() positionne aussi cra.comment
    avec ce même texte ; nouvelle méthode exchanges(craId).
  - CraController : GET /api/cras/{id}/exchanges (avec contrôle d'accès requireSocAccess comme getById).
  - le nom de l'acteur (manager) en validate/reject est lu via SecurityUtils.currentPrincipal()
    .getFullName() avec repli sur le manager du consultant (pour ne pas casser les tests sans contexte).
- frontend (soc360-front-react) :
  - api/types.ts : interface CraExchangeDto ; api/cras.ts : crasApi.exchanges(id).
  - CraDetail.tsx : bouton « Historique » dans l'en-tête -> HistoryModal (dialog) qui liste les
    échanges (sender -> receiver, date/heure via formatDateTime, commentaire).
- tests : CraExchangeIntegrationTest.submit_reject_validate_recordsExchangeHistory (non transactionnel,
  nettoyage @AfterEach incluant notification -> user_account) ; CraServiceTest.submit_notifiesManager.
- vérifs : mvn test -> 185 tests, seul échec = adminCannotDeleteActiveUser PRÉ-EXISTANT (inchangé) ;
  frontend : oxlint OK (warnings pré-existants), vitest 110 tests OK, tsc + vite build OK.
- notes : le .gitignore ignorait `db/` (les migrations Flyway n'étaient pas versionnées, seules V15/V16
  l'étaient) -> retrait de `db/` + commit de toutes les migrations V1..V27.

2026-08-18 12:42:44
changer NotificationBell : pour ne pas solliciter le serveur chaque 30 s, on met une case à cocher
à côté du Bell pour activer ou non la sollicitation du serveur tous les x secondes, x étant la valeur
d'un input text modifiable à côté.
- frontend (src/layout/NotificationBell.tsx) :
  - ajout d'une case à cocher + d'un input text numérique à côté de la cloche (dans l'en-tête).
  - état pollEnabled (défaut false = aucun polling) et intervalSec (défaut « 30 »).
  - l'effect charge le compteur non-lus une fois au montage ; le poll périodique n'est lancé QUE si la
    case est cochée, toutes les max(1, intervalSec) secondes (setInterval + clearInterval).
  - l'input (désactivé tant que la case est décochée) filtre les caractères non numériques.
- vérifs : frontend oxlint OK (warnings pré-existants), vitest 110 tests OK, tsc + vite build OK ;
  backend non concerné.

2026-08-18 14:05:13
page CRA : liste des CRA en table (paginée si > 5 lignes) : annee_mois, consultant, status,
total worked days, submitted at, validated at, comment, Actions (edit, supprimer) ; on ne peut pas
éditer ni supprimer un CRA validé ou soumis ; en bas, un bouton pour ajouter un nouveau CRA.
- backend (soc360-back-java) :
  - CraService.delete(id) : interdit la suppression d'un CRA SUBMITTED ou VALIDATED
    (ApiException.badRequest « Impossible de supprimer un CRA soumis ou validé ») puis
    craRepository.delete(cra) (cascade jours/activités ; cra_exchange via ON DELETE CASCADE).
  - CraController : DELETE /api/cras/{id} (@PreAuthorize CRA_DELETE + ADMIN + RESPONSIBLE_SOC,
    contrôle requireSocAccess).
  - tests : CraExchangeIntegrationTest.delete_draftCra_removesCra et delete_submittedCra_throws.
- frontend (soc360-front-react) :
  - api/cras.ts : crasApi.delete(id).
  - CraList.tsx réécrit : table paginée (5 lignes/page) avec colonnes Année-mois, Consultant, Statut,
    Jours, Soumis le, Validé le, Commentaire, Actions ; Actions = « Éditer » (DRAFT/REJECTED) ou
    « Ouvrir » (SUBMITTED/VALIDATED) + « Supprimer » (masqué si soumis/validé) + Valider/Rejeter
    (manager sur SUBMITTED) ; bouton « Nouveau CRA » en bas (consultant -> getOrCreate + navigation) ;
    filtre par mois (consultant et manager).
  - CraList.test.tsx adapté (suppression colonne Heures, bouton « Nouveau CRA », filtre par mois)
    + test suppression d'un CRA non soumis.
- vérifs : mvn test -> 187 tests, seul échec = adminCannotDeleteActiveUser PRÉ-EXISTANT (inchangé) ;
  frontend : oxlint OK (warnings pré-existants), vitest 111 tests OK, tsc + vite build OK.

2026-08-18 14:05:13
page CRA : à droite de « Période mois année », ajouter 3 boutons prec / auj / suiv ; chaque clic
affiche le CRA correspondant au mois s'il existe.
- frontend (src/pages/CraList.tsx) :
  - helper shiftMonth(year, month, delta) ; boutons ◀ (mois précédent), « auj » (aujourd'hui),
    ▶ (mois suivant) à côté des selects mois/année ; chaque clic met à jour year/month et recharge
    la liste du mois correspondant (findByMonth pour manager, filtre du mois pour consultant).
- vérifs : frontend oxlint OK, vitest 111 tests OK, tsc + vite build OK ; backend non concerné.

2026-08-18 14:56:37
page CRA : table en haut (non filtrée par la période), « Nouveau Cra » juste en dessous (s'ouvre à la
période choisie), champ « Période » en dessous ; « Ouvrir »/« Nouveau Cra » ouvrent le CRA dans la
même page (en dessous) ; la navigation par la période ouvre le CRA (en dessous) s'il existe, sinon
c'est un nouveau.
- backend (soc360-back-java) :
  - CraService.findBySocYear(socId, year) + CraController GET /api/cras/soc/{socId}/{year}
    (isolation via effectiveSocId) : liste des CRA d'une société sur une année (table non filtrée
    par le mois pour les managers/ADMIN).
- frontend (soc360-front-react) :
  - api/cras.ts : crasApi.findBySocYear(socId, year).
  - CraDetail.tsx : refactor — signature CraDetail({ id, onClose, onChange }) (plus de useParams) ;
    nouveau composant CraDetailRoute (useParams) pour la route /cras/:id ; bouton « ← Fermer »
    (onClose) en mode inline vs « ← Retour aux CRA » en mode route ; onChange() appelé après
    save/submit/validate/reject pour rafraîchir la liste parente.
  - App.tsx : route /cras/:id -> CraDetailRoute.
  - CraList.tsx réécrit : table des CRA en haut (année, non filtrée par mois), pagination 5 lignes ;
    « Nouveau Cra » en dessous de la table (consultant -> getOrCreate + ouverture inline) ; champ
    « Période » (mois/année + ◀/auj/▶) en dessous ; « Éditer/Ouvrir » et « Nouveau Cra » ouvrent le
    CRA en ligne (CraDetail inline avec onClose/onChange) ; navigation période (selects et boutons)
    -> consultant : getOrCreate + ouverture inline (crée si absent), manager : mise à jour période.
  - CraList.test.tsx adapté (findBySocYear pour manager, table non filtrée par mois, « Nouveau Cra »,
    ouverture inline mockée avec getById + activitiesApi).
- vérifs : mvn test -> 187 tests, seul échec = adminCannotDeleteActiveUser PRÉ-EXISTANT (inchangé) ;
  frontend : oxlint OK (warnings pré-existants), vitest 111 tests OK, tsc + vite build OK.

2026-08-18 15:20:11
page Clients / Fournisseurs : ajouter un client (ou fournisseur) qui se référence à n'importe quelle
société.
- frontend (soc360-front-react) — Clients.tsx et Suppliers.tsx :
  - pour un non-admin (RESPONSIBLE_SOC), le champ « Société associée » référence désormais n'importe
    quelle société via socParentId (auparavant il mappait socId = société propriétaire, restreint par
    le backend) ; la société propriétaire socId reste la société sélectionnée (selectedSocId/user.socId),
    implicite pour l'isolation multi-société.
  - selectCompany(field, value) remplace selectSoc : auto-remplissage nom/contact/gérant depuis la
    société référencée (socToCompanyLookup) pour socId (ADMIN) ou socParentId (non-admin).
  - openCreate : plus d'auto-remplissage initial ; l'utilisateur choisit la société associée.
  - colonne du tableau renommée dynamiquement : « Société parent » (ADMIN) / « Société associée »
    (non-admin) et affiche désormais socParent.name pour tous.
- backend : aucun changement (socParentId était déjà accepté sans contrôle d'accès ; socId reste
  contrôlé par requireSocAccess et vaut la société de l'utilisateur).
- vérifs : frontend oxlint OK (warnings pré-existants), vitest 111 tests OK, tsc + vite build OK ;
  backend non concerné (aucun changement).

2026-08-18 15:57:10
page CRA : juste avant la table des CRA, un input text pour filtrer les CRA par année-mois,
consultant et statut.
- frontend (src/pages/CraList.tsx) :
  - état search + input « Filtrer par année-mois, consultant ou statut… » affiché avant la table
    (visible dès que les données sont chargées).
  - filtrage du tableau sur : année-mois (AAAA-MM), nom du consultant et libellé de statut
    (insensible à la casse) ; reset de la pagination à la saisie ; état vide adapté
    (« Aucun CRA ne correspond au filtre » vs « Aucun CRA pour cette année »).
- vérifs : frontend oxlint OK, vitest 111 tests OK, tsc + vite build OK ; backend non concerné.

2026-08-18 15:57:10
avant de commiter, mettre à jour la date du dernier commit dans la page menu, en bas.
- frontend :
  - vite.config.ts : __BUILD_DATE__ vaut désormais la date du dernier commit git
    (execSync('git log -1 --format=%ci') parsée en jj/mm/aaaa hh:mm, repli sur la date du build si
    git indisponible) au lieu de la date de build courante.
  - MainLayout.tsx : libellé « Déploiement : … » -> « Dernier commit : … » (bas de la sidebar).
- vérifs : frontend oxlint OK, vitest 111 tests OK, tsc + vite build OK (le bundle embarqué contient
  bien la date du dernier commit, ex. « 18/08/2026 15:46 ») ; backend non concerné.

2026-08-18 16:42:02
CRA : nouvelle propriété type = 'CRA' par défaut ; bouton « Nouveau Congé » (à droite de « Nouveau
Cra ») qui crée un CRA de type 'CONGE' ; champ Période avant le bouton Nouveau Cra ; couleurs de fond
des événements CONGE (jaune) et HOLIDAY (orange).
- backend (soc360-back-java) :
  - migration V28__cra_type.sql : colonne cra.type VARCHAR(20) NOT NULL DEFAULT 'CRA' ; contrainte
    unique étendue à (consultant_id, month_number, period_year, type) pour permettre CRA + CONGE sur
    le même mois.
  - Cra : champ type (String, défaut "CRA") ; CraRepository.findByConsultantIdAndMonthAndYearAndType ;
    CraDtos.CraDto.type.
  - CraService : getOrCreate(..., type) + createForMonth(..., type) — un CRA type CONGE initialise ses
    jours en LEAVE (congé, 0h) au lieu de WORKED ; toDto renvoie type.
  - CraController : GET /cras/consultant/{id}/{year}/{month} accepte ?type= (défaut CRA).
  - DashboardService + DashboardServiceTest adaptés au renommage du repository.
- frontend (soc360-front-react) :
  - types.ts CraDto.type ; cras.ts getOrCreate(..., type = 'CRA').
  - CraList.tsx : champ Période déplacé avant les boutons ; bouton « Nouveau Congé » (createCra('CONGE'))
    à droite de « Nouveau Cra ».
  - CraDetail.tsx : helper dayBackground — cellules calendrier : LEAVE = jaune (bg-yellow-100),
    PUBLIC_HOLIDAY = orange (bg-orange-100), WORKED = blanc, autres = gris.
- note : remplacement du mécanisme __BUILD_DATE__ par la variable manuelle _LAST_COMMIT_ dans
  MainLayout.tsx (affichée dans le menu), suppression de la ligne « Déploiement/Dernier commit » en bas
  de la sidebar (vite.config.ts et vite-env.d.ts nettoyés).
- vérifs : mvn test -> 187 tests, seul échec = adminCannotDeleteActiveUser PRÉ-EXISTANT (inchangé) ;
  frontend : oxlint OK (warnings pré-existants), vitest 111 tests OK, tsc + vite build OK.

2026-08-18 18:01:46
page CRA : boutons « Nouveau Cra » / « Nouveau Congé » sur une même ligne et centrés, bouton
« Nouveau Congé » jaune ; dans la table des CRA, si une ligne est un congé (type CONGE), la première
cellule (année-mois) a un fond jaune ; au clic sur Nouveau Cra/Nouveau Congé, si le mois affiché a
déjà un CRA rempli (soumis/validé), on avance mois par mois jusqu'à trouver un mois sans CRA ou
partiellement rempli ; bouton + plage de dates (début/fin) pour remplir la plage en évitant les
cellules déjà remplies, les week-ends et les jours fériés.
- frontend (soc360-front-react) :
  - components/ui.tsx : Button accepte une prop variant ('primary' | 'yellow' | 'green'), défaut primary.
  - CraList.tsx : boutons centrés (justify-center) + « Nouveau Congé » variant yellow ; cellule
    « année-mois » en bg-yellow-100 quand cra.type === 'CONGE' ; createCra() -> findFreePeriod(type)
    qui, pour chaque mois à partir du mois courant (jusqu'à 24 mois), regarde le CRA du type donné
    (data de l'année courante, sinon findByConsultant de l'année suivante) et s'arrête au premier mois
    sans CRA ou en DRAFT/REJECTED ; puis getOrCreate + ouvre en ligne et repositionne la période.
  - CraDetail.tsx : bouton « Remplir une plage » + FillRangeModal (date début/fin, liste d'activités du
    mois) ; handleFillRange() remplit les jours WORKED vides de la plage [début, fin] avec l'activité
    choisie (1 j), en évitant les jours déjà remplis, week-ends et jours fériés (dayType != WORKED).
- note : commité aussi l'horloge numérique et les classes font_last_commit / text-horloge du menu
  (travaux précédents non commités).
- vérifs : frontend oxlint OK (warnings pré-existants), vitest 111 tests OK, tsc + vite build OK ;
  backend non concerné (aucun changement).

2026-08-18 18:43:36
lorsque je clique sur « Nouveau Congé », faire pareil que « Nouveau Cra » : chercher la période où il
n'y a aucun CRA rempli.
- frontend (src/pages/CraList.tsx) :
  - findFreePeriod() ne prend plus de type : il recherche désormais un mois où le CRA régulier
    (type 'CRA') est absent ou en DRAFT/REJECTED (donc pas rempli/validé), quel que soit le bouton
    cliqué ; createCra(type) applique la même recherche pour 'CRA' et 'CONGE' puis getOrCreate sur le
    mois trouvé.
- vérifs : frontend oxlint OK (warnings pré-existants), vitest 111 tests OK, tsc + vite build OK ;
  backend non concerné.

2026-08-18 18:50:17
click sur « Nouveau Congé » ne doit pas remplir tout de suite tout le mois par des congés.
- backend (soc360-back-java, CraService.createForMonth) : suppression du remplissage automatique des
  jours en LEAVE pour un CRA de type CONGE — les jours d'un CRA congé sont désormais initialisés comme
  un CRA normal (WORKED pour les jours ouvrés, WEEKEND / PUBLIC_HOLIDAY selon la config), l'utilisateur
  remplit ensuite les congés via la plage de dates.
- vérifs : mvn -Dtest=CraServiceTest,CraExchangeIntegrationTest -> 10 tests OK ; frontend non concerné.

2026-08-18 18:50:17
dans la page Nouveau Cra/Congé : un bouton et un sélecteur de dates (début, fin) pour ajouter une
activité (à choisir) dans cette plage de dates.
- frontend (src/pages/CraDetail.tsx) : déjà en place (bouton « Remplir une plage » + FillRangeModal
  avec dates début/fin et liste d'activités du mois) ; handleFillRange() remplit les jours WORKED vides
  de la plage avec l'activité choisie, en évitant jours remplis/week-ends/fériés.
- vérifs : frontend oxlint OK, vitest 111 tests OK, tsc + vite build OK (aucun changement nécessaire).

2026-08-18 18:52:27
lorsqu'un consultant envoie un CRA ou un congé à valider, son manager doit recevoir une notification
« un CRA / congé à valider ».
- backend (soc360-back-java, CraService.submit) : la notification in-app + l'email envoyés au manager
  distinguent désormais le type — titre « CRA soumis — <mois> <année> » ou « Congé soumis — <mois>
  <année> », corps « <consultant> a soumis son CRA / son congé pour validation. » (selon cra.type).
- vérifs : mvn -Dtest=CraServiceTest -> 7 tests OK (la notification existe déjà via
  submit_notifiesManager) ; frontend non concerné.

2026-08-18 18:55:42
chaque appel au serveur (sauf le chargement des notifications) doit lancer un écran de chargement,
qui se ferme dès que le serveur répond (ok ou ko).
- frontend (soc360-front-react) :
  - api/client.ts : compteur global activeRequests + setLoadingListener() ; request/upload/download
    incrémentent (beginLoading) et décrémentent (endLoading, finally) ce compteur ; les chemins
    commençant par /notifications (poll unread-count, liste, marquer lu, …) sont exemptés pour ne pas
    faire clignoter l'écran en permanence.
  - nouveau composant src/components/GlobalLoading.tsx : overlay plein écran (fond semi-transparent +
    spinner + « Chargement… ») affiché quand activeRequests > 0, masqué sinon.
  - MainLayout.tsx : rendu de <GlobalLoading /> en fin de layout.
- vérifs : frontend oxlint OK (warnings pré-existants), vitest 111 tests OK, tsc + vite build OK ;
  backend non concerné.

2026-08-18 19:04:49
page Documents : partager un document à un autre consultant / manager / responsable_soc de la même
société, ou de façon publique au sein de la société.
- backend (soc360-back-java) :
  - migration V29__document_share.sql : colonne hr_document.visibility (VARCHAR(20) DEFAULT 'PRIVATE')
    + table document_share (document_id -> hr_document ON DELETE CASCADE, user_id -> user_account).
  - HrDocument.visibility (PRIVATE/PUBLIC) ; entités DocumentShare + DocumentShareRepository ;
    HrDocumentRepository.findByVisibilityAndSocId / findByUploadedByAndSocId.
  - DocumentController : upload accepte visibility + sharedWith (liste d'userIds) et crée les
    document_share ; POST /{id}/share (partage d'un document existant) ; findAll pour non
    ADMIN/RESPONSIBLE_SOC retourne public + partagé + uploadé par moi + consultant assigné ;
    requireDocumentAccess() pour le téléchargement (public/uploadé/assigné/partagé sinon 403).
- frontend (soc360-front-react) :
  - types.ts HrDocumentDto.visibility ; documents.ts upload(visibility/sharedWith) + share(id, userIds).
  - Documents.tsx : le formulaire « Partager un document » ajoute « Visibilité » (Privé/Public) et,
    en privé, une liste à cocher « Partager avec » (consultants/managers/responsables via
    consultantsApi.managers).
- vérifs : mvn test -> 187 tests, seul échec = adminCannotDeleteActiveUser PRÉ-EXISTANT (inchangé) ;
  frontend : oxlint OK (warnings pré-existants), vitest 111 tests OK, tsc + vite build OK.

2026-08-18 19:32:00
un congé est valide meme si toutes les cellules ne sont pas toutes remplies ; mais on n'a pas le droit
de mettre une activité qui n'est pas de type congé.
- backend (CraService) : submit() ne vérifie plus la complétude (1j par jour travaillé) quand
  cra.type = CONGE ; save() rejette une activité dont le code de type ne commence pas par « CONGE »
  dans un CRA congé.
- frontend (CraDetail) : incompleteDays / craValid ignorés pour type CONGE (le congé est soumis
  même s'il reste des cellules vides).
- vérifs : CraServiceTest + CraExchangeIntegrationTest 10 OK ; frontend lint OK, vitest 111 OK, build OK.

2026-08-18 19:32:00
lorsqu'on a ouvert un congé (cra de type congé) on affiche un btn en haut qui convertit le congé en
cra ; après toutes les règles du cra reviennent.
- backend : CraService.convertToCra(id) (setType "CRA") + CraController POST /api/cras/{id}/convert
  (requireSocAccess sur le consultaant du cra).
- frontend : crasApi.convertToCra(id) ; dans l'en-tête de CraDetail un bouton « Convertir en CRA »
  visible si cra.type === 'CONGE' et crayon (DRAFT/REJECTED) ; après conversion le congé redevient
  un CRA (activités toutes types, validation complétude…).
- vérifs : CraServiceTest/CraExchangeIntegrationTest 10 OK ; frontend lint/test/build OK.

2026-08-18 19:32:00
lorsqu'on a ouvert un congé, on filtre les activités afin qu'elles soient toutes de type congé ; et
lorsqu'on ouvre un cra, on montre toutes les activités.
- frontend (CraDetail) : memo filteredActivities — si cra.type === 'CONGE', seules les activités dont
  le code de type commence par « CONGE » (CONGE_RTT, CONGE_PAYE, CONGE_NON_PAYE) sont affichées/sélectionnables
  (currentActivity, monthActivities, lignes, modal événement) ; sinon toutes les activités.
- clique+remplissage, remplisser plage, remplir tout le mois utilisent filteredActivities.
- vérifs : frontend lint OK, vitest 111 OK, build OK.

2026-08-18 19:36:00
ajouter un paramètre par user : choix du thème de l'application — liste de 10 thèmes.
- backend : migration V30__user_theme.sql (user_account.theme VARCHAR(30) DEFAULT 'ocean') ;
  UserAccount.theme ; UserDto.theme ; AuthService.updateTheme (10 thèmes autorisés : ocean,
  forest, sunset, lilac, emerald, ruby, amber, sky, slate, rose) + PUT /auth/me/theme.
- frontend : types.ts theme ; authApi.updateTheme ; AuthContext applique data-theme sur
  <html> (comme fontSize) ; index.css : @theme inline brand-* -> variables CSS, palettes
  par [data-theme=...] (10 thèmes) ; Paramètres : sélecteur de thème avec aperçu de couleur.
- vérifs : backend AuthServiceTest+UserControllerTest 31 OK ; frontend lint OK, vitest 111 OK, build OK.

2026-08-18 19:38:00
page CRA — table des cras : si une ligne est de type congé, couleur de fond jaune sur toute la ligne.
- frontend (CraList) : la classe bg-yellow-100 est posée sur le <tr> (toute la ligne) quand
  cra.type === 'CONGE' (précédemment posée seulement sur la première cellule).
- vérifs : frontend lint OK, vitest 111 OK, build OK.

2026-08-18 20:03:00
une fois tout est terminé : maj de la variable _LAST_COMMIT_, commit des 3 projs (soc360-doc,
soc360-front-react, soc360-back-java), deploy frontend + backend (cible dev).
- _LAST_COMMIT_ : MainLayout.tsx -> 18/08/2026 19:51 (commit front d5c495b).
- commits : front 8668fc0 (ligne congé jaune) + e2b29bb (thème) + 09b1436/c24adb3 (congé) +
  d5c495b (_LAST_COMMIT_) ; back d323328 (thème V30) + 3dd0476 (congé) ; doc archives a2e02ca,
  311fb66, 3c279e0 + celui-ci.
- deploy frontend dev : ./deploy_front_to_gh_pages.sh dev -> docs/ build + push GitHub Pages
  (https://mzamouneisi.github.io/soc360-front-react/), commit 62fee61, push main.
- deploy backend dev : ./deploy_app_docker.sh -> image esn360-backend-v2-dev:20260818195605,
  Container App esn360-backend-v2-dev (RG-ESN360-DEV), prête.
- smoke tests : login admin/Admin123! -> HTTP 200 ; GET /auth/me -> theme=ocean,fontSize=22 ;
  PUT /auth/me/theme {forest} -> forest ; reset {ocean} -> ocean (migration V30 appliquée).

2026-08-18 20:12:00
pourquoi le manager d'un consultant (qui peut être un responsable_soc) ne reçoit pas de notification ?
- diagnostic : pour le CRA, CraService.submit notifie déjà le manager (in-app + email) quel que soit
  son rôle (RESPONSIBLE_SOC compris) — vérifié. Le manque venait de la NOTE DE FRAIS :
  NoteFraisService.submit ne notifiait personne (statut seul).
- correction : NoteFraisService.submit notifie désormais le manager du consultant (comme le CRA) :
  notification in-app (NotificationService.create, lien /notes-frais) + email (EmailService.send).
- test : NoteFraisServiceTest.submit_setsStatusAndDate enrichi (manager, verify notification + email),
  + 3 mocks ajoutés. NoteFraisServiceTest 13 OK.

2026-08-18 20:32:00
chaque société peut définir des jours fériés spécifiques dans une année ; elle peut dupliquer dans
l'année suivante via un bouton ; IHM calendrier avec boutons préc / aujourd'hui / suiv.
- backend : migration V31__soc_holiday.sql (table soc_holiday, unique id_soc+date) ; package
  com.soc360.socholiday : SocHoliday (soc/date/label), SocHolidayRepository, SocHolidayService
  (list/create/delete/duplicate), SocHolidayController (/api/soc-holidays).
  Isolation multi-société : list/create/duplicate via SecurityUtils.effectiveSocId, delete via
  requireSocAccess(soc). Écritures réservées ADMIN/RESPONSIBLE_SOC/MANAGER.
- frontend : page /jours-feries (Holidays.tsx) : calendrier mensuel (Préc/Aujourd'hui/Suiv),
  clic sur un jour pour ajouter (libellé) ou supprimer, bouton « Dupliquer vers année+1 » ;
  ajout au menu Activité + route App.tsx + icône calendrier ; socHolidaysApi + SocHolidayDto.
- tests : SocHolidayServiceTest 5 OK ; CraExchangeIntegrationTest 3 OK (Flyway V31 appliqué) ;
  frontend lint OK, vitest 111 OK, build OK.

2026-08-18 20:36:00
page CRA — ajouter un bouton en haut pour rafraîchir la liste des CRA.
- frontend (CraList) : bouton « Actualiser » (icône refresh) dans l'en-tête (PageHeader actions),
  visible pour tous les rôles (consultant compris) ; appelle reload() du hook useAsync.
  Les boutons Exporter CSV/PDF restent réservés aux non-consultants.
- vérifs : frontend lint OK, vitest 111 OK, build OK.

2026-08-18 20:45:00
chaque page : il faut un bouton refresh.
- frontend : composant réutilisable RefreshButton (components/ui.tsx, icône + libellé « Actualiser ») ;
  ajouté en haut de chaque page de données : Dashboard, Clients, Fournisseurs, Projets, Missions,
  Consultants, Activités, Types d'activités, Notes de frais, Facturation, Fiches de paie, Documents,
  Messages, Support, Utilisateurs, Sociétés (SocAdmin), Jours fériés, CRA (CraList + CraDetail), Profil.
  Logs et Tables disposaient déjà d'un bouton de rechargement. Chaque bouton appelle reload() du hook
  useAsync (ajouté là où il manquait : Dashboard, Facturation, Missions, Users, Profile, CraDetail)
  ou un tick de rafraîchissement (Holidays).
- vérifs : frontend lint OK, vitest 111 OK, build OK.

2026-08-18 20:55:00
page Paramètres — choix de la couleur d'en-tête des tables et de la couleur de bordure des tables.
- backend : migration V32__user_table_colors.sql (user_account.table_header_color DEFAULT '#f9fafb',
  table_border_color DEFAULT '#e5e7eb') ; UserAccount + UserDto enrichis ; AuthService.updateTableColors
  (validation hex #RRGGBB) + PUT /auth/me/table-colors.
- frontend : Paramètres : deux sélecteurs de couleur (type color) + aperçu ; AuthContext applique
  --table-header / --table-border sur <html> ; index.css définit les valeurs par défaut ; le composant
  Table + les tables brutes (CraList, Profile, Tables) utilisent var(--table-header)/var(--table-border).
- tests : AuthServiceTest 31 OK, CraExchangeIntegrationTest 3 OK (V32 appliqué) ; frontend 111 OK.

2026-08-18 20:55:00
les lignes des tables sont de couleur légèrement différente selon pair ou impair (zebrage).
- frontend : lignes paires en bg-gray-50 (even:bg-gray-50) sur le composant Table et les tables brutes
  (CraList — sauf lignes congé qui restent jaunes, Profile connexions, Tables).
- vérifs : frontend lint OK, vitest 111 OK, build OK.

2026-08-18 21:05:00
à l'ouverture d'un cra/congé, charger les jours fériés nationaux et aussi spécifiques de la société.
- frontend (CraDetail) : à l'ouverture, chargement parallèle des jours fériés nationaux
  (holidaysApi.findByCountryYear('FR', année)) et des jours fériés de la société
  (socHolidaysApi.list(année), X-SOC-Id auto) ; construction d'une Map date->libellé ;
  les jours du calendrier correspondants (type WORKED sans activité) sont marqués
  PUBLIC_HOLIDAY (fond orange) et affichent leur libellé spécifique (ex. « Fête nationale »,
  libellé de la société).
- vérifs : frontend lint OK, vitest 111 OK, build OK.

2026-08-18 21:12:00
les cellules des en-têtes de tables sont toujours en gras.
- frontend : passage de font-semibold à font-bold sur les cellules d'en-tête (<th>) de toutes les
  tables : composant Table partagé (components/data.tsx), CRA (CraList + tableau "ligne" de
  CraDetail), Profil (historique des connexions), Paramètres (aperçu), Tables (console SQL).
  Les titres de cartes/le calendrier (div) ne sont pas concernés.
- vérifs : frontend lint OK, vitest 111 OK, build OK.

2026-08-18 21:25:00
page paramètres : définir NB_LIGNES_MAX_VIEW (nombre de lignes d'une table qu'on voit, défaut 5,
défini par user) ; le reste vient par boutons Précédent/Suivant. Appliqué à toutes les tables.
- backend : migration V33__user_page_size.sql (user_account.page_size INT DEFAULT 5) ;
  UserAccount.pageSize, UserDto.pageSize, AuthService.updatePageSize (1..100) +
  PUT /auth/me/page-size.
- frontend : Paramètres : sélecteur « Lignes par page » (5/10/15/20/50/100) ;
  composant Table partagé : prop paginate -> pagination client-side (pageSize de useAuth, défaut 5)
  + barre Précédent/Suivant (Pagination) ; prop paginate ajoutée aux 9 pages côté client
  (Activités, Types, Clients, Documents, Fiches paie, Notes frais, Projets, Sociétés, Fournisseurs) ;
  pages paginées serveur (Utilisateurs, Consultants, Support, Messages) : size = user.pageSize ;
  CRA (CraList) : PAGE_SIZE remplacé par user.pageSize.
- test : data.test.tsx mocke useAuth (Table utilise désormais le contexte d'auth). frontend 111 OK.
- vérifs : AuthServiceTest 31 OK, CraExchangeIntegrationTest 3 OK (V33 appliqué) ;
  frontend lint OK, vitest 111 OK, build OK.

2026-08-18 22:49:12
menu Activités & tarifs : bouton « Gérer les types » + correction « liste des types vide »
à l'ajout d'une activité (tâches 1 et 2 de todo_save).
- tâche 2 (bug « liste des types d'activités vide, je suis bloqué ») : cause = le formulaire
  « + Nouvelle activité » chargeait types/projets/consultants seulement si une société était
  résolue (workingSocId). Pour ADMIN (aucune société), workingSocId = null -> listes vides
  -> ajout d'activité bloqué. Pour MANAGER/RESPONSIBLE_SOC (soc défini) l'API renvoyait déjà
  les types (vérifié : /api/activity-types?socId=1 = 14 types).
- frontend (Activities.tsx) :
  - sélecteur « Société * » dans la modale pour ADMIN (liste via socsApi.findAll()) ; au changement
    de société, type/projet/consultant réinitialisés.
  - types/projets/consultants chargés selon effectiveSocId = (ADMIN ? soc choisi dans le formulaire
    : workingSocId) ; handleSubmit utilise le bon socId pour ADMIN (validation soc requise).
  - bouton « Gérer les types » (InlineButton) dans l'en-tête, navigate('/types-activites') vers la
    page dédiée qui permet lister/ajouter/rechercher/modifier/supprimer.
- vérifs : frontend oxlint OK (aucun warning nouveau), tsc -b OK, vitest 111 OK, build OK ;
  endpoints admin testés OK (/api/socs = 4 socs, activity-types/projects/consultants?socId=1 OK).

2026-08-18 23:38:39
sur la page Clients : je click sur modifier une ligne client, une dialog s'ouvre, je vois
« Société associée » — est-ce que ça correspond à id_soc ou id_soc_parent ? je veux que ce soit
id_soc, et ajouter un autre champ liste « Société parente (id_soc_parent) » = la société selected
(aussi appliqué aux Fournisseurs).
- inversion de la sémantique client/fournisseur (confirmé avec l'utilisateur) :
  - id_soc (soc) = société associée (entreprise recherchée, n'importe quelle société, sans
    contrôle d'accès) ;
  - id_soc_parent (socParent) = société parente / société de travail (société sélectionnée) ->
    portée de l'isolation multi-société (effectiveSocId / requireSocAccess).
- backend :
  - ClientRepository / SupplierRepository : ajout findBySocParentId + deleteAllBySocParentId ;
    retrait de detachSocParent (plus de détachement : la société parente = propriétaire, on supprime).
  - ClientService / SupplierService : findAll filtre par socParentId ; create exige socId
    (société associée) et force socParent = effectiveSocId(socParentId) (non-admin -> sa société
    de travail, jamais null) ; update/delete vérifient requireSocAccess sur socParent.
  - SocService : deleteLinkedObjects supprime clients/fournisseurs via deleteAllBySocParentId
    (au lieu de détacher socParent), ordre FK-safe conservé (projets avant clients) ;
    dependencies() liste findBySocParentId.
  - DefaultClientProjectSeeder : le client interne est rattaché à socParent = soc (visible pour
    les utilisateurs de la société).
- frontend :
  - Clients.tsx / Suppliers.tsx : formulaire unifié « Société associée * » (socId, liste de
    toutes les sociétés, auto-remplissage nom/contact/gérant/email/tel) + « Société parente
    (société de travail) » (socParentId : sociétés de l'utilisateur pour non-admin, toutes pour
    admin) ; openCreate présélectionne socParent = société sélectionnée ; colonnes du tableau
    renommées « Société associée » / « Société parente ».
- tests : ClientServiceTest / SupplierServiceTest (findAll par socParent,
  create_nonAdmin_forcesSocParentToCurrentSoc, delete avec contrôle d'accès) ;
  SocDeleteIntegrationTest.deleteSoc_deletesClientsOwnedBySocParent ;
  SecurityIntegrationTest (clients/fournisseurs rattachés via socParent).
- vérifs : mvn test -> 192 tests, seul échec = adminCannotDeleteActiveUser PRÉ-EXISTANT
  (échec d'ordre d'isolation, indépendant) ; frontend : oxlint OK (warnings pré-existants),
  vitest 111 tests OK, tsc + vite build OK.

2026-08-18 23:57:38
cas qui ne fonctionne pas : un consultant (mzamoun) soumet son CRA à son manager (mzamouneisi,
le responsable_soc de sa société Eisi-Consulting) mais mzamouneisi ne reçoit PAS de notification
dans sa cloche (Bell). En revanche, quand mzamouneisi rejette/valide, le consultant reçoit bien
la sienne. À corriger.
- diagnostic (données réelles H2 ./data/soc360) : le consultant « mourad zamoun » (id 66) n'a
  AUCUN manager renseigné (manager_id = null) ; son responsable_soc est « mzamouneisi »
  (id 33, soc 33 = EISI CONSULTING). CraService.submit() ne notifiait que consultant.getManager()
  -> null -> aucune notification. C'est le même défaut sur NoteFraisService.submit().
- fix backend :
  - CraService.submit() : nouvelle méthode notificationRecipients(consultant) — si le consultant a
    un manager explicite, on notifie ce manager ; sinon on notifie le(s) responsable_soc de la
    société du consultant (findBySocIdAndRoleInAndActiveTrue + findBySocIdInSocs pour couvrir le
    multi-société, dédoublonnage par id). Chaque destinataire reçoit la notification in-app
    (lien /cras) + l'email. recordExchange garde le nom des destinataires (joints par virgule).
  - NoteFraisService.submit() : même logique (notificationRecipients + notification in-app
    lien /notes-frais + email).
- tests : CraServiceTest.submit_notifiesResponsibleSocWhenNoManager (consultant sans manager ->
  1 notification pour le responsable_soc) ; NoteFraisServiceTest.submit_notifiesResponsibleWhenNoManager
  + submit_setsStatusAndDate (retrait du stub findByEmail devenu inutile).
- vérifs : mvn test -> 194 tests, seul échec = adminCannotDeleteActiveUser PRÉ-EXISTANT
  (échec d'ordre d'isolation, indépendant) ; frontend non concerné (aucun changement).

2026-08-19 11:05:39
dans un cra ou congé, chaque evenement ne doit pas depasser 1j
- déjà en place (règle « un événement = 0,5 ou 1 jour, max 1 jour par événement ») :
  - backend CraService.save() : rejette (400) tout événement dont `days` <= 0, > 1 ou ≠ 0,5/1
    (« Un événement doit représenter 0,5 ou 1 jour (maximum 1 jour par événement) »), appliqué
    indifféremment aux CRA et aux congés (type CONGE) — la validation n'est pas liée au type.
  - frontend CraDetail.tsx : DAY_VALUES = ['0.5', '1'] -> les sélecteurs « jours » des événements
    (vue ligne + modale d'événements du jour) ne proposent que 0,5 j ou 1 j ; les remplissages
    automatiques (mois, plage) posent days = '1'.
- ajout de tests de régression explicites (CraServiceTest) :
  - save_rejectsEventExceedingOneDay (days = 1.5 -> 400 « maximum 1 jour »)
  - save_rejectsEventInvalidDayValue (days = 0.75 -> 400 « 0,5 ou 1 jour »)
- vérifs : mvn -Dtest=CraServiceTest -> 10 tests OK ; mvn test -> 196 tests, seul échec =
  adminCannotDeleteActiveUser PRÉ-EXISTANT (échec d'ordre d'isolation, indépendant) ;
  frontend non concerné (règle déjà en place).

2026-08-19 11:57:55
un refresh navigateur de http://localhost:5173/cras génère en console :
« In HTML, <div> cannot be a descendant of <p> » / « <p> cannot contain a nested <div> »
- cause : MainLayout.tsx enveloppait l'horloge numérique dans un <p id="_horloge_numerique">
  alors que HorlogeNumerique() retourne un arbre de <div> (bloc) -> HTML invalide
  (<div> descendant d'un <p>), signalé par React comme erreur d'hydratation.
- fix : src/layout/MainLayout.tsx -> le conteneur <p>…</p> de l'horloge devient <div>…</div>
  (id _horloge_numerique conservé). Aucun autre <div> imbriqué dans un <p> ailleurs.
- vérifs : oxlint OK (aucun warning nouveau, warnings pré-existants docs/ + 3 warnings src
  inchangés) ; vitest 111 tests OK ; tsc -b + vite build OK ; backend non concerné.

2026-08-19 12:09:17
page CRA : les actions des btns Exporter CSV ou PDF ne fonctionnent pas -> à remplacer par
un btn « Export Pdf Client » quand on a ouvert un CRA (génère un PDF à envoyer au client des
activités du CRA ; plusieurs clients -> autant de PDF que de clients).
- cause des boutons cassés : open-in-view=false + exportCsv/exportPdf non transactionnels
  -> LazyInitializationException sur les relations LAZY (cra.days, day.activities, activity…)
  au moment de l'export -> 500.
- backend :
  - CraService.exportClientPdf(craId) @Transactional(readOnly=true) : collecte les clients
    distincts des activités du CRA (cra_day_activity -> activity -> project -> client, dédup
    par id) ; 1 client -> 1 PDF ; plusieurs clients (ou activités sans client) -> ZIP contenant
    1 PDF par client (+ « interne.pdf » pour les activités sans client) ; CRA sans activité -> 1 PDF.
  - buildClientPdf : titre « CRA de {société} », « Client : … », « Consultant : … »,
    « Date du CRA : {mois} {année} », « Jours travaillés : » (liste des dates), « Total : … » ;
    les jours/total sont filtrés par client (jours travaillés portant une activité de ce client).
  - CraDtos.ClientExport(byte[] data, String filename, String contentType) ;
    CraController : GET /api/cras/{id}/export/client (requireSocAccess sur la soc du consultant).
  - slug() pour les noms de fichiers/entrées ZIP.
- frontend :
  - api/cras.ts : crasApi.exportClientPdf(id) -> download /cras/{id}/export/client.
  - CraDetail.tsx : bouton « Export Pdf Client » dans l'en-tête du CRA ouvert (à côté d'Historique).
  - CraList.tsx : suppression des boutons « Exporter CSV » / « Exporter PDF » (cassés) + état
    exporting/handleExport/import Spinner retirés.
- tests : CraServiceTest.exportClientPdf_generatesPdfForClient (client + projet + activité +
  1 jour rempli -> PDF non vide, contentType application/pdf, filename contient le client) ;
  CraList.test.tsx : suppression du test d'export CSV/PDF + des assertions des boutons.
- vérifs : mvn -Dtest=CraServiceTest -> 11 tests OK ; mvn test -> 197 tests, seul échec =
  adminCannotDeleteActiveUser PRÉ-EXISTANT (échec d'ordre d'isolation, indépendant) ;
  frontend : oxlint OK (warnings pré-existants inchangés), vitest 110 tests OK,
  tsc -b + vite build OK.

2026-08-19 12:17:50
on ne laisse pas le consultant saisir un autre evenement si le total est > 1j
- backend (CraService.save) : validation ajoutée — le total des `days` d'un jour ne peut pas
  dépasser 1 (sinon 400 « Le total du jour <date> ne peut pas dépasser 1 jour »), en plus de la
  règle existante « 1 événement = 0,5 ou 1 jour ».
- frontend (CraDetail.tsx) :
  - helper allowedDaysFor(day, actIndex) : ne propose dans le select « jours » d'un événement que
    les valeurs qui maintiennent le total du jour ≤ 1 (la valeur courante est toujours conservée
    pour l'affichage d'éventuelles données existantes > 1j).
  - addActivity (vue ligne + modale événement) : refus si le total du jour ≥ 1 (message d'erreur) ;
    l'événement ajouté est pré-rempli à 0,5 j quand il ne reste que 0,5 j, sinon 1 j.
  - updateActivity : garde — refus d'un changement de `days` qui ferait dépasser 1 j.
  - bouton « + Ajouter un événement » désactivé (grisé) quand le total du jour ≥ 1 (les deux vues).
- tests : CraServiceTest.save_rejectsDayTotalExceedingOneDay (2 événements de 1 j -> 400).
- vérifs : mvn -Dtest=CraServiceTest -> 12 tests OK ; mvn test -> 198 tests, seul échec =
  adminCannotDeleteActiveUser PRÉ-EXISTANT (échec d'ordre d'isolation, indépendant) ;
  frontend : oxlint OK (warnings pré-existants inchangés), vitest 110 tests OK,
  tsc -b + vite build OK.

2026-08-19 13:14:33
btn Export Pdf Client — forme de présentation des jours travaillés : une table avec en-tête =
numéros de jours du mois (week-ends en rouge clair), une cellule par jour (nb de jours travaillés,
vide si rien), total en bas ; les congés et la formation ne sont pas comptés (seules les missions
chez le client le sont).
- backend (CraService) :
  - isMissionActivity(Activity) : activité non comptée si type null ou code commençant par « CONGE »
    ou égal à « FORMATION » (congés + formation exclus) ; le reste est considéré « mission client ».
  - clientMatches(Activity, Client) : l'activité doit appartenir au projet du client concerné.
  - exportClientPdf : ne collecte désormais que les clients ayant au moins une activité « mission »
    (congé/formation ignorés) -> plus de PDF vide pour la société mère (proj_interne, activités
    internes/congés).
  - buildClientPdf : la liste de dates est remplacée par un PdfPTable de N colonnes (N = nb de jours
    du mois) : ligne d'en-tête = numéros 1..N (week-ends samedi/dimanche fond rouge clair
    Color(255,214,214)), ligne data = nb de jours par case (vide si 0, format FR « 0,5 ») ;
    « Total : X jour(s) » en bas. isWeekend() calcule samedi/dimanche.
- tests : exportClientPdf_excludesCongeAndFormationActivities (un jour rempli uniquement par un
  congé -> export ZIP générique, aucun PDF par client) ; exportClientPdf_generatesPdfForClient
  (mission DEV -> PDF application/pdf, filename contient le client).
- note technique : OpenPDF 1.3.43 n'a plus com.lowagie.text.BaseColor -> couleur de fond via
  java.awt.Color (PdfPCell.setBackgroundColor(java.awt.Color)).
- vérifs : mvn -Dtest=CraServiceTest -> 13 tests OK ; mvn test -> 199 tests, seul échec =
  adminCannotDeleteActiveUser PRÉ-EXISTANT (échec d'ordre d'isolation, indépendant) ;
  frontend non concerné (aucun changement).

2026-08-19 13:20:56
Export Pdf Client : colorier en jaune la case correspondant au congé.
- backend (CraService.buildClientPdf) : ajout d'un tableau boolean[] conge indexé par jour du mois.
  isCongeDay(CraDay) détecte un jour de congé si dayType == LEAVE ou si le jour contient une
  activité dont le code de type commence par « CONGE ». La ligne « data » du PdfPTable colore la
  case concernée en jaune clair (Color(255,235,150)) — la valeur reste vide (le congé n'est pas
  compté dans le total, cf. isMissionActivity). Le fond rouge clair des week-ends est conservé ;
  un jour de congé (en semaine) n'entre pas en conflit avec les week-ends.
- vérifs : mvn -Dtest=CraServiceTest -> 13 tests OK ; mvn test -> 199 tests, seul échec =
  adminCannotDeleteActiveUser PRÉ-EXISTANT (échec d'ordre d'isolation, indépendant) ;
  frontend non concerné (aucun changement).

2026-08-19 13:59:43
Ajouter btn Export Pdf Ma Societe : on reprend la même chose que Export Pdf Client mais on ajoute
d'autres tables : Congés, Formations, inter_contrat, ...
- backend :
  - CraService.exportCompanyPdf(craId) + endpoint GET /api/cras/{id}/export/company
    (requireSocAccess sur la soc du consultant) : un PDF unique « …-societe.pdf » (pas de ZIP).
  - refactor du rendu PDF : renderSections() partagé entre buildClientPdf et buildCompanyPdf ;
    addDayTable() (table jour du mois : en-tête numéros, week-ends rouge clair, cases jaunes),
    dayCounts(cra, Predicate<Activity>), congeFlags(), sum(), formatDays().
  - buildCompanyPdf : 4 tables successives — « Missions » (activités ni CONGE ni FORMATION ni
    INTER_CONTRAT), « Congés » (CONGE_* + jours dayType LEAVE comptés 1j, cases jaunes),
    « Formations » (FORMATION), « Inter-contrat » (INTER_CONTRAT) ; chaque table a son Total.
  - le PDF client (buildClientPdf) est réécrit sur les mêmes helpers, comportement inchangé.
- frontend :
  - api/cras.ts : crasApi.exportCompanyPdf(id) -> download /cras/{id}/export/company.
  - CraDetail.tsx : bouton « Export Pdf Ma Societe » dans l'en-tête du CRA ouvert (à côté
    d'« Export Pdf Client »).
- tests : CraServiceTest.exportCompanyPdf_generatesPdf (mission -> PDF application/pdf,
  filename « -societe.pdf »).
- vérifs : mvn -Dtest=CraServiceTest -> 14 tests OK ; mvn test -> 200 tests, seul échec =
  adminCannotDeleteActiveUser PRÉ-EXISTANT (échec d'ordre d'isolation, indépendant) ;
  frontend : oxlint OK (warnings pré-existants inchangés), vitest 110 tests OK,
  tsc -b + vite build OK.
